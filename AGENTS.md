# AGENTS.md

## 入口规则

1. 本文件是项目最高优先级规则入口，后续 AI 和开发者进入项目后必须先读本文件。
2. 本文件只写必须遵守的硬规则；详细说明放在 `docs/` 下的中文文档。
3. 如果本文与 `docs/` 下文档冲突，以本文为准。
4. 如果代码现状与文档冲突，先指出冲突，不要编造或自行假设。

## 固定回复规则

1. 所有自然语言回复使用简体中文。
2. 最终答复必须以 `你好，安吉彭于晏 ` 开头，空一行后再写正文。
3. 实事求是，不编造；不确定就说明不确定。
4. 先指出问题，再给方案；不要空泛套话。
5. 不要擅自回退用户已有改动。

## 文档索引

1. `docs/README.md`：文档入口，进入项目后先读。
2. `docs/技术栈.md`：涉及依赖、命令、运行环境时读。
3. `docs/开发规则.md`：涉及代码、前端、数据、文档和验证时读。
4. `docs/通用视频生产流程设计.md`：涉及生产流程、作品工作台、分镜、视频和导出时读。
5. `docs/项目功能清单.md`：需要确认当前已有功能时读。
6. `docs/后续目标与待办.md`：涉及计划、后续目标和未完成事项时读。
7. `docs/模型能力审计与设计.md`：涉及供应商接入、模型能力、参数联动、模型测试和项目模型选择时读。

## 工具和 Skill 规则

1. 涉及前端代码、页面、组件、样式、交互、Vue、TypeScript、Tailwind、TDesign、Pinia、路由、i18n 时，必须遵守 Front Skill。
2. 文档整理不使用文档体系搭建类 skill，除非用户明确要求。
3. 修改文件前先确认现有文件和上下文，不凭空改。
4. 查找文本或文件优先使用 `rg`。

## Node / pnpm 规则

1. 本项目运行 Node、npm、pnpm 相关命令时，必须使用用户本机环境。
2. pnpm 固定使用 `D:\software\nodejs\pnpm.cmd`。
3. 禁止直接使用 `pnpm`、`npx pnpm` 或 Codex 自带 pnpm。
4. 常用命令：

```text
D:\software\nodejs\pnpm.cmd install
D:\software\nodejs\pnpm.cmd run dev
D:\software\nodejs\pnpm.cmd run typecheck
D:\software\nodejs\pnpm.cmd run build
```

## 文档规则

1. `docs/README.md` 只做文档入口，不写阶段计划、执行过程和长篇设计。
2. `docs/项目功能清单.md` 只写当前项目已有功能，不写未来计划。
3. `docs/后续目标与待办.md` 是唯一长期 TODO 文档。
4. 不新增复杂文档体系；优先保持 `docs/` 根目录下少量中文文档。
5. 旧 AI 短剧方案、参考项目分析、HTML 原型不再作为当前规范保留。
6. 文档内容要让后续 AI 能直接执行：写清楚目标、边界、禁止项、验收标准。

## 通用视频生产基本架构

1. 本项目是通用视频生产工作流，不得把主流程设计成只适用于 AI 短剧。短剧、小说推文、古诗动画等只是不同内容类型，应共用统一生产对象和主流程。
2. AI 工具负责可选的上游内容处理，例如把长内容拆成多个作品；一个作品对应最终导出的一条完整视频。
3. 作品工作台的固定主流程为：`内容 -> 资源 -> 分镜 -> 分镜画面 -> 视频 -> 导出`。
4. 分镜拆分是可选能力，不增加“整段制作/分段制作”等前置制作方式选择，也不保存不必要的模式字段；但生产结构始终至少包含一条分镜。
5. 用户点击“智能拆分”时生成多个分镜，点击“新增分镜”时手动添加；用户不拆分时，必须把整个作品内容保存为一条真实分镜，不得使用空 `storyboardIds` 表示新作品的整篇制作。
6. 视频步骤始终按分镜生成一个或多个视频片段，并按分镜顺序合成最终作品；不拆分时，整篇分镜对应唯一视频片段。
7. 界面统一使用“视频片段”描述顺序生成的内容，不使用容易被理解为剧情分支的“路线”。内部可以继续复用 `videoTrack` 数据结构。
8. “导演计划”不得作为顶部独立步骤或要求用户编辑的中间文本。它只作为智能拆分分镜时的内部规划能力，已有 `directorPlan/script_plan` 字段可暂时保留用于兼容旧数据、任务追踪和 AI 上下文。
9. 智能拆分应根据内容类型加载对应规划规则，例如短剧关注场次、台词和情绪，小说推文关注旁白段落和信息节点，古诗动画关注诗句、意象和节奏；用户只审核结构化分镜结果，不处理导演计划或分镜表 Markdown。
10. 分镜应关联角色、场景、道具等资源；视频片段可以生成多个候选，用户选择正式候选后才能进入正式合成或导出。
11. 可以吸收 Toonflow 的分镜资源关联、视频轨道、候选视频和正式候选机制，但不得照搬其把 `scriptPlan`、`storyboardTable` 作为用户主步骤的设计。
12. 调整生产流程时优先复用当前项目已有的 `ProductionStoryboardItem`、`ProductionVideoTrackItem`、视频候选和 `selectedVideoId` 结构，不在没有必要时推倒重建数据模型。
13. 作品工作台每个步骤都必须提供“规则与手册”查看入口，展示该步骤实际读取的内置规则、Skill、Prompt 模板、视觉手册和导演手册；不得把未参与当前操作的规则标记为已使用，生成结果还应保留可追溯快照。

## 模型能力硬规则

1. 模型能力、参数联动和供应商 adapter 不兼容旧能力格式，不使用通用模式、时长、分辨率或输入数量兜底。
2. 连接、模型、操作或能力字段不完整时，直接标记为不可用，不猜测、不自动补值、不进入项目和工作台选择器。
3. 模型能力协议发生变化后必须重启主进程，不允许渲染进程混用旧响应和新逻辑。
4. 已废弃或能力不完整的模型不进入新项目；已有项目引用时要求用户重新选择，不静默替换。
