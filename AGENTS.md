# AGENTS.md

## 固定规则

1. 所有自然语言回复使用简体中文。
2. 最终答复必须以 `你好，安吉彭于晏 ` 开头，空一行后再写正文。
3. 实事求是，不编造；不确定就说明不确定。
4. 先指出问题，再给方案；不要空泛套话。
5. 不要擅自回退用户已有改动。

## 工具和 Skill 规则

1. 涉及前端代码、页面、组件、样式、交互、Vue、TypeScript、Tailwind、TDesign、Pinia、路由、i18n 时，必须遵守 Front Skill。
2. 不要使用文档体系搭建类 skill，除非用户明确要求。
3. 修改文件前先确认现有文件和上下文，不凭空改。
4. 查找文件或文本优先使用 `rg`。
5. 手动改文件使用 `apply_patch`。

## Node / pnpm 规则

1. 本项目运行 Node、npm、pnpm 相关命令时，使用用户本机环境。
2. pnpm 固定使用 `D:\software\nodejs\pnpm.cmd`。
3. 禁止直接使用 `pnpm`、`npx pnpm` 或 Codex 自带 pnpm。

## 文档规则

1. `docs/README.md` 是项目功能清单，只写当前项目有哪些功能。
2. 不要在功能清单里写阶段计划、执行进度、交接记录、阅读顺序或整理过程。
3. 未经用户明确要求，不新增复杂文档体系。
