# 功能实现地图

## 1. 职责

本文档是入口，不写所有细节。

```txt
看长期规则：00-项目规范.md
看菜单列表：02-菜单功能清单.md
看当前进度：03-执行进度.md
看偏差结论：04-对齐验收与偏差记录.md
看后续批次：05-后续执行计划.md
看功能实现：features/
看单功能执行：tasks/
```

后续 AI 开工前必须先读 `00`，再读本文档，再读对应 `features/M-xxx`，最后再写或读取对应 `tasks/xxx`。

## 2. 参考项目

```txt
参考产品：Toonflow
参考前端：D:\project\短视频\Toonflow-web-master
参考后端：D:\project\短视频\Toonflow-app-master
```

## 3. 功能实现文档索引

| 顺序 | 文档 | 内容 | 状态 |
|---|---|---|---|
| 0 | `features/00-公共底层.md` | 数据库、文件、任务、模型、Socket、登录、错误 | 已整理 |
| 1 | `features/M-001-全局导航与项目入口.md` | 全局菜单、顶部菜单、设置入口、登录跳转、外链 | 已列入 features |
| 2 | `features/M-002-设置.md` | 供应商、模型服务、Agent、提示词、模型与提示词映射、数据库、文件、Skill、记忆等 | 已整理 |
| 3 | `features/M-003-项目管理.md` | 新建、编辑、删除、打开、模型配置、视觉/导演手册 | 已列入 features |
| 4 | `features/M-004-小说原文.md` | 导入、列表、编辑、删除、事件分析、事件管理 | 已列入 features |
| 5 | `features/M-011-剧本Agent.md` | Agent 对话、计划数据、原文上下文、记忆、剧本写入 | 已列入 features |
| 6 | `features/M-005-剧本.md` | 新增、批量新增、编辑、删除、搜索、导出、资产提取 | 已列入 features |
| 7 | `features/M-006-资产中心.md` | 角色、场景、道具、素材、音频、提示词、图片生成 | 已列入 features |
| 8 | `features/M-007-角景音频绑定.md` | 资产音频绑定、批量绑定、音频生成轮询 | 已列入 features |
| 9 | `features/M-008-生产工作台.md` | 流程图、Agent、分镜、图片、视频工作台、轨道 | 已列入 features |
| 10 | `features/M-009-任务中心.md` | 任务列表、筛选、状态、失败原因、刷新 | 已列入 features |
| 11 | `features/M-010-导出.md` | 时间线构建、素材校验、剪映草稿导出 | 已列入 features |

## 4. features 文档必须写到什么程度

```txt
页面入口在哪里
页面有什么区域
有哪些按钮和操作
每个按钮会打开什么弹窗或触发什么动作
表单有哪些字段、字段来源、默认值、是否必填
字段的 Toonflow 源码名是什么
字段的业务语义是什么
前端调用哪个接口
接口参数是什么
接口返回后页面怎么变
后端读写哪些表
表名只作为参考源码名，不代表 VT Studio 正式命名
是否读写本地文件
是否创建任务记录
任务状态怎么流转
是否轮询或 Socket
是否调用模型
模型调用走 SDK、vendor 脚本、HTTP 还是 ai-sdk
成功怎么提示
失败怎么提示
删除有没有级联影响
哪些点不能随意改
每个功能缺少哪些实现逻辑和细节
哪些源码未找到
哪些需要用户确认
```

`features/M-xxx.md` 里不要重复大段 VT Studio 总落地规则。总规则在 `00-项目规范.md`。功能文档只写本功能特殊点。

## 5. 不确定项规则

```txt
已经查过但没找到：写“源码未找到”
需要产品取舍：写“需要你确认”
参考项目没有但 VT Studio 要新增：写“VT Studio 增强能力”
需要 VT Studio 改动：写入 04 偏差记录
```

不要写模糊占位词。没有源码事实就写“源码未找到”，需要产品取舍就写“需要你确认”。

## 6. 开工规则

```txt
1. 先读对应 features 文档
2. 重新打开参考项目源码核对
3. 创建 tasks/xxx.md
4. tasks 开头必须先写“快速理解”，用通俗语言说明这一步要做什么
5. tasks 必须写“要做什么功能”，逐项写清每个功能怎么做
6. tasks 结尾必须写“最后大白话”，说明我准备怎么做、不会做什么
7. 优化 tasks 到可执行粒度
8. 用户确认 tasks 后再写代码
9. 完成后更新 03 和 04
```
