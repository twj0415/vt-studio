# M-004 小说/原文

状态：已补源码事实和缺少逻辑；P5 原文输入批已完成

## 1. 参考源码

Toonflow-web：

```txt
src/views/novel/index.vue
src/views/novel/components/importNovel.vue
src/views/novel/components/editNodel.vue
src/views/novel/components/event.vue
src/views/novel/components/eventAnalysis.vue
src/utils/parseNovel.ts
src/stores/setting.ts
```

Toonflow-app：

```txt
src/routes/novel/addNovel.ts
src/routes/novel/getNovel.ts
src/routes/novel/getNovelData.ts
src/routes/novel/getNovelEventState.ts
src/routes/novel/getNovelIndex.ts
src/routes/novel/updateNovel.ts
src/routes/novel/delNovel.ts
src/routes/novel/batchDeleteNovel.ts
src/routes/novel/event/generateEvents.ts
src/routes/novel/event/getEvent.ts
src/routes/novel/event/deletEvent.ts
src/routes/novel/event/batchDeleteEvent.ts
src/utils/cleanNovel.ts
src/lib/fixDB.ts
src/lib/initDB.ts
```

## 2. 菜单入口和页面结构

入口：项目内顶部菜单「小说/原文」，仅 `projectType=novel` 的项目显示。  
页面作用：导入原文、管理章节、按选中章节触发事件分析、查看章节级事件摘要和失败原因。

主页面区域：

```txt
顶部操作栏：导入原文、批量删除、事件分析
搜索区：章节名搜索输入框、搜索按钮
原文章节表格
正文/事件预览弹窗
导入原文弹窗
编辑章节弹窗
```

主表格列：

```txt
多选
index：章节序号
reel：卷
chapter：章节名
chapterData：章节正文预览
event：事件摘要/生成中/失败原因
operation：编辑、删除
```

## 3. 功能拆分

| 功能 ID | 功能 | 要做到什么 |
|---|---|---|
| F-004-001 | 小说/原文导入 | 打开导入弹窗，支持拖拽/点击上传 txt、docx，支持粘贴文本，解析章节，选择章节，保存，自动触发事件摘要生成，关闭后刷新列表 |
| F-004-002 | 原文列表、搜索、分页 | 按当前项目查询章节，支持按章节名搜索，支持分页，显示正文预览、事件摘要、生成中、失败原因 |
| F-004-003 | 编辑原文章节 | 打开编辑弹窗，回填章节名、事件内容、章节正文，保存后刷新列表 |
| F-004-004 | 删除/批量删除原文 | 单条删除和批量删除都有确认，删除章节和事件关联，删除后刷新列表，单条删除最后一条时回退页码 |
| F-004-005 | 事件分析生成 | 选择章节后二次确认，调用生成接口，把选中章节置为生成中，用 `universalAi` 提取事件摘要 |
| F-004-006 | 事件状态轮询 | 对 `eventState=0` 的章节每 3 秒轮询，完成后更新事件文本，失败后更新失败原因 |
| F-004-007 | 事件管理 | `event.vue` 存在事件列表/删除/批量删除 UI，但主页面未挂载；其数据来自 `o_event/o_eventChapter`，和主页面章节级 `o_novel.event` 不是同一条链路 |

## 4. 页面操作细节

主页面：

```txt
进入页面：调用 /novel/getNovel
离开页面：停止事件状态轮询
导入原文按钮：importNovelShow=true
批量删除按钮：没有选中章节时禁用
事件分析按钮：没有选中章节时禁用
搜索按钮：page=1 后重新查询
表格行可多选，selectedRowKeys 保存选中 id
正文超过 80 字显示“查看详情”
事件超过 80 字显示“查看详情”
eventState=0：事件列显示 loading，编辑/删除按钮禁用
eventState=-1 且 event 为空：事件列显示生成失败按钮，点击预览 errorReason
```

导入弹窗：

```txt
弹窗没有默认 footer，内部用两步 tabs
Step 1：上传文件或粘贴文本
Step 1：显示字符数；内容大于 0 且小于 100 时显示过短提示
Step 1：实时解析章节数量
Step 1：没有内容或没有解析出章节时不能进入下一步
Step 2：展示解析出的章节表格
Step 2：用户勾选要保存的章节
Step 2：显示已选正文总字数
Step 2：点击保存调用 /novel/addNovel
关闭弹窗：清空 content、fileList、selectedRowKeys，activeKey 回到 To1
```

导入文件规则：

```txt
支持 text/plain：用 TextDecoder 读取
支持 docx：MIME 为 application/vnd.openxmlformats-officedocument.wordprocessingml.document，用 mammoth.extractRawText 读取
不支持 doc：MIME 为 application/msword 时提示不支持
不支持其他 MIME：提示文件类型不支持
单文件最大 10MB
上传文件只在 renderer 中读取文本，源码没有保存原始文件副本
```

章节解析规则：

```txt
卷正则：/^(第[\d一二三四五六七八九十百千]+卷)\s*([^\n第]*)/gm
默认章节正则：/第\s*([0-9０-９零一二三四五六七八九十百千万]+)\s*[章回节]\s*([^\n\r]*)/g
如果设置 otherSetting.chapterReg，优先使用用户配置的正则
chapterReg 支持 /表达式/flags 格式，也支持普通字符串直接 new RegExp
没有卷结构时，默认 reel=正文卷
没有章节匹配但文本非空时，生成一个章节：index=1，chapter=""，text=全文
有卷结构时，按卷拆分，再按章节拆分，每卷内章节按 index 排序
中文数字支持零、一、二、三、四、五、六、七、八、九、十、百、千
```

编辑弹窗：

```txt
打开时使用当前行 formData
可编辑章节名 chapter
可编辑事件内容 event
可编辑章节正文 chapterData
不展示 reel 和 index，但保存时会带原值回传
保存成功后 emit select，父页面刷新列表
组件文件名 editNodel 是 Toonflow 拼写旧痕迹，VT Studio 必须改成 editNovel
```

事件分析：

```txt
主页面只对选中章节执行事件分析
点击事件分析后弹确认框，确认后调用 /novel/event/generateEvents
参数 projectId 来自当前项目
参数 novelIds 来自 selectedRowKeys
参数 concurrentCount 来自 otherSetting.assetsBatchGenereateSize
接口返回后清空 selectedRowKeys 并刷新列表
前端没有单独 catch，VT Studio 必须补失败提示
```

事件轮询：

```txt
notCompultedData = tableData.filter(item => !item.eventState)
当前源码用 !eventState 判断，主要覆盖 eventState=0
每 3 秒调用 /novel/getNovelEventState
请求参数 ids 为生成中章节 id
后端只返回 eventState != 0 的章节
返回后更新当前表格行 eventState、event、errorReason
没有生成中章节时停止轮询
```

事件管理组件：

```txt
event.vue 有事件列表、重新生成、生成、删除、批量删除
event.vue 查询 /novel/event/getEvent
event.vue 删除 /novel/event/deletEvent 和 /novel/event/batchDeleteEvent
event.vue 的生成/重新生成调用 /novel/event/generateEvents 但没有传 novelIds
后端 generateEvents 要求 novelIds 必填，因此这里是源码冲突，VT Studio 不能照抄
event.vue 未在 novel/index.vue 中挂载，实际入口源码未找到
eventAnalysis.vue 调用 /novel/event/eventAnalysis，但后端没有该 route，是旧/无效组件
```

## 5. 表单字段和校验

原文章节字段：

| 业务含义 | Toonflow 源码名 | 控件 | 必填 | 字段来源 | 说明 |
|---|---|---|---|---|---|
| 项目 ID | `projectId` | 隐式 | 是 | 当前项目 | 所有原文按项目隔离 |
| 章节序号 | `index` / `chapterIndex` | 解析/隐藏 | 是 | 解析结果或后端顺序生成 | 导入时后端忽略解析 index，按项目已有最大 chapterIndex 继续递增 |
| 卷 | `reel` | 解析/隐藏 | 是 | 解析结果 | 无卷时为 `正文卷` |
| 章节名 | `chapter` | 解析/输入框 | 否 | 解析结果或编辑 | 搜索只查这个字段 |
| 章节正文 | `chapterData` | 文本域 | 是 | 上传/粘贴/编辑 | 事件分析输入 |
| 事件摘要 | `event` | 文本域/展示 | 否 | 模型生成或编辑 | 存在 `o_novel.event` |
| 事件状态 | `eventState` | 状态展示 | 是 | 后端写入 | `0` 生成中，`1` 完成，`-1` 失败 |
| 失败原因 | `errorReason` | 预览弹窗 | 否 | 模型异常 | 失败时必须可见 |

导入弹窗字段：

| 业务含义 | Toonflow 源码名 | 控件 | 必填 | 字段来源 | 说明 |
|---|---|---|---|---|---|
| 上传文件 | `fileList` | `t-upload` | 否 | 本地文件 | 只允许 1 个文件，不自动上传到后端 |
| 粘贴内容 | `content` | textarea | 否 | 用户粘贴/文件读取 | content 和解析结果同时存在才能下一步 |
| 选中章节 | `selectedRowKeys` | 表格多选 | 是 | 用户选择 | 保存前必须至少选一章 |

其他设置字段：

| 业务含义 | Toonflow 源码名 | 控件 | 默认值 | 说明 |
|---|---|---|---|---|
| 章节拆分正则 | `otherSetting.chapterReg` | 设置页 textarea | `/第\s*([0-9０-９零一二三四五六七八九十百千万]+)\s*[章回节]\s*([^\n\r]*)/g` | 导入解析优先使用 |
| 事件分析并发数 | `otherSetting.assetsBatchGenereateSize` | 设置页数字输入 | `5` | 被事件分析复用为 concurrentCount |

## 6. 调用链路

| 用户动作 | Toonflow 前端调用 | Toonflow 后端文件 | 读写对象 | 成功反馈 | 失败反馈 |
|---|---|---|---|---|---|
| 查询章节 | `POST /novel/getNovel` | `routes/novel/getNovel.ts` | `o_novel` | 渲染表格和分页 | 源码没有显式 catch |
| 导入保存 | `POST /novel/addNovel` | `routes/novel/addNovel.ts` | `o_novel`，随后更新事件字段 | 保存成功、关闭弹窗、刷新列表 | 显示接口错误 |
| 编辑章节 | `POST /novel/updateNovel` | `routes/novel/updateNovel.ts` | `o_novel` | 更新成功、刷新列表 | 显示接口错误 |
| 删除章节 | `POST /novel/delNovel` | `routes/novel/delNovel.ts` | `o_novel`、`o_eventChapter`、`o_event` | 删除成功、刷新列表 | 显示接口错误 |
| 批量删除章节 | `POST /novel/batchDeleteNovel` | `routes/novel/batchDeleteNovel.ts` | `o_novel`、`o_eventChapter`、`o_event` | 删除成功、刷新列表 | 源码没有显式 catch |
| 事件分析 | `POST /novel/event/generateEvents` | `routes/novel/event/generateEvents.ts`、`utils/cleanNovel.ts` | `o_novel.event/eventState/errorReason` | 刷新列表，显示生成中 | VT Studio 必须补失败提示 |
| 轮询事件状态 | `POST /novel/getNovelEventState` | `routes/novel/getNovelEventState.ts` | `o_novel` | 更新事件状态 | 控制台打印错误 |
| Agent 读取原文 | `POST /novel/getNovelData` | `routes/novel/getNovelData.ts` | `o_novel` | 返回全部章节 | 显示错误 |
| 事件列表 | `POST /novel/event/getEvent` | `routes/novel/event/getEvent.ts` | `o_event` + `o_eventChapter` + `o_novel` | 渲染事件列表 | 显示错误 |
| 删除事件 | `POST /novel/event/deletEvent` | `routes/novel/event/deletEvent.ts` | `o_event`、`o_eventChapter` | 删除成功 | 显示错误 |
| 批量删除事件 | `POST /novel/event/batchDeleteEvent` | `routes/novel/event/batchDeleteEvent.ts` | `o_event`、`o_eventChapter` | 删除成功 | 显示错误 |

## 7. 数据和状态

数据库：

```txt
Toonflow 原文章节表参考名：o_novel
字段：id, chapterIndex, reel, chapter, chapterData, projectId, eventState, event, errorReason, createTime

Toonflow 事件表参考名：o_event
字段：id, name, detail, createTime

Toonflow 事件章节关系表参考名：o_eventChapter
字段：id, eventId, novelId

VT Studio 不能直接照搬 o_ 表名。
```

章节导入写入：

```txt
后端先查当前 projectId 下最大 chapterIndex
每保存一章，chapterIndex = lastChapterIndex + 1
保存 reel、chapter、chapterData、projectId、createTime
新增章节 eventState 初始为 0
新增后立即启动 cleanNovel 生成章节事件摘要
addNovel 不等待 cleanNovel 全部完成就返回成功
```

事件分析写入：

```txt
generateEvents 先把选中章节 eventState=0、event=null
cleanNovel 逐章调用 universalAi
成功：更新 o_novel.event 为模型输出，eventState=1
失败：更新 o_novel.event=null，eventState=-1，errorReason=错误信息
fixDB 会在应用启动时把残留 eventState=0 改为 -1，并写 errorReason=软件退出导致失败
```

模型调用：

```txt
模型 key：universalAi
调用方式：u.Ai.Text("universalAi").invoke
提示词来源优先级：
  1. o_prompt where type=eventExtraction 的 useData
  2. o_prompt where type=eventExtraction 的 data
  3. utils/getPrompts.ts 中 getPrompts("event") 的默认提示词
模型输出会经过 stripThink
cleanNovel 不创建 o_tasks 任务记录
```

事件列表链路：

```txt
主页面事件摘要：写在 o_novel.event
event.vue 事件列表：读取 o_event/o_eventChapter
当前源码没有找到从 o_novel.event 写入 o_event/o_eventChapter 的 route
所以“章节事件摘要”和“事件列表管理”不是同一条完整闭环
```

本地文件：

```txt
导入原文源码只读取 txt/docx 内容，不保存原始文件副本
VT Studio 如果要保存原文件副本，属于增强能力，需要写偏差记录
```

任务记录：源码没有为原文导入或事件分析写 `o_tasks`。  
状态流转：`0 -> 1` 成功；`0 -> -1` 失败；启动修复把残留 `0 -> -1`。  
轮询/Socket：主页面用 3 秒轮询，无 Socket。  

## 8. 不能漏的逻辑

1. 导入不是直接全量保存，用户要在解析结果里勾选章节。
2. 导入后会自动开始事件摘要生成，新增章节初始 `eventState=0`。
3. 事件分析按钮只分析选中的章节，不是全项目一键分析。
4. 事件生成不写 `o_tasks`，如果 VT Studio 要纳入任务中心，必须作为偏差记录。
5. `editNodel` 是旧拼写，VT Studio 必须改成 `editNovel`。
6. `assetsBatchGenereateSize` 拼写错误但被复用为事件分析并发数，VT Studio 要改成清晰命名。
7. 主页面事件摘要存在 `o_novel.event`，`event.vue` 事件列表读取 `o_event/o_eventChapter`，两者当前源码没有打通。
8. `eventAnalysis.vue` 调用不存在的后端 route，不能作为实现依据。
9. 单条/批量删除章节源码用 `o_eventChapter` 的 `id` 去删 `o_event.id`，不是 `eventId`，这是源码风险，VT Studio 必须修正。
10. 导入预览表格 row-key 使用 `index`，不同卷出现相同章节序号时可能影响选择，VT Studio 必须用稳定临时 id。
11. 主页面单条删除成功提示源码会触发两次，VT Studio 必须修正为一次。
12. 事件分析接口返回后前端没有失败 catch，VT Studio 必须补失败提示。

## 9. 源码未找到

```txt
event.vue 在当前路由或 novel/index.vue 中的挂载入口源码未找到
/novel/event/eventAnalysis 后端 route 源码未找到
o_novel.event 自动同步到 o_event/o_eventChapter 的实现源码未找到
导入原文保存原始文件副本的实现源码未找到
原文列表按正文、卷、事件内容搜索的实现源码未找到；已找到只按 chapter 搜索
```

## 10. 需要你确认

| 编号 | 问题 | 建议 |
|---|---|---|
| C-004-001 | VT Studio 菜单叫“小说”还是“原文” | 建议 UI 叫“原文”，文档保留小说/原文映射 |
| C-004-002 | 导入原文是否保存原始文件副本 | 建议保存到项目业务目录，便于追溯；这是 VT Studio 增强能力 |
| C-004-003 | 事件分析是否进入任务中心 | 建议进入任务中心，但要记录为与 Toonflow 的偏差 |
| C-004-004 | 是否保留独立事件列表管理 | 建议先不做旧 `event.vue`，先完成章节级事件摘要；如果要做事件库，需要重新设计 `o_novel.event -> events` 的同步规则 |

## 11. 缺少的逻辑和实现细节

### F-004-001 小说/原文导入

参考项目导入是“解析 -> 勾选 -> 保存章节 -> 自动事件摘要”，不是上传后直接全量入库。

必须明确：

```txt
支持文件：txt/docx
拆章规则：来自设置里的 chapterReg
非法正则：保存设置时要拦截；导入时仍要兜底报错
预览列表：必须有稳定临时 id，不能用 chapterIndex 当 row-key
用户动作：勾选章节后保存
保存后：按当前项目最大 chapterIndex 继续累加
原文件副本：Toonflow 未保存；VT Studio 若保存，路径放项目业务目录并写偏差
```

### F-004-002 原文列表、搜索、分页

参考项目只确认按章节名搜索。正文、卷名、事件内容搜索没有源码事实。

必须明确：

```txt
列表范围：只查当前项目
搜索字段：先按 chapter 对齐参考项目
分页：后端分页，切搜索重置第 1 页
eventState 显示：未生成、生成中、成功、失败
失败原因：必须能看到 errorReason
```

### F-004-003 编辑原文章节

编辑章节会影响后续事件摘要和剧本 Agent 上下文。

必须明确：

```txt
可编辑字段：卷、章节名、正文
正文变更后：必须把事件摘要标记为需要重新分析，不能继续使用旧 event
保存后：刷新列表
如果该章节正在事件分析：禁止编辑或提示会取消/重置分析
```

### F-004-004 删除/批量删除原文

参考源码删除事件关系有明显风险，VT Studio 必须修正。

必须明确：

```txt
删除章节：同步删除章节事件关系
删除事件：必须按 eventId 删除，不按关系表 id 误删事件
批量删除：事务化处理，任一失败要回滚
正在事件分析的章节：禁止删除或先取消任务
删除后：刷新分页，当前页为空时回退上一页
```

### F-004-005 / F-004-006 事件分析生成和轮询

参考项目事件分析不写任务中心，靠章节字段和 3 秒轮询。

必须明确：

```txt
触发范围：只分析用户选中的章节
开始状态：eventState=0，event=null
成功状态：eventState=1，写 event
失败状态：eventState=-1，写 errorReason
异常退出修复：启动时把残留 eventState=0 改失败
VT Studio 如果进入任务中心：任务状态和章节 eventState 要同步
```

### F-004-007 事件管理

参考项目 `event.vue` 和主列表的 `o_novel.event` 没有找到闭环。

必须明确：

```txt
第一版建议只做章节级事件摘要
如果做独立事件库：必须设计 event 和 chapter 的同步规则
旧 eventAnalysis.vue 调用的 route 未找到，不能照它做
剧本 Agent 读取上下文时以章节 event 为准，不依赖未闭环的事件库
```
