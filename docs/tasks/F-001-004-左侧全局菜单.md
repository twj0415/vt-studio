# F-001-004 左侧全局菜单

状态：已完成（由 P2 批次覆盖）  
所属菜单：M-001 全局导航与项目入口  
对应功能文档：`docs/features/M-001-全局导航与项目入口.md`  
原则：先确认本文档，再改代码

## 0. 快速理解

```txt
一句话：做应用左侧固定菜单，只放“项目”和“任务中心”这种全局入口。
为什么现在做：项目内菜单必须和全局菜单分开，否则后面页面会混乱。
做完后有什么用：无论有没有打开项目，都能进入项目列表和任务中心。
这一步不碰什么：不做小说、剧本、资产、生产这些项目内入口。
```

## 1. 本次做什么

```txt
目标：
  对齐 Toonflow 左侧全局菜单。

只做：
  1. 左侧全局导航布局。
  2. 项目入口。
  3. 任务中心入口。
  4. 当前路由高亮。
  5. 无项目状态可用。

不做：
  1. 不做项目内顶部菜单。
  2. 不做设置弹窗。
  3. 不做登录路由。
  4. 不做反馈外链。
```

## 2. 参考项目怎么做

| 参考文件 | 关键逻辑 |
|---|---|
| `Toonflow-web/src/pages/workbench/index.vue` | 左侧全局菜单固定为项目、任务中心 |
| `Toonflow-web/src/router/index.ts` | 当前路由 path 决定菜单高亮 |

参考项目关键事实：

```txt
1. 左侧全局菜单不放小说、剧本、角景、生产、资产。
2. 项目内功能只有打开项目后才出现在顶部菜单。
3. 全局菜单项是固定配置，不从数据库读取。
4. 无项目状态下仍可进入项目和任务中心。
5. Toonflow 代码里有 `needProject` 判断，但当前左侧菜单没有使用。
```

## 3. 用户操作

```txt
入口：
  应用主窗口左侧菜单。

按钮/操作：
  1. 点击项目。
  2. 点击任务中心。

弹窗/表单：
  无。

成功反馈：
  路由切换，菜单高亮更新。

失败反馈：
  路由跳转失败时提示。
```

## 4. 要做什么功能

### 1. 全局菜单渲染

怎么做：
- 输入：固定菜单配置。
- 输出：左侧菜单。
- 写什么数据：不写。
- 状态怎么变：页面布局显示。
- 异常怎么处理：无。
- 限制：菜单项固定，不从数据库动态生成。

### 2. 项目入口

怎么做：
- 输入：点击项目。
- 输出：跳转 `/project`。
- 写什么数据：不写。
- 状态怎么变：路由切换，activeMenu 变为 `/project`。
- 异常怎么处理：跳转失败显示错误。
- 限制：不要求当前项目。

### 3. 任务中心入口

怎么做：
- 输入：点击任务中心。
- 输出：跳转 `/task`。
- 写什么数据：不写。
- 状态怎么变：路由切换，activeMenu 变为 `/task`。
- 异常怎么处理：跳转失败显示错误。
- 限制：任务中心必须能看全部项目任务，不依赖 currentProject。

### 4. 菜单高亮

怎么做：
- 输入：当前 route.path。
- 输出：active menu key。
- 写什么数据：不写。
- 状态怎么变：对应菜单高亮。
- 异常怎么处理：未知路由不高亮或回退项目。
- 限制：不能用 currentProject 判断全局菜单高亮。

## 5. 数据和状态

```txt
字段：
  route.path
  globalMenu.key
  activeGlobalMenu

接口/能力：
  router.push

数据读写：
  无。

任务状态：
  无。

轮询/Socket：
  无。

模型调用：
  无。

删除影响：
  无。
```

## 6. VT Studio 怎么落

```txt
能力名：
  globalNavigation

调用链：
  renderer layout -> router

需要新增：
  左侧菜单组件
  全局菜单配置常量

需要修改：
  主布局把项目内顶部菜单和左侧全局菜单分开
```

## 7. 偏差

```txt
和 Toonflow 不同的地方：
  无业务偏差。

原因：
  对齐参考项目。

是否写入 04：
  否。
```

## 8. 验收

```txt
1. 左侧菜单只显示项目和任务中心等全局入口。
2. 未打开项目也能进入项目页。
3. 未打开项目也能进入任务中心。
4. 小说、剧本、资产、生产不出现在左侧全局菜单。
5. 当前路由高亮正确。
6. 刷新页面后高亮仍正确。
```

## 9. 用户确认点

| 编号 | 确认点 | 专业建议 |
|---|---|---|
| C-F-001-004-001 | 是否把小说/剧本/资产放左侧 | 不放，对齐参考项目 |
| C-F-001-004-002 | 任务中心是否依赖当前项目 | 不依赖，必须能看全部项目 |

## 10. 执行后记录

```txt
改了哪些文件：（完成后填写）
验证结果：（完成后填写）
未完成事项：（完成后填写）
最终结论：（完成后填写）
```

## 11. 最后大白话

```txt
我这次准备怎么做：
1. 左侧只放全局入口。
2. 项目和任务中心随时能点。
3. 高亮只跟路由走。

我不会做什么：
1. 不做顶部项目菜单。
2. 不把项目内功能塞到左侧。

确认规则：
用户确认后才执行；未确认前只改文档。
如果本任务有需要用户确认点，或执行中新增确认点，必须先停下来问用户；未确认前不能开始或继续实现。
```
