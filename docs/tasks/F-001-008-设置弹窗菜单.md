# F-001-008 设置弹窗菜单

状态：已完成（由 P2 批次覆盖）  
所属菜单：M-001 全局导航与项目入口  
对应功能文档：`docs/features/M-001-全局导航与项目入口.md`  
原则：先确认本文档，再改代码

## 0. 快速理解

```txt
一句话：设置弹窗左侧列出 16 个设置模块，点击切换右侧内容。
为什么现在做：设置入口只是壳，菜单结构要先固定，后续 M-002 每个设置功能才有挂载位置。
做完后有什么用：所有设置模块都有统一入口和稳定 key。
这一步不碰什么：不实现每个设置模块的业务表单。
```

## 1. 本次做什么

```txt
目标：
  对齐 Toonflow 的设置弹窗菜单结构，并修正旧拼写。

只做：
  1. 设置菜单配置。
  2. 16 个设置模块 key。
  3. 点击切换 activeMenu。
  4. about 菜单更新红点。
  5. 未实现模块显示占位或加载失败提示。

不做：
  1. 不做模块具体功能。
  2. 不做设置数据读写。
  3. 不做退出登录动作。
```

## 2. 参考项目怎么做

| 参考文件 | 关键逻辑 |
|---|---|
| `Toonflow-web/src/components/setting/index.vue` | `t-dialog` 内左侧 `t-menu`，点击设置 `activeMenu` |
| `Toonflow-web/src/components/setting/components/*` | 16 个设置组件 |
| `Toonflow-web/src/stores/setting.ts` | 保存 `activeMenu`、`needUpdate` |

参考项目关键事实：

```txt
1. 设置弹窗左侧有 16 个菜单。
2. `agentConfog` 是旧拼写错误，正式命名应为 `agentConfig`。
3. about 菜单在 needUpdate 时显示红点。
4. activeMenu 默认是 ui。
5. 菜单 key 决定右侧加载哪个设置组件。
```

## 3. 用户操作

```txt
入口：
  打开设置弹窗后左侧设置菜单。

按钮/操作：
  点击外观、语言、模型供应商、模型映射、Agent、提示词、Skill、记忆、用户、数据库、文件、其他、请求、开发者、关于、退出登录。

弹窗/表单：
  设置弹窗内左侧菜单和右侧内容区。

成功反馈：
  activeMenu 切换，右侧对应模块显示。

失败反馈：
  模块未实现或加载失败时显示明确占位。
```

## 4. 要做什么功能

### 1. 设置菜单配置

怎么做：
- 输入：固定设置菜单列表。
- 输出：左侧菜单。
- 写什么数据：不写。
- 状态怎么变：弹窗打开后显示菜单。
- 异常怎么处理：配置缺失时显示错误占位。
- 限制：设置菜单固定，不从数据库动态生成。

### 2. activeMenu 切换

怎么做：
- 输入：点击菜单 key。
- 输出：当前 activeMenu。
- 写什么数据：写前端状态。
- 状态怎么变：右侧组件切换。
- 异常怎么处理：未知 key 回退 `ui`。
- 限制：关闭弹窗不重置 activeMenu。

### 3. about 更新红点

怎么做：
- 输入：`needUpdate` 和菜单 key。
- 输出：about 菜单显示红点。
- 写什么数据：不写。
- 状态怎么变：needUpdate true 时 about 显示 dot。
- 异常怎么处理：无。
- 限制：只有 about 显示更新红点。

### 4. 旧 key 修正

怎么做：
- 输入：参考项目 key。
- 输出：VT Studio 正式 key。
- 写什么数据：不写。
- 状态怎么变：无。
- 异常怎么处理：无。
- 限制：`agentConfog` 改成 `agentConfig`，不能继承错字。

## 5. 数据和状态

```txt
字段：
  settingStore.activeMenu
  settingStore.needUpdate
  settingMenu.key
  settingMenu.labelKey

接口/能力：
  settings.setActiveMenu

数据读写：
  写前端状态。
  不写 SQLite。

任务状态：
  无。

轮询/Socket：
  无。

模型调用：
  无。

删除影响：
  无。
```

## 6. VT Studio 怎么落

```txt
能力名：
  settingDialogMenu

调用链：
  renderer setting dialog -> setting menu config -> setting store -> component map

需要新增：
  设置菜单配置
  组件映射表
  unknown key fallback

需要修改：
  设置弹窗接入菜单和右侧内容区
```

正式菜单 key：

```txt
ui
language
vendorConfig
modelMap
agentConfig
promptManage
skillManagement
memoryConfig
loginConfig
dbConfig
fileManagement
otherConfig
requestConfig
devConfig
about
logoutConfig
```

## 7. 偏差

```txt
和 Toonflow 不同的地方：
  `agentConfog` 改为 `agentConfig`。

原因：
  旧源码拼写错误，VT Studio 正式 key 不能继承错字。

是否写入 04：
  否，属于命名修正。
```

## 8. 验收

```txt
1. 设置弹窗左侧显示 16 个菜单。
2. 默认 activeMenu 是 ui。
3. 点击菜单能切换右侧内容。
4. 关闭再打开仍保留 activeMenu。
5. needUpdate=true 时 about 显示红点。
6. 未实现模块显示占位，不白屏。
7. 正式代码不出现 `agentConfog`。
```

## 9. 用户确认点

| 编号 | 确认点 | 专业建议 |
|---|---|---|
| C-F-001-008-001 | `agentConfog` 是否照搬 | 不照搬，改 `agentConfig` |
| C-F-001-008-002 | 设置菜单是否动态读取 | 不动态读取，固定配置 |

## 10. 执行后记录

```txt
改了哪些文件：（完成后填写）
验证结果：（完成后填写）
未完成事项：（完成后填写）
最终结论：（完成后填写）
```

## 11. 最后大白话

```txt
我这次准备怎么做：
1. 设置弹窗左侧固定放 16 个入口。
2. 点哪个，右侧就显示哪个模块。
3. 老项目拼错的 key 不照搬。

我不会做什么：
1. 不实现每个设置表单。
2. 不把设置菜单做成数据库配置。
3. 不把 about 红点用于别的意思。

确认规则：
用户确认后才执行；未确认前只改文档。
如果本任务有需要用户确认点，或执行中新增确认点，必须先停下来问用户；未确认前不能开始或继续实现。
```
