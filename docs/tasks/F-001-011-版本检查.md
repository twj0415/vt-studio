# F-001-011 版本检查

状态：已完成（由 P2 批次覆盖）  
所属菜单：M-001 全局导航与项目入口  
对应功能文档：`docs/features/M-001-全局导航与项目入口.md`  
原则：先确认本文档，再改代码

## 0. 快速理解

```txt
一句话：检查当前应用版本是否有更新，有更新就显示提醒和设置红点。
为什么现在做：版本检查会影响全局红点和关于页入口，必须先把规则定清楚。
做完后有什么用：用户能知道是否有新版本，但开发期不自动乱联网。
这一步不碰什么：不做下载安装细节，不写更新器。
```

## 1. 本次做什么

```txt
目标：
  对齐 Toonflow 版本检查语义，但清理 Toonflow 更新源，并把开发期默认设为手动检查。

只做：
  1. 手动版本检查入口。
  2. 检查中状态。
  3. 有更新时设置 needUpdate。
  4. 有更新时提示用户。
  5. 打开设置 about 的入口。
  6. 检查失败提示。
  7. 自动检查规则先关闭或可配置。

不做：
  1. 不使用 Toonflow OSS update.json。
  2. 不做下载安装。
  3. 不做增量更新。
  4. 不在开发期默认自动联网。
```

## 2. 参考项目怎么做

| 参考文件 | 关键逻辑 |
|---|---|
| `Toonflow-web/src/pages/workbench/index.vue` | `startVersionCheck()` 立即检查并每 2 分钟检查；needUpdate 后停止轮询 |
| `Toonflow-app/src/routes/setting/about/checkUpdate.ts` | 根据 source/url 获取 update.json，比对 Major/Minor/Patch |
| `Toonflow-app/src/routes/other/getVersion.ts` | 获取当前应用版本 |
| `Toonflow-web/src/components/setting/components/about.vue` | 关于页展示版本并触发检查更新 |

参考项目关键事实：

```txt
1. 启动后立即 checkVersion。
2. 每 2 分钟检查一次。
3. `needUpdate=true` 后停止轮询。
4. Major/Minor 高于当前版本时 `reinstall=true`。
5. Patch 高于当前版本时 `reinstall=false`。
6. 默认更新源是 Toonflow OSS：`https://toonflow.oss-cn-beijing.aliyuncs.com/update.json`。
7. 源码没有看到版本检查失败时的完整 UI 提示。
```

## 3. 用户操作

```txt
入口：
  主界面版本检查入口，或设置 about 页面。

按钮/操作：
  1. 点击检查版本。
  2. 有更新提示时点击打开设置。
  3. 关闭更新提示。

弹窗/表单：
  更新提示通知或小弹窗。

成功反馈：
  显示当前已是最新，或提示发现新版本。

失败反馈：
  网络失败、更新源未配置、版本信息格式错误都要提示。
```

## 4. 要做什么功能

### 1. 手动检查版本

怎么做：
- 输入：用户点击检查版本。
- 输出：检查结果。
- 写什么数据：写前端状态 `checkingUpdate`、`needUpdate`。
- 状态怎么变：idle -> checking -> success/failed。
- 异常怎么处理：失败显示 msg，不影响主流程。
- 限制：开发期默认不自动发外网请求。

### 2. 更新源读取

怎么做：
- 输入：设置中的 update source/url。
- 输出：更新信息 JSON。
- 写什么数据：不写业务数据。
- 状态怎么变：无。
- 异常怎么处理：源未配置或格式错误返回失败。
- 限制：不能使用 Toonflow 默认 OSS 地址。

### 3. 版本号比对

怎么做：
- 输入：当前版本、远端版本。
- 输出：是否有更新、是否需要重装。
- 写什么数据：不写。
- 状态怎么变：更新 `needUpdate`。
- 异常怎么处理：版本号格式不合法时返回失败。
- 限制：按语义版本 Major/Minor/Patch 比较，不用字符串直接比较。

### 4. 更新提示

怎么做：
- 输入：检查结果。
- 输出：提示用户。
- 写什么数据：写 `needUpdate`。
- 状态怎么变：有更新时显示红点。
- 异常怎么处理：提示显示失败不影响检查结果。
- 限制：更新提示里的打开设置要切到 `about`。

### 5. 自动检查控制

怎么做：
- 输入：设置里的自动检查开关。
- 输出：是否启动定时检查。
- 写什么数据：读写设置。
- 状态怎么变：开启则定时检查，发现更新后停止。
- 异常怎么处理：检查失败只提示或记录，不循环弹窗。
- 限制：默认关闭自动检查；如果开启，轮询间隔不能低于参考项目 2 分钟。

## 5. 数据和状态

```txt
字段：
  appVersion.current
  updateInfo.latestVersion
  updateInfo.reinstall
  updateInfo.downloadUrl
  settingStore.needUpdate
  checkingUpdate
  autoCheckUpdate
  updateSource
  updateUrl

接口/能力：
  app.getVersion
  settings.about.checkUpdate
  settings.openDialog
  settings.setActiveMenu("about")

数据读写：
  读当前版本。
  读更新源配置。
  写前端 needUpdate。
  不写业务数据库。

任务状态：
  无。

轮询/Socket：
  自动检查开启时使用定时器；默认关闭。

模型调用：
  无。

删除影响：
  无。
```

## 6. VT Studio 怎么落

```txt
能力名：
  versionCheck

调用链：
  renderer layout/about -> window.vtStudio.app.checkUpdate -> main/ipc -> main/services/update

需要新增：
  当前版本读取
  检查更新 service
  版本号比较
  更新提示状态

需要修改：
  设置红点和 about 菜单红点联动
```

## 7. 偏差

```txt
和 Toonflow 不同的地方：
  1. 不使用 Toonflow OSS 更新源。
  2. 开发期默认不自动联网检查。
  3. 检查失败必须有 UI 提示。

原因：
  旧品牌源必须清理；桌面开发期不应该启动就访问外网。

是否写入 04：
  是，属于版本检查策略偏差。
```

## 8. 验收

```txt
1. 手动点击能检查版本。
2. 检查中有 loading 状态。
3. 当前最新时提示已是最新。
4. 有新版本时 needUpdate=true。
5. 设置按钮和 about 菜单红点显示。
6. 点击打开设置能切到 about。
7. 检查失败显示可读提示。
8. 默认不自动访问外网。
9. 不出现 Toonflow update.json。
```

## 9. 用户确认点

| 编号 | 确认点 | 专业建议 |
|---|---|---|
| C-F-001-011-001 | 开发期是否自动检查版本 | 不自动，默认手动 |
| C-F-001-011-002 | 是否保留 Toonflow 更新源 | 不保留 |
| C-F-001-011-003 | 下载更新是否本任务做 | 不做，放关于页/更新任务 |

## 10. 执行后记录

```txt
改了哪些文件：（完成后填写）
验证结果：（完成后填写）
未完成事项：（完成后填写）
最终结论：（完成后填写）
```

## 11. 最后大白话

```txt
我这次准备怎么做：
1. 做一个安全的版本检查入口。
2. 有更新就亮红点并提示。
3. 默认不自动联网，也不使用旧更新源。

我不会做什么：
1. 不做下载和安装。
2. 不保留 Toonflow 更新地址。
3. 不启动就偷偷请求外网。

确认规则：
用户确认后才执行；未确认前只改文档。
如果本任务有需要用户确认点，或执行中新增确认点，必须先停下来问用户；未确认前不能开始或继续实现。
```
