# F-003-002 编辑项目

状态：已完成（由 P3 批次覆盖）  
所属菜单：M-003 项目管理  
对应功能文档：`docs/features/M-003-项目管理.md`  
原则：先确认本文档，再改代码

## 0. 快速理解

```txt
一句话：项目卡片点编辑，回填项目信息，允许修改名称、简介、模型、画质、比例和手册。
为什么现在做：模型或手册可能失效，打开项目失败时也需要通过编辑项目修复。
做完后有什么用：用户能修正项目配置，避免项目因为模型被删或禁用而无法继续。
这一步不碰什么：不删除项目，不移动项目文件，不编辑小说/剧本/资产。
```

## 1. 本次做什么

```txt
目标：
  对齐 Toonflow editProject 流程，实现项目配置编辑和失效配置修复。

只做：
  1. 项目卡片编辑按钮
  2. 回填项目表单
  3. 回显视频模型 mode
  4. 修改项目字段
  5. 保存编辑
  6. 保存后刷新列表和当前项目上下文
  7. 模型/手册失效提示

不做：
  1. 删除项目
  2. 打开项目跳转
  3. 编辑手册文件内容
  4. 批量编辑项目
```

## 2. 参考项目怎么做

| 参考文件 | 关键逻辑 |
|---|---|
| `Toonflow-web/src/views/project/index.vue` | 编辑按钮 stop 事件，打开项目弹窗并传 projectData |
| `Toonflow-web/src/views/project/components/projectDialog.vue` | watch projectData 回填；如果有 videoModel，调用模型详情拉取 mode 列表 |
| `Toonflow-app/src/routes/project/editProject.ts` | 更新项目记录 |
| `Toonflow-app/src/routes/modelSelect/getModelDetail.ts` | 获取模型能力用于回显和校验 |

## 3. 用户操作

```txt
入口：
  项目页 -> 项目卡片 -> 编辑按钮。

按钮/操作：
  1. 点击编辑。
  2. 修改字段。
  3. 重新选择模型或手册。
  4. 点击确认保存。

弹窗/表单：
  项目弹窗编辑模式。

成功反馈：
  编辑成功，关闭弹窗，刷新项目列表。

失败反馈：
  模型失效、手册失效、字段缺失、保存失败。
```

## 4. 要做什么功能

### 1. 打开编辑弹窗

怎么做：
- 输入：项目 id。
- 输出：回填后的项目表单。
- 写什么数据：不写。
- 状态怎么变：dialogShow=true，editProjectData=项目数据。
- 异常怎么处理：项目不存在提示并刷新列表。
- 限制：点击编辑按钮必须阻止卡片打开项目事件。

### 2. 回填字段

怎么做：
- 输入：项目记录。
- 输出：表单字段。
- 写什么数据：不写。
- 状态怎么变：表单进入编辑模式。
- 异常怎么处理：旧字段缺失时显示失效状态。
- 限制：回填必须包含模型、画质、比例、视觉手册、导演手册。

### 3. 回显视频模式

怎么做：
- 输入：videoModel。
- 输出：mode 选项和当前 mode。
- 写什么数据：不写。
- 状态怎么变：视频模式下拉可用。
- 异常怎么处理：模型不存在或供应商禁用时显示失效并要求重新选择。
- 限制：不能因为 mode 拉取失败就静默清空旧值。

### 4. 保存编辑

怎么做：
- 输入：完整表单。
- 输出：更新成功。
- 写什么数据：更新 projects。
- 状态怎么变：保存中 -> 成功后关闭弹窗。
- 异常怎么处理：字段非法、引用失效、数据库失败返回明确 msg。
- 限制：不改变 projectId 和项目业务目录。

### 5. 同步当前项目上下文

怎么做：
- 输入：保存后的项目。
- 输出：当前项目 store 更新。
- 写什么数据：本地当前项目上下文。
- 状态怎么变：如果编辑的是当前打开项目，同步新配置。
- 异常怎么处理：同步失败提示刷新项目。
- 限制：未打开项目则只刷新列表。

## 5. 数据和状态

```txt
字段：
  projects.* 同 F-003-001

接口/能力：
  project.update
  project.get
  modelSelect.detail
  projectManual.listVisual
  projectManual.listDirector

数据读写：
  读项目
  更新项目
  读模型和手册状态

任务状态：
  不写 tasks 表

轮询/Socket：
  无

模型调用：
  不调用模型，只查模型配置

删除影响：
  无
```

## 6. VT Studio 怎么落

```txt
能力名：
  project.update

调用链：
  renderer 项目页 -> window.vtStudio.project.update -> main/ipc -> main/services/project

需要新增：
  项目弹窗编辑模式
  project update IPC/service

需要修改：
  当前项目 store 支持配置同步
```

## 7. 偏差

```txt
和 Toonflow 不同的地方：
  1. 模型/手册失效必须显示，不静默空白。
  2. 不允许编辑 projectId 和目录。

原因：
  防止项目上下文和本地文件目录错位。

是否写入 04：
  不需要。
```

## 8. 验收

```txt
1. 编辑按钮不会触发打开项目。
2. 编辑弹窗能完整回填项目字段。
3. 视频模式能按视频模型能力回显。
4. 模型失效显示明确提示。
5. 手册失效显示明确提示。
6. 保存后刷新列表。
7. 编辑当前项目后同步当前项目上下文。
8. 不修改项目目录。
9. 页面不直接访问 SQLite。
```

## 9. 用户确认点

| 编号 | 确认点 | 专业建议 |
|---|---|---|
| C-F-003-002-001 | 是否允许编辑项目来源类型 | 建议允许但要提示可能影响入口跳转 |
| C-F-003-002-002 | 是否允许修改项目目录 | 建议不允许，本功能不做迁移 |
| C-F-003-002-003 | 当前项目编辑后是否同步上下文 | 建议必须同步 |

## 10. 执行后记录

```txt
改了哪些文件：（完成后填写）
验证结果：（完成后填写）
未完成事项：（完成后填写）
最终结论：（完成后填写）
```

## 11. 最后大白话

```txt
我这次准备怎么做：
1. 项目卡片加编辑入口。
2. 打开后回填所有项目配置。
3. 用户改完保存。
4. 如果模型或手册坏了，页面直接提示并让用户重新选。

我不会做什么：
1. 不删项目。
2. 不移动项目目录。
3. 不改业务内容。

确认规则：
用户确认后才执行；未确认前只改文档。
```
