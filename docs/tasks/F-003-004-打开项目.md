# F-003-004 打开项目

状态：已完成（由 P3 批次覆盖）  
所属菜单：M-003 项目管理  
对应功能文档：`docs/features/M-003-项目管理.md`  
原则：先确认本文档，再改代码

## 0. 快速理解

```txt
一句话：点击项目卡片前先校验项目和模型可用，成功后写当前项目上下文并跳到小说或剧本页面。
为什么现在做：后续所有项目内页面都依赖 currentProject，不能点击就盲跳。
做完后有什么用：项目打开前能发现模型失效、项目不存在等问题，并引导用户修复。
这一步不碰什么：不编辑项目、不加载小说正文、不生成内容。
```

## 1. 本次做什么

```txt
目标：
  对齐 Toonflow 打开项目逻辑，实现项目校验、上下文写入和路由跳转。

只做：
  1. 点击项目卡片
  2. 查找项目
  3. 校验图片模型
  4. 校验视频模型
  5. 写当前项目上下文
  6. 按 sourceType 跳转
  7. 校验失败打开编辑项目弹窗

不做：
  1. 项目编辑保存
  2. 原文列表加载
  3. 剧本列表加载
  4. Agent 初始化
```

## 2. 参考项目怎么做

| 参考文件 | 关键逻辑 |
|---|---|
| `Toonflow-web/src/views/project/index.vue` | 点击项目卡片，按 id 找项目；校验模型；写 project store；按 projectType 跳转 |
| `Toonflow-app/src/routes/modelSelect/getModelDetail.ts` | 校验模型详情是否可用 |
| `Toonflow-app/src/routes/general/getSingleProject.ts` | 其他模块读取当前项目详情 |
| `Toonflow-web/src/stores/project.ts` | 保存当前项目上下文 |

参考项目关键事实：

```txt
1. 找不到项目提示项目不存在。
2. imageModel 或 videoModel 为空时提示模型供应商不可用并打开编辑弹窗。
3. 模型详情校验失败时也打开编辑弹窗。
4. projectType=novel 跳 /novel。
5. projectType=script 跳 /script。
```

## 3. 用户操作

```txt
入口：
  项目页 -> 点击项目卡片。

按钮/操作：
  点击卡片打开项目。

弹窗/表单：
  校验失败时打开项目编辑弹窗。

成功反馈：
  进入项目内小说页或剧本页。

失败反馈：
  项目不存在、图片模型不可用、视频模型不可用、来源类型不支持。
```

## 4. 要做什么功能

### 1. 查找项目

怎么做：
- 输入：项目 id。
- 输出：项目记录。
- 写什么数据：不写。
- 状态怎么变：打开中。
- 异常怎么处理：找不到提示项目不存在并刷新列表。
- 限制：不能只信任前端卡片缓存，main/service 要能二次确认。

### 2. 校验图片模型

怎么做：
- 输入：项目 imageModel/vendorId。
- 输出：可用/不可用。
- 写什么数据：不写。
- 状态怎么变：校验通过继续。
- 异常怎么处理：供应商禁用、模型删除、类型不是 image 时失败并打开编辑弹窗。
- 限制：不调用模型，只查配置。

### 3. 校验视频模型

怎么做：
- 输入：项目 videoModel/vendorId。
- 输出：可用/不可用。
- 写什么数据：不写。
- 状态怎么变：校验通过继续。
- 异常怎么处理：供应商禁用、模型删除、类型不是 video 时失败并打开编辑弹窗。
- 限制：同时校验项目保存的 mode 是否仍在模型能力内。

### 4. 写当前项目上下文

怎么做：
- 输入：项目记录。
- 输出：currentProject。
- 写什么数据：renderer 本地状态和必要上下文。
- 状态怎么变：未打开项目 -> 当前项目。
- 异常怎么处理：写入失败提示刷新。
- 限制：只保存必要字段，不保存整堆业务数据。

### 5. 按来源类型跳转

怎么做：
- 输入：sourceType。
- 输出：目标路由。
- 写什么数据：不写。
- 状态怎么变：路由进入项目内页面。
- 异常怎么处理：未知 sourceType 提示项目类型不支持。
- 限制：`novel -> /novel`，`script -> /script`。

## 5. 数据和状态

```txt
字段：
  projects.id
  projects.source_type
  projects.image_model
  projects.video_model
  projects.video_mode
  currentProject

接口/能力：
  project.open
  project.get
  modelSelect.detail

数据读写：
  读项目
  读模型配置
  写当前项目上下文

任务状态：
  不写 tasks 表

轮询/Socket：
  无

模型调用：
  不调用模型

删除影响：
  无
```

## 6. VT Studio 怎么落

```txt
能力名：
  project.open

调用链：
  renderer 项目页 -> window.vtStudio.project.open -> main/ipc -> main/services/project -> renderer store/router

需要新增：
  project open service
  当前项目 store

需要修改：
  项目卡片点击逻辑
  模型失效时打开编辑弹窗
```

## 7. 偏差

```txt
和 Toonflow 不同的地方：
  1. service 二次校验项目和模型，不只依赖前端列表。
  2. 校验 video mode 是否仍有效。

原因：
  防止打开项目后在项目内页面才报错。

是否写入 04：
  不需要。
```

## 8. 验收

```txt
1. 点击项目卡片能打开项目。
2. 项目不存在时提示并刷新。
3. 图片模型失效时提示并打开编辑弹窗。
4. 视频模型失效时提示并打开编辑弹窗。
5. 视频 mode 失效时提示并打开编辑弹窗。
6. 校验成功后写 currentProject。
7. novel 项目跳小说页。
8. script 项目跳剧本页。
9. 页面不直接访问 SQLite。
```

## 9. 用户确认点

| 编号 | 确认点 | 专业建议 |
|---|---|---|
| C-F-003-004-001 | 打开项目是否必须校验模型 | 建议必须 |
| C-F-003-004-002 | 模型失效是否自动打开编辑弹窗 | 建议打开，用户能马上修 |
| C-F-003-004-003 | 是否支持除 novel/script 外来源类型 | 建议第一版不支持 |

## 10. 执行后记录

```txt
改了哪些文件：（完成后填写）
验证结果：（完成后填写）
未完成事项：（完成后填写）
最终结论：（完成后填写）
```

## 11. 最后大白话

```txt
我这次准备怎么做：
1. 点项目卡片先检查项目还在不在。
2. 再检查图片和视频模型还能不能用。
3. 都没问题才写当前项目并跳页面。
4. 有问题就打开编辑项目让用户修。

我不会做什么：
1. 不加载小说内容。
2. 不加载剧本内容。
3. 不自动修项目配置。

确认规则：
用户确认后才执行；未确认前只改文档。
```
