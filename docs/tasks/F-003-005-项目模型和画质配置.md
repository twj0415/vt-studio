# F-003-005 项目模型和画质配置

状态：已完成（由 P3 批次覆盖）  
所属菜单：M-003 项目管理  
对应功能文档：`docs/features/M-003-项目管理.md`  
原则：先确认本文档，再改代码

## 0. 快速理解

```txt
一句话：在项目弹窗里选择图片模型、视频模型、视频模式、图片质量和视频比例。
为什么现在做：这些配置会被资产生成、分镜、视频生产反复使用，必须在项目层固定。
做完后有什么用：每个项目都有自己的生成模型和基础画面规格。
这一步不碰什么：不调用模型生成图片/视频，不管理供应商 API Key。
```

## 1. 本次做什么

```txt
目标：
  对齐 Toonflow modelSelect 逻辑，把项目模型和画质配置做成稳定可复用能力。

只做：
  1. 图片模型选择
  2. 视频模型选择
  3. 视频模式选择
  4. 图片质量选择
  5. 视频比例选择
  6. 模型能力读取和失效提示

不做：
  1. 模型供应商配置
  2. 模型测试
  3. 生成任务
  4. 提示词映射
```

## 2. 参考项目怎么做

| 参考文件 | 关键逻辑 |
|---|---|
| `Toonflow-web/src/components/modelSelect.vue` | 按 type 过滤供应商模型，选择后返回模型信息 |
| `Toonflow-web/src/views/project/components/projectDialog.vue` | 选择图片/视频模型；视频模型变化后生成 mode 选项 |
| `Toonflow-app/src/routes/modelSelect/getModelList.ts` | 查询 enable=1 的供应商模型 |
| `Toonflow-app/src/routes/modelSelect/getModelDetail.ts` | 查询模型详情 |
| `Toonflow-app/src/routes/project/getModelDetails.ts` | 项目内查询模型细节 |

参考项目关键事实：

```txt
1. 图片模型只显示 enable=1 供应商里的 type=image。
2. 视频模型只显示 enable=1 供应商里的 type=video。
3. 视频模式来自模型的 mode 字段。
4. 图片质量是 1K/2K/4K。
5. 视频比例是 16:9/9:16。
6. mode 可能是字符串，也可能是数组型多参考模式，源码会转 JSON 字符串保存。
```

## 3. 用户操作

```txt
入口：
  新建/编辑项目弹窗。

按钮/操作：
  1. 打开图片模型选择器。
  2. 选择图片模型。
  3. 选择图片质量。
  4. 打开视频模型选择器。
  5. 选择视频模型。
  6. 选择视频模式。
  7. 选择视频比例。

弹窗/表单：
  模型选择器。

成功反馈：
  选择后回填模型名称和模式选项。

失败反馈：
  没有可用模型、供应商禁用、模型被删除、mode 不支持。
```

## 4. 要做什么功能

### 1. 图片模型选择

怎么做：
- 输入：模型类型 image。
- 输出：启用供应商下的 image 模型列表。
- 写什么数据：确认项目表单时写项目配置。
- 状态怎么变：选中后表单 imageModel 更新。
- 异常怎么处理：无可用模型时提示先去设置模型供应商。
- 限制：不能显示禁用供应商模型。

### 2. 图片质量选择

怎么做：
- 输入：1K/2K/4K。
- 输出：imageQuality。
- 写什么数据：确认项目表单时写项目配置。
- 状态怎么变：表单更新。
- 异常怎么处理：非法值不能提交。
- 限制：图片质量不等同于视频分辨率。

### 3. 视频模型选择

怎么做：
- 输入：模型类型 video。
- 输出：启用供应商下的 video 模型列表。
- 写什么数据：确认项目表单时写项目配置。
- 状态怎么变：选中后清空旧 mode 并加载新 mode 选项。
- 异常怎么处理：无可用模型时提示先配置供应商。
- 限制：不能显示 image/text/tts 模型。

### 4. 视频模式选择

怎么做：
- 输入：选中视频模型的 mode 能力。
- 输出：mode 选项。
- 写什么数据：确认项目表单时写项目配置。
- 状态怎么变：mode 下拉更新。
- 异常怎么处理：mode 为空或不支持时不能提交。
- 限制：数组型多参考模式要序列化为稳定结构，不乱存页面展示文案。

### 5. 视频比例选择

怎么做：
- 输入：16:9 或 9:16。
- 输出：videoRatio。
- 写什么数据：确认项目表单时写项目配置。
- 状态怎么变：表单更新。
- 异常怎么处理：非法值不能提交。
- 限制：比例影响后续分镜/视频，不直接代表图片模型尺寸。

### 6. 模型失效提示

怎么做：
- 输入：项目已保存模型 + 当前供应商模型列表。
- 输出：有效/失效状态。
- 写什么数据：不写。
- 状态怎么变：表单显示红色失效提示。
- 异常怎么处理：无。
- 限制：不能静默替换为其他模型。

## 5. 数据和状态

```txt
字段：
  projects.image_model
  projects.image_quality
  projects.video_model
  projects.video_mode
  projects.video_ratio
  vendor_models.type
  vendor_models.mode
  vendor_models.duration_resolution_map

接口/能力：
  modelSelect.list
  modelSelect.detail
  project.validateModelConfig

数据读写：
  读供应商模型配置
  项目提交时写 projects

任务状态：
  不写 tasks 表

轮询/Socket：
  无

模型调用：
  不调用模型

删除影响：
  供应商/模型删除会导致项目模型配置失效
```

## 6. VT Studio 怎么落

```txt
能力名：
  modelSelect

调用链：
  renderer 项目弹窗 -> window.vtStudio.modelSelect -> main/ipc -> model/vendor service

需要新增：
  通用模型选择组件
  modelSelect IPC/service

需要修改：
  项目新建/编辑弹窗接入模型选择
```

## 7. 偏差

```txt
和 Toonflow 不同的地方：
  1. mode 保存为稳定结构，不保存展示文案。
  2. 失效模型必须提示。

原因：
  后续生产链路要可靠读取模型能力。

是否写入 04：
  不需要。
```

## 8. 验收

```txt
1. 图片模型只显示 image 模型。
2. 视频模型只显示 video 模型。
3. 禁用供应商模型不出现在可选列表。
4. 选择视频模型后 mode 选项更新。
5. 图片质量只有 1K/2K/4K。
6. 视频比例只有 16:9/9:16。
7. 模型失效时有提示。
8. 页面不直接读取供应商配置表。
```

## 9. 用户确认点

| 编号 | 确认点 | 专业建议 |
|---|---|---|
| C-F-003-005-001 | mode 是否保存结构化值 | 建议保存结构化值 |
| C-F-003-005-002 | 图片质量是否保持 1K/2K/4K | 建议对齐参考项目 |
| C-F-003-005-003 | 视频比例是否先只支持 16:9/9:16 | 建议先对齐参考项目 |

## 10. 执行后记录

```txt
改了哪些文件：（完成后填写）
验证结果：（完成后填写）
未完成事项：（完成后填写）
最终结论：（完成后填写）
```

## 11. 最后大白话

```txt
我这次准备怎么做：
1. 项目弹窗里做图片模型、视频模型选择。
2. 视频模型选完后自动显示它支持的模式。
3. 画质和比例只用固定选项。
4. 模型失效就明确提示。

我不会做什么：
1. 不配置供应商。
2. 不测试模型。
3. 不生成图片视频。

确认规则：
用户确认后才执行；未确认前只改文档。
```
