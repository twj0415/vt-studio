# F-004-002 原文列表、搜索、分页

状态：已完成（由 P5 批次覆盖）  
所属菜单：M-004 小说/原文  
对应功能文档：`docs/features/M-004-小说原文.md`  
原则：先确认本文档，再改代码

## 0. 快速理解

```txt
一句话：做原文章节列表，支持按章节名搜索、分页、查看正文和事件摘要状态。
为什么现在做：导入后必须能管理和检查章节，Agent 后续也要读取这些数据。
做完后有什么用：用户能看到每章正文、事件摘要、生成中和失败原因。
这一步不碰什么：不做全文搜索，不做事件重新生成，不做剧本生成。
```

## 1. 本次做什么

```txt
目标：
  对齐 Toonflow `/novel/getNovel` 的章节列表能力。

只做：
  1. 当前项目章节列表
  2. 按章节名搜索
  3. 分页
  4. 正文预览和查看详情
  5. 事件摘要展示
  6. 事件生成中和失败原因展示

不做：
  1. 正文全文搜索
  2. 卷名搜索
  3. 事件内容搜索
  4. 事件库管理
```

## 2. 参考项目怎么做

| 参考文件 | 关键逻辑 |
|---|---|
| `Toonflow-web/src/views/novel/index.vue` | 表格展示章节、正文预览、事件摘要、生成状态和操作 |
| `Toonflow-app/src/routes/novel/getNovel.ts` | 按 projectId 查询章节，支持 chapter 搜索和分页 |
| `Toonflow-app/src/routes/novel/getNovelIndex.ts` | 查询章节索引信息 |

参考项目关键事实：

```txt
1. 列表只查当前项目。
2. 搜索只确认按 chapter 字段搜索。
3. 正文超过 80 字显示查看详情。
4. eventState=0 时事件列显示 loading。
5. eventState=-1 且 event 为空时显示生成失败，可查看 errorReason。
```

## 3. 用户操作

```txt
入口：
  项目内顶部菜单 -> 原文。

按钮/操作：
  1. 输入章节名关键词。
  2. 点击搜索。
  3. 翻页。
  4. 查看正文详情。
  5. 查看事件摘要或失败原因。

弹窗/表单：
  正文/事件详情预览弹窗。

成功反馈：
  表格展示章节。

失败反馈：
  查询失败提示；无数据显示空状态。
```

## 4. 要做什么功能

### 1. 查询当前项目章节

怎么做：
- 输入：projectId、page、pageSize、chapterKeyword。
- 输出：章节列表和 total。
- 写什么数据：不写。
- 状态怎么变：loading=true -> false。
- 异常怎么处理：查询失败显示错误状态。
- 限制：没有当前项目不能查询。

### 2. 按章节名搜索

怎么做：
- 输入：章节名关键词。
- 输出：匹配章节列表。
- 写什么数据：不写。
- 状态怎么变：搜索后 page=1。
- 异常怎么处理：空关键词返回全部章节。
- 限制：先只按章节名搜索，对齐参考项目。

### 3. 展示正文和事件摘要

怎么做：
- 输入：chapterData、event、eventState、errorReason。
- 输出：表格预览和详情弹窗。
- 写什么数据：不写。
- 状态怎么变：无。
- 异常怎么处理：正文或事件为空时显示空文案。
- 限制：长文本不撑破表格。

### 4. 展示事件状态

怎么做：
- 输入：eventState。
- 输出：未生成/生成中/成功/失败显示。
- 写什么数据：不写。
- 状态怎么变：无。
- 异常怎么处理：未知状态显示未知并记录日志。
- 限制：内部状态不使用中文枚举。

## 5. 数据和状态

```txt
字段：
  source_chapters.id
  source_chapters.chapter_index
  source_chapters.volume_name
  source_chapters.chapter_title
  source_chapters.content
  source_chapters.event_status
  source_chapters.event_summary
  source_chapters.event_error

接口/能力：
  source.list

数据读写：
  读章节表

任务状态：
  不写 tasks

轮询/Socket：
  本任务不轮询，轮询见 F-004-006

模型调用：
  无

删除影响：
  无
```

## 6. VT Studio 怎么落

```txt
能力名：
  source.list

调用链：
  renderer 原文列表 -> window.vtStudio.source.list -> main/services/source

需要新增：
  原文列表表格
  搜索框
  分页器
  文本详情弹窗

需要修改：
  无
```

## 7. 偏差

```txt
和 Toonflow 不同的地方：
  内部事件状态用英文枚举，显示层映射中文。

原因：
  和任务状态、生成状态保持统一。

是否写入 04：
  如果 04 已记录状态枚举统一，不重复。
```

## 8. 验收

```txt
1. 只显示当前项目章节。
2. 搜索按章节名生效。
3. 分页生效。
4. 正文长内容可查看详情。
5. 事件摘要长内容可查看详情。
6. 生成中显示 loading。
7. 失败显示失败原因。
8. 页面不直接访问 SQLite。
```

## 9. 用户确认点

| 编号 | 确认点 | 专业建议 |
|---|---|---|
| C-F-004-002-001 | 是否第一版支持正文全文搜索 | 建议先不做，对齐参考项目只搜章节名 |

## 10. 执行后记录

```txt
改了哪些文件：由 P5-原文输入批统一完成，详见 docs/tasks/P5-原文输入批.md
验证结果：verify-p5-source、typecheck、build 通过
未完成事项：正文全文搜索、卷名搜索、事件内容搜索不在本批次
最终结论：已完成当前项目原文章节列表、章节名搜索、分页、正文预览、事件状态和失败原因展示
```

## 11. 最后大白话

```txt
我这次准备怎么做：
1. 做原文章节表格。
2. 支持按章节名搜和分页。
3. 把正文、事件摘要、失败原因显示清楚。

我不会做什么：
1. 不做全文搜索。
2. 不做事件重新生成。

确认规则：
用户确认后才执行；未确认前只改文档。
```
