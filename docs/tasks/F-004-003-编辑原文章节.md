# F-004-003 编辑原文章节

状态：已完成（由 P5 批次覆盖）  
所属菜单：M-004 小说/原文  
对应功能文档：`docs/features/M-004-小说原文.md`  
原则：先确认本文档，再改代码

## 0. 快速理解

```txt
一句话：用户可以编辑某一章的章节名、正文和事件摘要。
为什么现在做：导入解析可能不准，原文错误会影响事件分析和剧本 Agent。
做完后有什么用：用户能修正章节内容，并让后续生成基于正确文本。
这一步不碰什么：不做批量编辑，不做自动重写剧本，不做旧 editNodel 命名。
```

## 1. 本次做什么

```txt
目标：
  对齐 Toonflow `editNodel.vue` 和 `/novel/updateNovel`，同时修正旧拼写。

只做：
  1. 编辑章节按钮
  2. 编辑弹窗
  3. 回填章节名、正文、事件摘要
  4. 保存章节
  5. 正文变更后的事件状态处理规则

不做：
  1. 批量编辑
  2. 自动重新生成事件
  3. 自动更新已生成剧本
```

## 2. 参考项目怎么做

| 参考文件 | 关键逻辑 |
|---|---|
| `Toonflow-web/src/views/novel/components/editNodel.vue` | 打开后回填 chapter、event、chapterData，保存后 emit select 刷新 |
| `Toonflow-app/src/routes/novel/updateNovel.ts` | 更新章节数据 |

参考项目关键事实：

```txt
1. 组件名 editNodel 是旧拼写，VT Studio 必须改成 editNovel 或 sourceChapterEditor。
2. 可编辑 chapter、event、chapterData。
3. 弹窗不显示 reel 和 index，但保存会带原值。
4. 参考项目没有明确正文变更后自动重置事件摘要。
```

## 3. 用户操作

```txt
入口：
  原文列表 -> 某行编辑按钮。

按钮/操作：
  1. 点击编辑。
  2. 修改章节名、正文、事件摘要。
  3. 点击保存。

弹窗/表单：
  编辑章节弹窗。

成功反馈：
  保存成功，关闭弹窗，刷新列表。

失败反馈：
  必填缺失、保存失败、章节正在分析不可编辑。
```

## 4. 要做什么功能

### 1. 打开编辑弹窗

怎么做：
- 输入：章节 id。
- 输出：章节表单。
- 写什么数据：不写。
- 状态怎么变：dialogOpen=true，form 回填。
- 异常怎么处理：章节不存在时提示并刷新列表。
- 限制：事件分析中的章节不能编辑，或必须先取消/等待完成。

### 2. 保存章节修改

怎么做：
- 输入：章节名、正文、事件摘要。
- 输出：更新后的章节。
- 写什么数据：更新章节表。
- 状态怎么变：保存中 -> 成功关闭 -> 刷新列表。
- 异常怎么处理：正文为空不能保存；数据库失败回滚。
- 限制：只允许更新当前项目下章节。

### 3. 正文变更后的事件状态处理

怎么做：
- 输入：原正文、新正文。
- 输出：事件状态。
- 写什么数据：如果正文变更，标记事件摘要需要重新分析。
- 状态怎么变：event_status 变为 stale 或清空事件状态，按最终枚举定。
- 异常怎么处理：状态更新失败时整体保存失败。
- 限制：不能继续把旧 event 当作新正文的有效摘要。

## 5. 数据和状态

```txt
字段：
  source_chapters.chapter_title
  source_chapters.content
  source_chapters.event_summary
  source_chapters.event_status
  source_chapters.event_error
  source_chapters.updated_at

接口/能力：
  source.updateChapter

数据读写：
  更新章节表

任务状态：
  不写 tasks

轮询/Socket：
  无

模型调用：
  无

删除影响：
  无
```

## 6. VT Studio 怎么落

```txt
能力名：
  source.updateChapter

调用链：
  renderer 编辑弹窗 -> window.vtStudio.source.updateChapter -> main/services/source

需要新增：
  编辑章节弹窗

需要修改：
  原文列表操作列
```

## 7. 偏差

```txt
和 Toonflow 不同的地方：
  1. 不保留 editNodel 拼写。
  2. 正文变更后标记事件摘要需要重新分析。

原因：
  旧摘要基于旧正文，继续使用会误导剧本 Agent。

是否写入 04：
  正文变更导致事件摘要失效是合理修正，可写入偏差记录。
```

## 8. 验收

```txt
1. 点击编辑能回填章节数据。
2. 能修改章节名。
3. 能修改正文。
4. 能修改事件摘要。
5. 正文为空不能保存。
6. 正在事件分析的章节不能静默编辑。
7. 正文变更后旧事件摘要不会被当作有效摘要。
8. 页面不直接访问 SQLite。
```

## 9. 用户确认点

| 编号 | 确认点 | 专业建议 |
|---|---|---|
| C-F-004-003-001 | 正文修改后是否自动清空旧事件摘要 | 建议标记为需重新分析，并提示用户重新生成 |

## 10. 执行后记录

```txt
改了哪些文件：由 P5-原文输入批统一完成，详见 docs/tasks/P5-原文输入批.md
验证结果：verify-p5-source、typecheck、build 通过
未完成事项：批量编辑不在本批次
最终结论：已完成章节编辑；正文变更后旧事件摘要会失效并提示重新分析
```

## 11. 最后大白话

```txt
我这次准备怎么做：
1. 做章节编辑弹窗。
2. 用户能改章节名、正文、事件摘要。
3. 正文变了以后，不再默认信任旧事件摘要。

我不会做什么：
1. 不批量编辑。
2. 不自动重写剧本。

确认规则：
用户确认后才执行；未确认前只改文档。
```
