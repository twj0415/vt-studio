# F-004-006 事件状态轮询

状态：已完成（由 P5 批次覆盖）  
所属菜单：M-004 小说/原文  
对应功能文档：`docs/features/M-004-小说原文.md`  
原则：先确认本文档，再改代码

## 0. 快速理解

```txt
一句话：原文列表自动轮询事件分析中的章节，把结果刷新到表格。
为什么现在做：事件分析是异步的，用户需要看到什么时候完成或失败。
做完后有什么用：页面不用手动刷新，也能看到生成中章节的最新状态。
这一步不碰什么：不重新生成事件，不做任务中心自动轮询，不做 Socket。
```

## 1. 本次做什么

```txt
目标：
  对齐 Toonflow 每 3 秒轮询 `/novel/getNovelEventState`。

只做：
  1. 找出事件生成中的章节
  2. 每 3 秒轮询状态
  3. 更新表格行事件摘要、状态、失败原因
  4. 没有生成中章节时停止轮询
  5. 离开页面停止轮询

不做：
  1. Socket 推送
  2. 任务中心自动刷新
  3. 重新生成事件
```

## 2. 参考项目怎么做

| 参考文件 | 关键逻辑 |
|---|---|
| `Toonflow-web/src/views/novel/index.vue` | 过滤 `!item.eventState` 的章节，每 3 秒轮询 |
| `Toonflow-app/src/routes/novel/getNovelEventState.ts` | 接收 ids，只返回 eventState != 0 的章节 |
| `Toonflow-app/src/lib/fixDB.ts` | 启动时把残留 eventState=0 改为失败 |

参考项目关键事实：

```txt
1. 轮询间隔是 3 秒。
2. 后端只返回不再生成中的章节。
3. 前端拿返回结果更新当前行 eventState、event、errorReason。
4. 没有生成中章节时停止轮询。
5. 软件异常退出时，fixDB 把残留生成中改失败。
```

## 3. 用户操作

```txt
入口：
  原文列表页面自动执行。

按钮/操作：
  无。

弹窗/表单：
  无。

成功反馈：
  生成中章节自动变为成功或失败。

失败反馈：
  轮询失败时提示或静默降级，但不能让页面崩溃。
```

## 4. 要做什么功能

### 1. 识别生成中章节

怎么做：
- 输入：当前表格数据。
- 输出：running 状态章节 id 列表。
- 写什么数据：不写。
- 状态怎么变：有 running 才启动轮询。
- 异常怎么处理：无 id 的行忽略。
- 限制：不能用 `!eventState` 这种含糊判断，必须用明确枚举。

### 2. 轮询事件状态

怎么做：
- 输入：running chapterIds。
- 输出：已完成或失败的章节状态。
- 写什么数据：不写，服务只查询。
- 状态怎么变：每 3 秒请求一次。
- 异常怎么处理：请求失败记录错误并保留下次轮询。
- 限制：页面卸载必须清理 timer。

### 3. 更新表格行

怎么做：
- 输入：轮询返回的章节状态。
- 输出：表格局部更新。
- 写什么数据：不写。
- 状态怎么变：running -> succeeded/failed。
- 异常怎么处理：返回不存在 id 时忽略。
- 限制：不整体丢失用户当前分页和选择状态。

### 4. 启动修复残留状态

怎么做：
- 输入：应用启动时残留 running 章节。
- 输出：失败状态和原因。
- 写什么数据：更新 event_status=failed、event_error=软件退出导致失败。
- 状态怎么变：running -> failed。
- 异常怎么处理：修复失败写日志。
- 限制：该能力属于启动修复服务，不写在页面里。

## 5. 数据和状态

```txt
字段：
  source_chapters.event_status
  source_chapters.event_summary
  source_chapters.event_error

接口/能力：
  source.pollEventStatus
  source.recoverRunningEvents

数据读写：
  轮询读章节状态
  启动修复写失败状态

任务状态：
  如果 F-004-005 写任务中心，这里只同步章节状态，不直接改任务状态

轮询/Socket：
  3 秒轮询，无 Socket

模型调用：
  无

删除影响：
  无
```

## 6. VT Studio 怎么落

```txt
能力名：
  source.pollEventStatus

调用链：
  renderer 原文列表轮询 -> window.vtStudio.source.pollEventStatus -> main/services/sourceEvent

需要新增：
  页面轮询控制
  pollEventStatus service
  启动残留状态修复

需要修改：
  原文列表行状态更新逻辑
```

## 7. 偏差

```txt
和 Toonflow 不同的地方：
  使用明确 running 枚举，不用 `!eventState` 判断。

原因：
  避免 null、0、undefined 混淆。

是否写入 04：
  不需要，属于状态规范修正。
```

## 8. 验收

```txt
1. 有生成中章节时启动轮询。
2. 没有生成中章节时不轮询。
3. 轮询间隔约 3 秒。
4. 成功后事件摘要显示出来。
5. 失败后失败原因显示出来。
6. 离开页面后停止轮询。
7. 应用启动会修复残留 running。
8. 页面不直接访问 SQLite。
```

## 9. 用户确认点

| 编号 | 确认点 | 专业建议 |
|---|---|---|
| C-F-004-006-001 | 轮询失败是否 toast | 建议不要每次 toast，显示轻量错误并继续轮询 |

## 10. 执行后记录

```txt
改了哪些文件：由 P5-原文输入批统一完成，详见 docs/tasks/P5-原文输入批.md
验证结果：verify-p5-source、typecheck、build 通过
未完成事项：Socket 推送不在本批次
最终结论：已完成 3 秒轮询、离开页面停止轮询和启动残留 running 事件恢复
```

## 11. 最后大白话

```txt
我这次准备怎么做：
1. 页面自动找生成中的章节。
2. 每 3 秒查一次结果。
3. 完成就更新摘要，失败就更新原因。
4. 离开页面停止轮询。

我不会做什么：
1. 不用 Socket。
2. 不重新发起生成。

确认规则：
用户确认后才执行；未确认前只改文档。
```
