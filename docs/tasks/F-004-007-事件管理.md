# F-004-007 事件管理

状态：已完成（由 P5 批次覆盖）  
所属菜单：M-004 小说/原文  
对应功能文档：`docs/features/M-004-小说原文.md`  
原则：先确认本文档，再改代码

## 0. 快速理解

```txt
一句话：先把事件管理边界定清楚：第一版以章节事件摘要为准，不照搬无闭环的旧事件列表。
为什么现在做：参考项目 `event.vue` 存在但没有在主页面挂载，直接做会偏题。
做完后有什么用：后续 Agent 读取上下文不会混用两套事件数据。
这一步不碰什么：不做旧 `eventAnalysis.vue`，不调用不存在的接口。
```

## 1. 本次做什么

```txt
目标：
  整理并落地“章节级事件摘要”的管理能力，明确旧事件库不是第一版正式能力。

只做：
  1. 章节事件摘要查看
  2. 章节事件摘要手动编辑入口复用 F-004-003
  3. 章节事件失败原因查看
  4. 剧本 Agent 读取章节事件的统一来源
  5. 记录旧 event.vue 源码冲突

不做：
  1. 独立事件列表页
  2. 旧 eventAnalysis.vue
  3. `/novel/event/eventAnalysis`
  4. 未闭环的 o_event/o_eventChapter 自动同步
```

## 2. 参考项目怎么做

| 参考文件 | 关键逻辑 |
|---|---|
| `Toonflow-web/src/views/novel/index.vue` | 主页面展示章节级 `o_novel.event` |
| `Toonflow-web/src/views/novel/components/event.vue` | 有事件列表、删除、批量删除 UI，但主页面未找到挂载 |
| `Toonflow-web/src/views/novel/components/eventAnalysis.vue` | 调用 `/novel/event/eventAnalysis` |
| `Toonflow-app/src/routes/novel/event/getEvent.ts` | 读取 `o_event/o_eventChapter/o_novel` |
| `Toonflow-app/src/routes/novel/event/generateEvents.ts` | 生成章节级 `o_novel.event`，要求 novelIds |

参考项目关键事实：

```txt
1. 主页面用的是章节级 event 字段。
2. event.vue 读取 o_event/o_eventChapter，但没有找到和 o_novel.event 的完整同步闭环。
3. event.vue 调 generateEvents 时没有传 novelIds，而后端要求 novelIds。
4. eventAnalysis.vue 调的后端 route 没找到。
5. 剧本 Agent 工具读取的是章节事件，不依赖独立事件库。
```

## 3. 用户操作

```txt
入口：
  原文列表事件列、详情弹窗、编辑章节弹窗。

按钮/操作：
  1. 查看章节事件摘要。
  2. 查看失败原因。
  3. 编辑章节时可修正事件摘要。

弹窗/表单：
  事件详情弹窗；编辑章节弹窗。

成功反馈：
  事件摘要显示清楚。

失败反馈：
  事件生成失败显示 errorReason。
```

## 4. 要做什么功能

### 1. 统一事件来源

怎么做：
- 输入：当前项目章节。
- 输出：章节级事件摘要。
- 写什么数据：不写。
- 状态怎么变：无。
- 异常怎么处理：事件为空时显示未分析或空状态。
- 限制：第一版以 source_chapters.event_summary 为准。

### 2. 事件摘要查看和编辑

怎么做：
- 输入：章节 event_summary。
- 输出：详情查看；编辑时走 F-004-003。
- 写什么数据：编辑时更新章节事件摘要。
- 状态怎么变：编辑保存后刷新列表。
- 异常怎么处理：保存失败提示。
- 限制：不建立第二套事件表作为主来源。

### 3. 旧事件组件处理

怎么做：
- 输入：参考项目 event.vue、eventAnalysis.vue。
- 输出：记录为旧痕迹，不作为第一版实现依据。
- 写什么数据：必要时写 04 偏差。
- 状态怎么变：无。
- 异常怎么处理：无。
- 限制：不能调用不存在的 `/novel/event/eventAnalysis`。

## 5. 数据和状态

```txt
字段：
  source_chapters.event_summary
  source_chapters.event_status
  source_chapters.event_error

接口/能力：
  source.list
  source.updateChapter
  source.generateEvents

数据读写：
  读/写章节事件摘要

任务状态：
  不新增任务状态

轮询/Socket：
  事件状态轮询见 F-004-006

模型调用：
  事件生成见 F-004-005

删除影响：
  删除章节时事件摘要随章节删除
```

## 6. VT Studio 怎么落

```txt
能力名：
  source.eventSummary

调用链：
  renderer 原文页 -> window.vtStudio.source.list/updateChapter/generateEvents -> main/services/source

需要新增：
  事件摘要详情展示
  旧事件库偏差记录

需要修改：
  剧本 Agent 上下文读取规则：读取章节事件摘要
```

## 7. 偏差

```txt
和 Toonflow 不同的地方：
  第一版不做未挂载、未闭环的独立事件库。

原因：
  参考源码没有完整入口和同步逻辑，照搬会产生两套事件数据。

是否写入 04：
  写入，说明 VT Studio 第一版以章节事件摘要为准。
```

## 8. 验收

```txt
1. 原文列表能查看章节事件摘要。
2. 失败原因能查看。
3. 编辑章节能修正事件摘要。
4. 剧本 Agent 读取章节事件摘要作为上下文。
5. 不调用不存在的 eventAnalysis route。
6. 不生成第二套无同步规则的事件库。
7. 页面不直接访问 SQLite。
```

## 9. 用户确认点

| 编号 | 确认点 | 专业建议 |
|---|---|---|
| C-F-004-007-001 | 第一版是否不做独立事件库 | 建议不做，先保证章节事件摘要闭环 |
| C-F-004-007-002 | 后续是否需要事件库 | 建议等章节摘要和剧本 Agent 稳定后，再设计事件库同步规则 |

## 10. 执行后记录

```txt
改了哪些文件：由 P5-原文输入批统一完成，详见 docs/tasks/P5-原文输入批.md
验证结果：verify-p5-source、typecheck、build 通过
未完成事项：旧 event.vue 独立事件库不在第一版实现
最终结论：已完成章节级事件摘要查看、失败原因查看和编辑入口；第一版以 source_chapters.event_summary 作为统一事件来源
```

## 11. 最后大白话

```txt
我这次准备怎么做：
1. 明确事件先按章节管理。
2. 原文列表里看事件摘要和失败原因。
3. 剧本 Agent 也读这份章节事件。

我不会做什么：
1. 不照搬没挂载的旧 event.vue。
2. 不调用找不到后端的 eventAnalysis。
3. 不做两套事件数据。

确认规则：
用户确认后才执行；未确认前只改文档。
```

## 12. 本轮补充

```txt
参考项目 event.vue 确实有事件列表、删除、批量删除、生成/重新生成按钮。
但 novel/index.vue 未找到挂载入口；主列表使用的是章节级 o_novel.event。
因此第一版不要把旧 event.vue 当成完整事件库。
本任务如果开工，需要先定清：
1. 章节级 event 是否同步成独立事件库。
2. o_event/o_eventChapter 和 o_novel.event 的关系。
3. 剧本 Agent 读取上下文时第一版以章节 event 为准。
```
