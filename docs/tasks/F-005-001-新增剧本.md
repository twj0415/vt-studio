# F-005-001 新增剧本

状态：已完成（由 P7 剧本结果批覆盖）  
所属菜单：M-005 剧本  
对应功能文档：`docs/features/M-005-剧本.md`  
原则：已按 P7 批次集中实现

## 0. 快速理解

```txt
一句话：剧本页新增一集剧本，填写名称和内容，还可以关联已有角色/场景/道具。
为什么现在做：剧本是资产提取、生产工作台、视频生成的基础。
做完后有什么用：用户能手动创建剧本分集，不完全依赖 Agent。
这一步不碰什么：不做批量导入，不提取资产，不生成分镜。
```

## 1. 本次做什么

```txt
目标：
  对齐 Toonflow `addScript.vue` 和 `/script/addScript`。

只做：
  1. 新增剧本按钮
  2. 新增弹窗
  3. 剧本名称和内容
  4. txt/docx 读取到内容
  5. 字数上限校验
  6. 关联已有资产
  7. 保存剧本和资产关系

不做：
  1. 批量新增
  2. 编辑已有剧本
  3. 资产提取
  4. 剧本导出
```

## 2. 参考项目怎么做

| 参考文件 | 关键逻辑 |
|---|---|
| `Toonflow-web/src/views/script/components/addScript.vue` | 新增弹窗，名称、上传/粘贴内容、关联资产，字数限制 |
| `Toonflow-app/src/routes/script/addScript.ts` | 写 `o_script`，如果 assets 非空写 `o_scriptAssets` |
| `Toonflow-web/src/utils/assetsCheck.ts` | 打开资产选择器，类型限制 role/tool/scene |
| `Toonflow-web/src/stores/setting.ts` | `otherSetting.scriptEpisodeLength` 控制单集字数上限 |

参考项目关键事实：

```txt
1. 支持 txt/docx，不支持 doc。
2. 单文件最大 10MB。
3. 上传只读取文本，不保存原始文件。
4. 内容超过 scriptEpisodeLength 时确认按钮禁用。
5. 关联资产类型限制 role/tool/scene。
6. 新增后写剧本和剧本资产关系。
```

## 3. 用户操作

```txt
入口：
  项目内顶部菜单 -> 剧本 -> 新增剧本。

按钮/操作：
  1. 填写剧本名称。
  2. 上传 txt/docx 或粘贴剧本内容。
  3. 选择关联资产。
  4. 点击确认。

弹窗/表单：
  新增剧本弹窗。

成功反馈：
  新增成功，关闭弹窗，刷新剧本卡片列表。

失败反馈：
  名称为空、内容为空、字数超限、文件不支持、保存失败。
```

## 4. 要做什么功能

### 1. 打开新增弹窗

怎么做：
- 输入：点击新增按钮。
- 输出：空表单。
- 写什么数据：不写。
- 状态怎么变：dialogOpen=true。
- 异常怎么处理：无。
- 限制：打开时读取字数上限和资产选择能力。

### 2. 填写或导入剧本内容

怎么做：
- 输入：文本、txt/docx 文件。
- 输出：scriptContent。
- 写什么数据：确认前不写。
- 状态怎么变：字数统计实时更新。
- 异常怎么处理：doc 和未知 MIME 提示不支持。
- 限制：单文件最大 10MB。

### 3. 选择关联资产

怎么做：
- 输入：资产选择器返回 role/scene/tool。
- 输出：selectedAssets。
- 写什么数据：确认前不写。
- 状态怎么变：资产 tag 展示，可移除。
- 异常怎么处理：资产不存在时提示并移除无效项。
- 限制：不能选择 clip/audio。

### 4. 保存剧本

怎么做：
- 输入：name、content、selectedAssets、projectId。
- 输出：新剧本记录。
- 写什么数据：写 scripts；写 script_asset_links。
- 状态怎么变：保存中 -> 成功关闭 -> 刷新列表。
- 异常怎么处理：事务失败回滚。
- 限制：assets 为空也要明确保存为无关联。

## 5. 数据和状态

```txt
字段：
  scripts.id
  scripts.project_id
  scripts.name
  scripts.content
  scripts.extract_status
  scripts.created_at
  script_asset_links.script_id
  script_asset_links.asset_id

接口/能力：
  script.create
  asset.selector

数据读写：
  写 scripts
  写 script_asset_links
  读 settings.script_episode_length

任务状态：
  不写 tasks

轮询/Socket：
  无

模型调用：
  无

删除影响：
  无
```

## 6. VT Studio 怎么落

```txt
能力名：
  script.create

调用链：
  renderer 剧本新增弹窗 -> window.vtStudio.script.create -> main/services/script

需要新增：
  新增剧本弹窗
  script.create service
  资产选择器复用

需要修改：
  剧本列表刷新
```

## 7. 偏差

```txt
和 Toonflow 不同的地方：
  字段命名不用 o_script/o_scriptAssets；状态用英文枚举。

原因：
  遵守 VT Studio 命名规范。

是否写入 04：
  不需要，属于命名规范。
```

## 8. 验收

```txt
1. 点击新增打开空弹窗。
2. 名称和内容为空不能保存。
3. txt/docx 能读取。
4. doc 提示不支持。
5. 字数超过上限不能保存。
6. 能选择角色/场景/道具资产。
7. 保存后写剧本和资产关系。
8. 保存后刷新列表。
9. 页面不直接访问 SQLite 或文件系统。
```

## 9. 用户确认点

| 编号 | 确认点 | 专业建议 |
|---|---|---|
| C-F-005-001-001 | 新增剧本是否允许无关联资产 | 建议允许，后续可通过资产提取补全 |

## 10. 执行后记录

```txt
改了哪些文件：见 docs/tasks/P7-剧本结果批.md
验证结果：verify-p7-scripts、typecheck、build 通过
未完成事项：完整资产中心、角景音频、生产工作台和导出继续归 P8/P9/P10
最终结论：已完成；新增剧本能力由 P7 剧本结果批覆盖
```

## 11. 最后大白话

```txt
我这次准备怎么做：
1. 做新增剧本弹窗。
2. 用户填名称和内容，也能从 txt/docx 读入。
3. 可选关联资产，保存后写入剧本列表。

我不会做什么：
1. 不批量导入。
2. 不提取资产。
3. 不生成分镜。

确认规则：
用户确认后才执行；未确认前只改文档。
```
