# F-005-002 批量新增剧本

状态：已完成（由 P7 剧本结果批覆盖）  
所属菜单：M-005 剧本  
对应功能文档：`docs/features/M-005-剧本.md`  
原则：已按 P7 批次集中实现

## 0. 快速理解

```txt
一句话：把一大段多集剧本按正则拆成多集，预览勾选后批量保存。
为什么现在做：很多剧本是一次性导入多集，手动一集一集新增效率太低。
做完后有什么用：用户能用默认正则、自定义正则或 AI 正则快速拆集入库。
这一步不碰什么：不提取资产，不生成分镜，不自动创建项目。
```

## 1. 本次做什么

```txt
目标：
  对齐 Toonflow `batchAddScript.vue`、`parseScript.ts` 和 `/script/batchAddScript`。

只做：
  1. 批量新增弹窗
  2. txt/docx 或粘贴文本
  3. 默认集正则解析
  4. 自定义正则解析
  5. AI 生成正则
  6. 解析结果预览
  7. 勾选集数保存
  8. 字数上限校验

不做：
  1. 资产提取
  2. 剧本编辑
  3. 关联资产手动选择
```

## 2. 参考项目怎么做

| 参考文件 | 关键逻辑 |
|---|---|
| `Toonflow-web/src/views/script/components/batchAddScript.vue` | 两步 tabs：输入/解析 -> 勾选/保存 |
| `Toonflow-web/src/utils/parseScript.ts` | 默认按“第 N 集”拆分，支持自定义正则 |
| `Toonflow-app/src/routes/script/batchAddScript.ts` | 批量写 `o_script` |
| `Toonflow-app/src/routes/script/getAiRegex.ts` | 用 `universalAi` 根据文本前 2000 字生成正则 |

参考项目关键事实：

```txt
1. 默认正则是 `/第\s*([0-9０-９零一二三四五六七八九十百千万]+)\s*集\s*([^\n\r]*)/g`。
2. 自定义正则支持 /表达式/flags，没有 g 会自动补 g。
3. 没匹配但文本非空时，全文作为第 1 集。
4. AI 正则要求返回 JavaScript 正则字符串，必须有两个捕获组。
5. 保存前必须勾选要保存的剧本。
6. Toonflow row-key 用 index，有重复风险，VT Studio 必须用稳定临时 id。
```

## 3. 用户操作

```txt
入口：
  剧本页 -> 批量新增剧本。

按钮/操作：
  1. 上传 txt/docx 或粘贴剧本。
  2. 可填写自定义正则。
  3. 可点击 AI 获取正则。
  4. 点击下一步。
  5. 勾选要保存的集数。
  6. 点击保存。

弹窗/表单：
  两步批量导入弹窗。

成功反馈：
  保存成功，关闭弹窗，刷新剧本列表。

失败反馈：
  正则非法、没有解析结果、未勾选、字数超限、保存失败。
```

## 4. 要做什么功能

### 1. 输入和解析剧本文本

怎么做：
- 输入：txt/docx 文件或粘贴文本。
- 输出：原始 content 和解析结果。
- 写什么数据：确认保存前不写。
- 状态怎么变：content 更新后实时解析。
- 异常怎么处理：文件不支持或过大提示。
- 限制：单文件最大 10MB。

### 2. 自定义正则

怎么做：
- 输入：customRegStr。
- 输出：解析结果。
- 写什么数据：不写。
- 状态怎么变：正则非法时输入框 error。
- 异常怎么处理：非法正则不能进入下一步。
- 限制：必须至少能捕获集数和标题。

### 3. AI 生成正则

怎么做：
- 输入：content 前 2000 字。
- 输出：正则字符串。
- 写什么数据：不写。
- 状态怎么变：AI 按钮 loading -> 回填 customRegStr。
- 异常怎么处理：模型失败或返回空字符串时提示。
- 限制：只调用文本模型，不写任务中心。

### 4. 预览勾选保存

怎么做：
- 输入：解析结果 selectedRowKeys。
- 输出：批量剧本记录。
- 写什么数据：写 scripts。
- 状态怎么变：保存中 -> 成功关闭 -> 列表刷新。
- 异常怎么处理：任一写入失败回滚。
- 限制：row-key 使用稳定临时 id，不能用 index。

## 5. 数据和状态

```txt
字段：
  scripts.id
  scripts.project_id
  scripts.name
  scripts.content
  scripts.extract_status
  scripts.created_at

接口/能力：
  script.batchCreate
  script.generateParseRegex

数据读写：
  写 scripts
  读 settings.script_episode_length

任务状态：
  AI 正则测试不写 tasks

轮询/Socket：
  无

模型调用：
  AI 正则用文本模型 universalAi 或模型映射

删除影响：
  无
```

## 6. VT Studio 怎么落

```txt
能力名：
  script.batchCreate
  script.generateParseRegex

调用链：
  renderer 批量导入弹窗 -> window.vtStudio.script.batchCreate/generateParseRegex -> main/services/script

需要新增：
  批量导入弹窗
  parseScript 工具
  AI 正则生成服务

需要修改：
  剧本列表刷新
```

## 7. 偏差

```txt
和 Toonflow 不同的地方：
  解析预览 row-key 不用 index，使用稳定临时 id。

原因：
  不同集或重复编号可能导致勾选错乱。

是否写入 04：
  不需要，属于前端稳定性修正。
```

## 8. 验收

```txt
1. txt/docx 能读取。
2. doc 提示不支持。
3. 默认正则能拆“第 N 集”。
4. 自定义正则非法时不能下一步。
5. AI 正则能回填或显示失败。
6. 没解析结果不能下一步。
7. 未勾选不能保存。
8. 保存成功后刷新列表。
9. 页面不直接访问 SQLite、文件系统或模型 SDK。
```

## 9. 用户确认点

| 编号 | 确认点 | 专业建议 |
|---|---|---|
| C-F-005-002-001 | AI 正则生成是否保留 | 建议保留，对齐参考项目且实用 |

## 10. 执行后记录

```txt
改了哪些文件：见 docs/tasks/P7-剧本结果批.md
验证结果：verify-p7-scripts、typecheck、build 通过
未完成事项：完整资产中心、角景音频、生产工作台和导出继续归 P8/P9/P10
最终结论：已完成；批量新增剧本能力由 P7 剧本结果批覆盖
```

## 11. 最后大白话

```txt
我这次准备怎么做：
1. 做批量导入弹窗。
2. 用正则把多集剧本拆开。
3. 用户预览并勾选后才批量保存。

我不会做什么：
1. 不自动提取资产。
2. 不生成分镜。
3. 不用 index 当勾选 key。

确认规则：
用户确认后才执行；未确认前只改文档。
```
