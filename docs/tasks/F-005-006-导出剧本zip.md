# F-005-006 导出剧本 zip

状态：已完成（由 P7 剧本结果批覆盖）  
所属菜单：M-005 剧本  
对应功能文档：`docs/features/M-005-剧本.md`  
原则：已按 P7 批次集中实现

## 0. 快速理解

```txt
一句话：选中剧本后导出 zip，每集剧本一个 txt 文件。
为什么现在做：这是剧本文本导出，和后面的剪映草稿导出不是一回事。
做完后有什么用：用户能把剧本文本打包备份或给别人查看。
这一步不碰什么：不导出剪映草稿，不导出视频，不导出分镜图片。
```

## 1. 本次做什么

```txt
目标：
  对齐 Toonflow `/script/exportScript`。

只做：
  1. 选择剧本导出
  2. 每个剧本写一个 txt
  3. 打包 zip
  4. 处理非法文件名、重名、超长文件名
  5. 导出失败提示

不做：
  1. 剪映导出
  2. WebAV mp4 导出
  3. 分镜图片 zip
```

## 2. 参考项目怎么做

| 参考文件 | 关键逻辑 |
|---|---|
| `Toonflow-web/src/views/script/index.vue` | 选中剧本后请求 `/script/exportScript`，responseType=blob，下载 `script_YYYY-MM-DD.zip` |
| `Toonflow-app/src/routes/script/exportScript.ts` | 用 zip stream，每个剧本写 `${script.name}.txt` |

参考项目关键事实：

```txt
1. 导出的是剧本文本 zip。
2. 后端响应 Content-Type=application/zip。
3. 后端文件名 scripts.zip，前端下载名 script_YYYY-MM-DD.zip。
4. 源码没处理非法文件名和重名。
5. 没选中剧本时按钮禁用，但仍需要保护提示。
```

## 3. 用户操作

```txt
入口：
  剧本页顶部 -> 导出剧本。

按钮/操作：
  1. 勾选剧本。
  2. 点击导出剧本。
  3. 选择保存位置或直接下载。

弹窗/表单：
  可选保存目录弹窗，按桌面端实现决定。

成功反馈：
  zip 导出成功，显示路径或触发下载。

失败反馈：
  未选中、文件名处理失败、写 zip 失败。
```

## 4. 要做什么功能

### 1. 选择剧本导出

怎么做：
- 输入：scriptIds。
- 输出：待导出剧本列表。
- 写什么数据：不写数据库。
- 状态怎么变：导出中 -> 成功。
- 异常怎么处理：未选择时提示。
- 限制：只能导出当前项目剧本。

### 2. 生成 zip

怎么做：
- 输入：剧本 name/content。
- 输出：zip 文件。
- 写什么数据：写导出文件。
- 状态怎么变：导出中 -> 完成。
- 异常怎么处理：某个剧本写入失败要列出失败项。
- 限制：txt 文件名必须清理非法字符和重名。

### 3. 返回导出结果

怎么做：
- 输入：zip 路径或 blob。
- 输出：导出成功反馈。
- 写什么数据：可选写导出历史，第一版不强制。
- 状态怎么变：按钮 loading 结束。
- 异常怎么处理：失败显示 msg。
- 限制：这不是剪映导出，不进入 M-010。

## 5. 数据和状态

```txt
字段：
  scripts.id
  scripts.name
  scripts.content

接口/能力：
  script.exportZip

数据读写：
  读 scripts
  写 zip 文件或返回 blob

任务状态：
  第一版可不写 tasks；如导出大量剧本可后续接入任务中心

轮询/Socket：
  无

模型调用：
  无

删除影响：
  无
```

## 6. VT Studio 怎么落

```txt
能力名：
  script.exportZip

调用链：
  renderer 导出按钮 -> window.vtStudio.script.exportZip -> main/services/scriptExport

需要新增：
  导出按钮逻辑
  zip 生成服务
  文件名清理工具

需要修改：
  无
```

## 7. 偏差

```txt
和 Toonflow 不同的地方：
  处理非法文件名、重名和超长文件名。

原因：
  Windows 文件系统限制必须处理，否则导出会失败。

是否写入 04：
  不需要，属于健壮性增强。
```

## 8. 验收

```txt
1. 未选中时不能导出。
2. 选中剧本能生成 zip。
3. 每集剧本一个 txt。
4. 重名剧本不会覆盖。
5. 非法文件名能清理。
6. 导出失败有提示。
7. 不和剪映导出混淆。
8. 页面不直接访问 SQLite 或文件系统。
```

## 9. 用户确认点

| 编号 | 确认点 | 专业建议 |
|---|---|---|
| C-F-005-006-001 | 桌面端导出位置 | 建议让用户选择目录，默认记住上次路径 |

## 10. 执行后记录

```txt
改了哪些文件：见 docs/tasks/P7-剧本结果批.md
验证结果：verify-p7-scripts、typecheck、build 通过
未完成事项：完整资产中心、角景音频、生产工作台和导出继续归 P8/P9/P10
最终结论：已完成；导出剧本 zip 能力由 P7 剧本结果批覆盖
```

## 11. 最后大白话

```txt
我这次准备怎么做：
1. 用户勾选剧本后导出 zip。
2. 每集剧本是一个 txt。
3. 文件名会处理非法字符和重名。

我不会做什么：
1. 不导出剪映草稿。
2. 不导出视频。
3. 不导出分镜图。

确认规则：
用户确认后才执行；未确认前只改文档。
```
