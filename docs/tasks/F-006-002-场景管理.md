# F-006-002 场景管理

状态：已完成（由 P8 资产和角景音频批覆盖）  
所属菜单：M-006 资产中心  
对应功能文档：`docs/features/M-006-资产中心.md`  
原则：先确认本文档，再改代码

## 0. 快速理解

```txt
一句话：资产中心做场景 tab，管理场景资产的列表、新增、编辑、删除和图片状态。
为什么现在做：场景资产会参与分镜、角景、图片生成和视频生产。
做完后有什么用：用户能维护场景名称、描述、提示词、主图和衍生子场景。
这一步不碰什么：不做分镜生成，不做视频生成，不做生产页衍生资产调度。
```

## 1. 本次做什么

```txt
目标：
  对齐 Toonflow `type=scene` 的资产管理能力。

只做：
  1. 场景 tab
  2. 场景父子资产列表
  3. 新增场景
  4. 编辑场景
  5. 删除/批量删除场景
  6. 展示主图、提示词、描述、备注、创建时间

不做：
  1. 批量生成提示词
  2. 批量生成图片
  3. 生产工作台衍生资产
```

## 2. 参考项目怎么做

| 参考文件 | 关键逻辑 |
|---|---|
| `Toonflow-web/src/views/assets/index.vue` | scene tab 逻辑同 role/tool |
| `Toonflow-web/src/views/assets/components/addAssets.vue` | 普通资产新增/编辑 |
| `Toonflow-app/src/routes/assets/getAssetsApi.ts` | 按 type=scene 查询资产 |
| `Toonflow-app/src/routes/assets/addAssets.ts` | 新增场景资产 |
| `Toonflow-app/src/routes/assets/updateAssets.ts` | 更新场景资产 |
| `Toonflow-app/src/routes/assets/delAssets.ts` | 删除资产 |

参考项目关键事实：

```txt
1. 场景类型是 scene。
2. 场景视觉手册用于后续提示词润色：父场景 key=art_scene，衍生场景 key=art_scene_derivative。
3. 场景和角色/道具的列表逻辑一致。
4. 删除源码同样存在批量删除不完整风险。
```

## 3. 用户操作

```txt
入口：
  项目内顶部菜单 -> 资产 -> 场景 tab。

按钮/操作：
  新增、编辑、删除、批量删除、展开子场景、勾选。

弹窗/表单：
  新增/编辑场景弹窗；删除确认框。

成功反馈：
  保存或删除成功，刷新列表。

失败反馈：
  必填缺失、生成中禁止操作、删除失败。
```

## 4. 要做什么功能

### 1. 查询场景列表

怎么做：
- 输入：projectId、name、page、pageSize。
- 输出：场景父子资产树。
- 写什么数据：不写。
- 状态怎么变：loading -> loaded。
- 异常怎么处理：查询失败显示错误状态。
- 限制：只查 type=scene。

### 2. 新增/编辑场景

怎么做：
- 输入：name、describe、remark、prompt。
- 输出：场景资产。
- 写什么数据：写或更新 assets。
- 状态怎么变：保存中 -> 列表刷新。
- 异常怎么处理：name/describe 缺失不能保存。
- 限制：不能修改资产类型。

### 3. 删除场景

怎么做：
- 输入：scene assetIds。
- 输出：删除结果。
- 写什么数据：删除场景、子场景、媒体、关系和文件。
- 状态怎么变：删除中 -> 列表刷新。
- 异常怎么处理：生成中禁止删除；失败回滚。
- 限制：批量删除必须完整级联。

## 5. 数据和状态

```txt
字段：
  assets.type=scene
  media_files
  asset relations

接口/能力：
  asset.list
  asset.create
  asset.update
  asset.delete
  asset.batchDelete

数据读写：
  读写 assets/media_files

任务状态：
  本任务不创建任务

轮询/Socket：
  无

模型调用：
  无

删除影响：
  影响剧本资产关系、分镜资产关系、生产引用
```

## 6. VT Studio 怎么落

```txt
能力名：
  asset.scene

调用链：
  renderer 场景 tab -> window.vtStudio.asset.* -> main/services/asset

需要新增：
  场景 tab
  场景筛选和列表

需要修改：
  资产类型常量：scene
```

## 7. 偏差

```txt
和 Toonflow 不同的地方：
  删除使用完整级联，不照搬 batchDelete 只删 o_assets。

原因：
  防止媒体文件和关系残留。

是否写入 04：
  和 F-006-001 共用删除偏差记录。
```

## 8. 验收

```txt
1. 场景 tab 能查询当前项目场景。
2. 可新增场景。
3. 可编辑场景。
4. 可删除和批量删除场景。
5. 生成中场景不能删除。
6. 子场景能展开。
7. 删除清理关系和文件。
8. 页面不直接访问 SQLite 或文件系统。
```

## 9. 用户确认点

| 编号 | 确认点 | 专业建议 |
|---|---|---|
| C-F-006-002-001 | 场景是否要单独做“地点层级” | 建议第一版不做，参考源码只做父子资产 |

## 10. 执行后记录

```txt
改了哪些文件：（完成后填写）
验证结果：（完成后填写）
未完成事项：（完成后填写）
最终结论：（完成后填写）
```

## 11. 最后大白话

```txt
我这次准备怎么做：
1. 做场景资产列表和增删改。
2. 场景和角色一样支持父子资产。
3. 删除时清理相关图片、文件和关系。

我不会做什么：
1. 不做地点层级。
2. 不做生产页衍生资产。

确认规则：
用户确认后才执行；未确认前只改文档。
```
