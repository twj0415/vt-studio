# F-006-006 素材预览

状态：已完成（由 P8 资产和角景音频批覆盖）  
所属菜单：M-006 资产中心  
对应功能文档：`docs/features/M-006-资产中心.md`  
原则：先确认本文档，再改代码

## 0. 快速理解

```txt
一句话：资产中心所有图片、视频、音频都必须能安全预览。
为什么现在做：资产看不到就无法判断是否可用，后续生产也会混乱。
做完后有什么用：用户能查看图片大图、播放视频和音频，并看到文件缺失状态。
这一步不碰什么：不生成素材，不编辑视频，不直接暴露本地路径。
```

## 1. 本次做什么

```txt
目标：
  对齐 Toonflow 图片/视频/音频预览能力，同时改成主进程受控文件 URL。

只做：
  1. 图片缩略图和大图预览
  2. 视频预览弹窗
  3. 音频预览播放器
  4. 文件缺失状态
  5. 受控文件 URL

不做：
  1. 素材生成
  2. 视频剪辑
  3. 文件管理器打开目录
```

## 2. 参考项目怎么做

| 参考文件 | 关键逻辑 |
|---|---|
| `Toonflow-web/src/views/assets/index.vue` | 图片用 image viewer，视频/audio 打开媒体预览 |
| `Toonflow-app/src/routes/assets/getAssetsApi.ts` | role/tool/scene 用 smallImageUrl，clip/audio 用 fileUrl |
| `Toonflow-app/src/routes/assets/getMaterialData.ts` | 按 filePath 转 URL |

参考项目关键事实：

```txt
1. 图片、视频、音频按扩展名判断媒体类型。
2. 图片大图可查看。
3. 视频和音频走媒体预览弹窗。
4. filePath 是内部路径，不是最终展示 URL。
5. VT Studio 必须由 main 服务生成受控 URL。
```

## 3. 用户操作

```txt
入口：
  资产中心各 tab 的预览列、素材卡片、媒体弹窗。

按钮/操作：
  1. 点击图片查看大图。
  2. 点击视频播放。
  3. 点击音频播放。

弹窗/表单：
  图片 viewer；媒体预览弹窗。

成功反馈：
  媒体正常显示或播放。

失败反馈：
  文件不存在、格式不支持、加载失败。
```

## 4. 要做什么功能

### 1. 生成受控预览 URL

怎么做：
- 输入：mediaId 或业务对象 id。
- 输出：短期可用预览 URL。
- 写什么数据：不写。
- 状态怎么变：加载中 -> 可预览。
- 异常怎么处理：文件不存在返回缺失状态。
- 限制：renderer 不接触裸 filePath。

### 2. 图片预览

怎么做：
- 输入：图片 URL。
- 输出：缩略图和大图。
- 写什么数据：不写。
- 状态怎么变：加载成功/失败。
- 异常怎么处理：加载失败显示占位。
- 限制：生成中状态显示 loading，不尝试加载空图。

### 3. 视频/音频预览

怎么做：
- 输入：媒体 URL。
- 输出：video/audio 播放器。
- 写什么数据：不写。
- 状态怎么变：播放/暂停。
- 异常怎么处理：格式不支持或文件缺失显示错误。
- 限制：不编辑媒体内容。

## 5. 数据和状态

```txt
字段：
  media_files.id
  media_files.file_path
  media_files.media_type
  media_files.status
  media_files.error_reason

接口/能力：
  media.getPreviewUrl

数据读写：
  读 media_files
  读文件存在性

任务状态：
  不写 tasks

轮询/Socket：
  无

模型调用：
  无

删除影响：
  无
```

## 6. VT Studio 怎么落

```txt
能力名：
  media.preview

调用链：
  renderer 预览组件 -> window.vtStudio.media.getPreviewUrl -> main/services/media

需要新增：
  媒体预览服务
  文件缺失状态
  图片/video/audio 预览组件

需要修改：
  资产列表不直接使用本地路径
```

## 7. 偏差

```txt
和 Toonflow 不同的地方：
  使用 main 侧受控预览 URL，不直接把 filePath 给页面。

原因：
  防止任意本地路径暴露，统一文件权限边界。

是否写入 04：
  如本地文件层已记录，不重复。
```

## 8. 验收

```txt
1. 图片能显示缩略图。
2. 图片能查看大图。
3. 视频能播放。
4. 音频能播放。
5. 文件缺失有缺失状态。
6. 格式不支持有提示。
7. 页面看不到裸本地路径。
8. 预览不直接访问文件系统。
```

## 9. 用户确认点

| 编号 | 确认点 | 专业建议 |
|---|---|---|
| C-F-006-006-001 | 预览 URL 是否短期有效 | 建议短期有效，避免长期暴露本地文件 |

## 10. 执行后记录

```txt
改了哪些文件：（完成后填写）
验证结果：（完成后填写）
未完成事项：（完成后填写）
最终结论：（完成后填写）
```

## 11. 最后大白话

```txt
我这次准备怎么做：
1. 图片、视频、音频都能预览。
2. 预览 URL 由主进程生成。
3. 文件不存在时给清楚提示。

我不会做什么：
1. 不生成素材。
2. 不编辑视频。
3. 不暴露本地路径。

确认规则：
用户确认后才执行；未确认前只改文档。
```
