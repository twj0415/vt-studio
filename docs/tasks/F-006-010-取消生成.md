# F-006-010 取消生成

状态：已完成（由 P8 资产和角景音频批覆盖）  
所属菜单：M-006 资产中心  
对应功能文档：`docs/features/M-006-资产中心.md`  
原则：先确认本文档，再改代码

## 0. 快速理解

```txt
一句话：参考项目后端有取消图片生成接口，但资产中心页面没有直接按钮，第一版先定边界。
为什么现在做：取消生成如果只改状态会变成假取消，必须和任务队列统一。
做完后有什么用：后续需要取消时知道传 imageId/mediaId，不会传资产 id 乱改。
这一步不碰什么：第一版资产中心不强行加取消按钮，不假装能取消供应商任务。
```

## 1. 本次做什么

```txt
目标：
  记录并规范 Toonflow `/assetsGenerate/cancelGenerate` 的取消能力，作为后续统一任务取消的资产侧边界。

只做：
  1. 明确取消目标是图片记录 id
  2. 明确资产中心第一版不加取消按钮也算对齐
  3. 明确如果加取消按钮必须接 task cancel handler
  4. 防止生成完成后覆盖取消状态

不做：
  1. 第一版 UI 取消按钮
  2. 供应商级真实取消协议
  3. 批量取消
  4. 视频取消
```

## 2. 参考项目怎么做

| 参考文件 | 关键逻辑 |
|---|---|
| `Toonflow-app/src/routes/assetsGenerate/cancelGenerate.ts` | 接收 o_image.id，把 state 改成生成失败 |
| `Toonflow-app/src/routes/assetsGenerate/generateAssets.ts` | 生成完成前检查 o_image.state，如果已是生成失败，不再写完成 |
| `Toonflow-app/src/routes/assetsGenerate/batchGenerateImageAssets.ts` | 批量生成也会检查状态 |
| `Toonflow-web/src/views/cornerScape/index.vue` | 角景页使用取消生成 |
| `Toonflow-web/src/views/assets/index.vue` | 资产中心页没找到直接取消按钮 |

参考项目关键事实：

```txt
1. 取消接口参数是 o_image.id，不是资产 id。
2. 取消效果是把图片 state 改为生成失败。
3. 生成 route 完成前会检查状态，已失败就不写完成。
4. 资产中心页面没有直接取消按钮，角景页有使用。
5. VT Studio 如果做取消，应接统一任务队列，不只是改状态。
```

## 3. 用户操作

```txt
入口：
  第一版资产中心无直接入口；后续如增加，入口在生成中图片状态上。

按钮/操作：
  后续可能点击取消生成。

弹窗/表单：
  取消确认框。

成功反馈：
  状态变为已取消或失败，生成结果不会覆盖。

失败反馈：
  无生成中记录、任务不支持取消、取消失败。
```

## 4. 要做什么功能

### 1. 明确取消目标

怎么做：
- 输入：mediaId/imageId。
- 输出：取消请求。
- 写什么数据：更新媒体状态或任务状态。
- 状态怎么变：running -> cancelled/failed。
- 异常怎么处理：id 不存在或不属于当前项目时拒绝。
- 限制：不能传 assetId 当取消目标。

### 2. 防止取消后被覆盖

怎么做：
- 输入：生成完成回调。
- 输出：检查当前状态。
- 写什么数据：仅当状态仍为 running 时写 succeeded。
- 状态怎么变：cancelled 不被覆盖。
- 异常怎么处理：状态已取消时丢弃生成结果并清理临时文件。
- 限制：所有图片生成实现都必须检查。

### 3. 接入统一任务取消

怎么做：
- 输入：taskId/mediaId。
- 输出：业务 cancel handler。
- 写什么数据：更新 tasks 和 media 状态。
- 状态怎么变：running -> cancelled。
- 异常怎么处理：供应商不支持真实取消时标记本地取消，并忽略后续结果。
- 限制：没有 cancel handler 的任务不能显示可取消按钮。

## 5. 数据和状态

```txt
字段：
  media_files.id/status/error_reason
  assets.current_media_id
  tasks.id/status

接口/能力：
  asset.cancelImageGeneration
  task.cancel

数据读写：
  更新 media_files
  更新 tasks

任务状态：
  running -> cancelled

轮询/Socket：
  轮询看到 cancelled 后更新页面

模型调用：
  不新调用模型；可能调用供应商取消接口

删除影响：
  取消后临时文件要清理
```

## 6. VT Studio 怎么落

```txt
能力名：
  asset.cancelImageGeneration

调用链：
  renderer 取消按钮 -> window.vtStudio.asset.cancelImageGeneration -> main/services/task/assetGeneration

需要新增：
  取消语义文档和任务 handler

需要修改：
  图片生成完成前检查状态
```

## 7. 偏差

```txt
和 Toonflow 不同的地方：
  取消状态用 cancelled，不用“生成失败”伪装取消。

原因：
  用户取消和模型失败是两种不同结果。

是否写入 04：
  写入，属于状态语义增强。
```

## 8. 验收

```txt
1. 文档明确取消目标是 imageId/mediaId。
2. 不会传 assetId 取消。
3. 取消后生成完成不会覆盖状态。
4. 没有 cancel handler 的任务不显示取消按钮。
5. cancelled 和 failed 区分显示。
6. 页面不直接改数据库状态。
```

## 9. 用户确认点

| 编号 | 确认点 | 专业建议 |
|---|---|---|
| C-F-006-010-001 | 资产中心第一版是否加取消按钮 | 建议先不加，对齐参考项目；统一任务取消稳定后再加 |
| C-F-006-010-002 | 取消状态是否独立为 cancelled | 建议必须独立，不和失败混用 |

## 10. 执行后记录

```txt
改了哪些文件：（完成后填写）
验证结果：（完成后填写）
未完成事项：（完成后填写）
最终结论：（完成后填写）
```

## 11. 最后大白话

```txt
我这次准备怎么做：
1. 先把取消生成的规则定清楚。
2. 取消目标是图片记录，不是资产。
3. 后续如果加按钮，必须接统一任务取消。

我不会做什么：
1. 第一版不硬加资产中心取消按钮。
2. 不把用户取消伪装成模型失败。
3. 不做假取消。

确认规则：
用户确认后才执行；未确认前只改文档。
```
