# F-007-001 角色/场景/音频资产列表

状态：已完成（由 P8 资产和角景音频批覆盖）  
所属菜单：M-007 角景音频绑定  
对应功能文档：`docs/features/M-007-角景音频绑定.md`  
原则：先确认本文档，再改代码

## 0. 快速理解

```txt
一句话：角景页展示当前项目的角色、场景、道具资产，并显示图片、提示词和已绑定音频。
为什么现在做：音频绑定和批量生成都要先有可操作的角景资产列表。
做完后有什么用：用户能在一个页面看到哪些资产有图、有 prompt、有绑定音频。
这一步不碰什么：不上传新音频，不生成 TTS，不做生产工作台。
```

## 1. 本次做什么

```txt
目标：
  对齐 Toonflow `/cornerScape/getAllAssets` 的角景资产看板。

只做：
  1. 查询 role/scene/tool 父资产
  2. 排除 clip/audio 和子资产
  3. 展示当前图片和历史图片
  4. 展示 prompt、promptState、imageState
  5. 展示已绑定音频
  6. 类型筛选和快捷选择

不做：
  1. 音频上传
  2. TTS 生成
  3. 生产分镜
  4. 资产中心 CRUD
```

## 2. 参考项目怎么做

| 参考文件 | 关键逻辑 |
|---|---|
| `Toonflow-web/src/views/cornerScape/index.vue` | 左侧批量设置，中间资产卡片，右侧单项抽屉 |
| `Toonflow-app/src/routes/cornerScape/getAllAssets.ts` | 查询父资产 role/scene/tool，拼 historyImages 和 relepedAudio |
| `Toonflow-app/src/routes/assets/getAssetsApi.ts` | 资产中心通用资产结构 |

参考项目关键事实：

```txt
1. 只取父资产，assetsId=null。
2. 排除 clip 和 audio。
3. 类型包括 role、scene、tool。
4. 查询历史成功图片。
5. 查询已绑定音频，字段旧拼写是 relepedAudio，VT Studio 不能照搬。
6. audioBindState 主要由角景页使用。
```

## 3. 用户操作

```txt
入口：
  项目内顶部菜单 -> 角景。

按钮/操作：
  1. 切换素材类型筛选。
  2. 全选、反选、取消选择。
  3. 选择 prompt 为空项、未生成项、错误项。
  4. 点击资产卡片打开右侧抽屉。

弹窗/表单：
  右侧单项配置抽屉。

成功反馈：
  资产卡片正常展示。

失败反馈：
  查询失败显示空状态或错误提示。
```

## 4. 要做什么功能

### 1. 查询角景资产

怎么做：
- 输入：projectId、types 可选。
- 输出：role/scene/prop 父资产列表。
- 写什么数据：不写。
- 状态怎么变：loading -> loaded。
- 异常怎么处理：查询失败显示错误状态。
- 限制：不返回 clip/audio，不返回子资产。

### 2. 展示图片和历史图

怎么做：
- 输入：asset current image、historyImages。
- 输出：卡片图片和抽屉历史图。
- 写什么数据：不写。
- 状态怎么变：根据 image status 显示生成中/失败/已完成。
- 异常怎么处理：文件缺失显示缺失状态。
- 限制：预览 URL 走媒体服务，不暴露路径。

### 3. 展示已绑定音频

怎么做：
- 输入：asset_audio_links。
- 输出：boundAudios 标签。
- 写什么数据：不写。
- 状态怎么变：无。
- 异常怎么处理：音频资产已删除时显示缺失。
- 限制：正式字段用 boundAudios，不用 relepedAudio。

### 4. 快捷选择

怎么做：
- 输入：当前资产列表。
- 输出：selectedIds。
- 写什么数据：不写。
- 状态怎么变：选中状态更新。
- 异常怎么处理：生成中资产不可选。
- 限制：选择只影响页面操作，不改数据库。

## 5. 数据和状态

```txt
字段：
  assets.id/type/name/description/prompt/prompt_status/audio_bind_status
  media_files
  asset_audio_links.asset_id/audio_asset_id

接口/能力：
  cornerScape.listAssets

数据读写：
  读 assets/media/audio links

任务状态：
  不写 tasks

轮询/Socket：
  轮询见后续功能

模型调用：
  无

删除影响：
  无
```

## 6. VT Studio 怎么落

```txt
能力名：
  cornerScape.listAssets

调用链：
  renderer 角景页 -> window.vtStudio.cornerScape.listAssets -> main/services/cornerScape

需要新增：
  角景页布局
  资产卡片
  类型筛选和选择逻辑

需要修改：
  正式字段 boundAudios
```

## 7. 偏差

```txt
和 Toonflow 不同的地方：
  relepedAudio 改为 boundAudios。

原因：
  relepedAudio 是明显拼写错误。

是否写入 04：
  不需要，属于旧痕迹修正。
```

## 8. 验收

```txt
1. 角景页只显示角色、场景、道具。
2. 不显示 clip/audio。
3. 不显示子资产作为主卡片。
4. 卡片显示图片状态。
5. 卡片显示 prompt 状态。
6. 卡片显示已绑定音频。
7. 生成中资产不可选。
8. 页面不直接访问 SQLite 或文件系统。
```

## 9. 用户确认点

| 编号 | 确认点 | 专业建议 |
|---|---|---|
| C-F-007-001-001 | 菜单是否继续叫“角景” | 建议暂时保留，页面标题可明确“角色/场景/道具生成与音频参考” |

## 10. 执行后记录

```txt
改了哪些文件：（完成后填写）
验证结果：（完成后填写）
未完成事项：（完成后填写）
最终结论：（完成后填写）
```

## 11. 最后大白话

```txt
我这次准备怎么做：
1. 做角景资产看板。
2. 只看角色、场景、道具。
3. 显示图片、提示词和绑定音频。

我不会做什么：
1. 不上传音频。
2. 不做 TTS。
3. 不做生产分镜。

确认规则：
用户确认后才执行；未确认前只改文档。
```
