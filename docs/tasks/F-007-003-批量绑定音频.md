# F-007-003 批量绑定音频

状态：已完成（由 P8 资产和角景音频批覆盖）  
所属菜单：M-007 角景音频绑定  
对应功能文档：`docs/features/M-007-角景音频绑定.md`  
原则：先确认本文档，再改代码

## 0. 快速理解

```txt
一句话：选中多个资产后，用文本模型从已有音色里自动匹配一个音色。
为什么现在做：手动给大量角色/场景/道具绑定音频很慢。
做完后有什么用：系统能按名称和描述自动选择最合适的已有音色。
这一步不碰什么：不生成新音频，不做 TTS，不允许一个资产多个音色。
```

## 1. 本次做什么

```txt
目标：
  对齐 Toonflow `/cornerScape/batchBindAudio`。

只做：
  1. 批量选择资产
  2. 查询候选音频父资产
  3. 读取 audioBindPrompt
  4. 调用文本模型匹配 audioId
  5. 写绑定关系
  6. 写 audioBindState 和失败原因

不做：
  1. TTS 生成
  2. 多音色绑定
  3. 上传音频
  4. 语音合成文件
```

## 2. 参考项目怎么做

| 参考文件 | 关键逻辑 |
|---|---|
| `Toonflow-web/src/views/cornerScape/index.vue` | 左侧“一键绑定音频”，选中后本地 audioBindState=生成中 |
| `Toonflow-app/src/routes/cornerScape/batchBindAudio.ts` | 查询待绑定资产和候选 audio，用 universalAi 匹配 audioId |
| `Toonflow-app/src/routes/cornerScape/pollingAudio.ts` | 轮询 audioBindState 不为生成中的资产 |

参考项目关键事实：

```txt
1. 候选音频是 assets type=audio 且 assetsId=null 的父资产。
2. 没有候选音频时返回“暂无设置音频，请先前往资产中心上传音频”。
3. 提示词来源 o_prompt type=audioBindPrompt，优先 useData。
4. resultTool schema 描述说列表/空数组，但实际字段是单个 audioId；VT Studio 按单个 audioId 做。
5. 失败只写 audioBindState=生成失败，源码没有独立失败原因字段，VT Studio 要补。
```

## 3. 用户操作

```txt
入口：
  角景页 -> 勾选资产 -> 一键绑定音频。

按钮/操作：
  1. 勾选资产。
  2. 点击一键绑定音频。

弹窗/表单：
  可选确认框。

成功反馈：
  资产显示音频绑定中，完成后显示绑定音色。

失败反馈：
  未选中、没有音频素材、模型失败、无匹配结果。
```

## 4. 要做什么功能

### 1. 启动批量绑定

怎么做：
- 输入：assetIds、projectId。
- 输出：启动结果。
- 写什么数据：assets.audio_bind_status=running。
- 状态怎么变：idle/failed -> running。
- 异常怎么处理：未选中或没有候选音频时提示。
- 限制：只处理 role/scene/prop。

### 2. 模型匹配音色

怎么做：
- 输入：待绑定资产列表、候选音频列表、audioBindPrompt。
- 输出：每个资产的 audioId。
- 写什么数据：不直接在 renderer 写。
- 状态怎么变：每个资产独立完成。
- 异常怎么处理：模型失败、返回非法 audioId、无匹配都写失败原因。
- 限制：输出是单个 audioId，不是数组。

### 3. 写绑定关系

怎么做：
- 输入：assetId、audioId。
- 输出：绑定结果。
- 写什么数据：删除旧关系，插入新关系，更新 audio_bind_status。
- 状态怎么变：running -> succeeded。
- 异常怎么处理：写库失败回滚该资产绑定状态为 failed。
- 限制：一个资产一个音色。

### 4. 写任务中心

怎么做：
- 输入：批量绑定请求。
- 输出：任务记录。
- 写什么数据：tasks。
- 状态怎么变：running -> succeeded/failed。
- 异常怎么处理：失败原因脱敏。
- 限制：这是 VT Studio 增强，写 04。

## 5. 数据和状态

```txt
字段：
  assets.audio_bind_status
  assets.audio_bind_error
  asset_audio_links
  prompts.audio_bind_prompt

接口/能力：
  cornerScape.batchBindAudio

数据读写：
  读 assets/audio candidates/prompts
  写 asset_audio_links
  写 audio bind status

任务状态：
  建议写 tasks，分类为音频绑定

轮询/Socket：
  轮询见 F-007-004

模型调用：
  文本模型 universalAi 或模型映射

删除影响：
  无
```

## 6. VT Studio 怎么落

```txt
能力名：
  cornerScape.batchBindAudio

调用链：
  renderer 一键绑定 -> window.vtStudio.cornerScape.batchBindAudio -> main/services/cornerScape -> model adapter

需要新增：
  批量绑定服务
  audioBindError 字段
  任务中心接入

需要修改：
  resultTool schema 使用单个 audioId
```

## 7. 偏差

```txt
和 Toonflow 不同的地方：
  1. 增加 audio_bind_error。
  2. 批量绑定写任务中心。
  3. result schema 明确单个 audioId。

原因：
  失败原因必须可见；参考 schema 描述和实际不一致。

是否写入 04：
  写入。
```

## 8. 验收

```txt
1. 未选中资产不能批量绑定。
2. 没有音频素材时提示去资产中心添加。
3. 批量绑定开始后状态为 running。
4. 成功后写绑定关系。
5. 失败后写失败原因。
6. 一个资产只绑定一个音色。
7. 任务中心能看到批量绑定任务。
8. 页面不直接调用模型 SDK。
```

## 9. 用户确认点

| 编号 | 确认点 | 专业建议 |
|---|---|---|
| C-F-007-003-001 | 批量绑定音频是否进入任务中心 | 建议进入 |
| C-F-007-003-002 | 没有合适音色是否算失败 | 建议算可读失败，写“未匹配到合适音色” |

## 10. 执行后记录

```txt
改了哪些文件：（完成后填写）
验证结果：（完成后填写）
未完成事项：（完成后填写）
最终结论：（完成后填写）
```

## 11. 最后大白话

```txt
我这次准备怎么做：
1. 用户选多个资产。
2. 系统用模型从已有音色里选一个最合适的。
3. 绑定结果写关系表，失败写原因。

我不会做什么：
1. 不生成音频。
2. 不绑定多个音色。
3. 不新增音频素材。

确认规则：
用户确认后才执行；未确认前只改文档。
```
