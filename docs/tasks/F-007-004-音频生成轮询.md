# F-007-004 音频生成轮询

状态：已完成（由 P8 资产和角景音频批覆盖）  
所属菜单：M-007 角景音频绑定  
对应功能文档：`docs/features/M-007-角景音频绑定.md`  
原则：先确认本文档，再改代码

## 0. 快速理解

```txt
一句话：这里的“音频轮询”其实是轮询音频绑定状态，不是轮询 TTS 生成。
为什么现在做：批量绑定音频是异步模型任务，页面要自动更新结果。
做完后有什么用：绑定完成后卡片自动显示音色，失败后显示原因。
这一步不碰什么：不生成音频文件，不做 TTS，不轮询视频。
```

## 1. 本次做什么

```txt
目标：
  对齐 Toonflow `/cornerScape/pollingAudio`，但把命名解释清楚。

只做：
  1. 找出 audioBindStatus=running 的资产
  2. 每 3 秒轮询绑定状态
  3. 成功后刷新 boundAudios
  4. 失败后展示 audioBindError
  5. 离开页面停止轮询

不做：
  1. TTS 文件生成状态
  2. 音频文件上传状态
  3. 视频生成轮询
```

## 2. 参考项目怎么做

| 参考文件 | 关键逻辑 |
|---|---|
| `Toonflow-web/src/views/cornerScape/index.vue` | computed audioBindData 找 audioBindState=生成中，每 3 秒轮询 |
| `Toonflow-app/src/routes/cornerScape/pollingAudio.ts` | 查询 id in ids 且 audioBindState != 生成中 的资产 |

参考项目关键事实：

```txt
1. audioBindState=生成中 才轮询。
2. 每 3 秒请求一次。
3. 返回完成项后刷新 getAllAssets，以更新 relepedAudio。
4. 源码没有失败原因字段，VT Studio 要补。
5. 这不是音频文件生成。
```

## 3. 用户操作

```txt
入口：
  角景页自动执行。

按钮/操作：
  无。

弹窗/表单：
  无。

成功反馈：
  绑定完成后卡片和抽屉显示音色。

失败反馈：
  绑定失败后卡片显示失败状态和原因。
```

## 4. 要做什么功能

### 1. 识别绑定中资产

怎么做：
- 输入：dataList。
- 输出：audioBindStatus=running 的 assetIds。
- 写什么数据：不写。
- 状态怎么变：有 running 才启动轮询。
- 异常怎么处理：无 id 的项忽略。
- 限制：使用明确状态枚举。

### 2. 轮询绑定状态

怎么做：
- 输入：assetIds。
- 输出：已完成或失败的资产状态。
- 写什么数据：不写。
- 状态怎么变：每 3 秒请求一次。
- 异常怎么处理：请求失败不清空列表，下一轮继续。
- 限制：页面卸载停止 timer。

### 3. 刷新绑定结果

怎么做：
- 输入：状态变化结果。
- 输出：最新 boundAudios。
- 写什么数据：不写。
- 状态怎么变：running -> succeeded/failed。
- 异常怎么处理：刷新失败提示。
- 限制：保留当前选择状态合理清理。

## 5. 数据和状态

```txt
字段：
  assets.audio_bind_status
  assets.audio_bind_error
  asset_audio_links

接口/能力：
  cornerScape.pollAudioBindStatus
  cornerScape.listAssets

数据读写：
  读 assets/audio links

任务状态：
  如果 F-007-003 写任务中心，这里只同步业务状态

轮询/Socket：
  3 秒轮询，无 Socket

模型调用：
  无

删除影响：
  无
```

## 6. VT Studio 怎么落

```txt
能力名：
  cornerScape.pollAudioBindStatus

调用链：
  renderer 角景页轮询 -> window.vtStudio.cornerScape.pollAudioBindStatus -> main/services/cornerScape

需要新增：
  轮询控制
  失败原因展示

需要修改：
  角景资产刷新逻辑
```

## 7. 偏差

```txt
和 Toonflow 不同的地方：
  明确命名为音频绑定轮询，并展示失败原因。

原因：
  参考项目没有 TTS，不能误导成音频生成。

是否写入 04：
  失败原因字段偏差在 F-007-003 记录即可。
```

## 8. 验收

```txt
1. 有绑定中资产时启动轮询。
2. 没有绑定中资产时不轮询。
3. 轮询间隔约 3 秒。
4. 成功后显示绑定音色。
5. 失败后显示失败原因。
6. 离开页面停止轮询。
7. 不生成任何音频文件。
8. 页面不直接访问 SQLite。
```

## 9. 用户确认点

| 编号 | 确认点 | 专业建议 |
|---|---|---|
| C-F-007-004-001 | 文档和 UI 是否改名为“音频绑定轮询” | 建议改，避免误解为 TTS 生成 |

## 10. 执行后记录

```txt
改了哪些文件：（完成后填写）
验证结果：（完成后填写）
未完成事项：（完成后填写）
最终结论：（完成后填写）
```

## 11. 最后大白话

```txt
我这次准备怎么做：
1. 页面自动轮询正在绑定音频的资产。
2. 绑定成功就显示音色。
3. 绑定失败就显示原因。

我不会做什么：
1. 不生成音频文件。
2. 不做 TTS。
3. 不轮询视频。

确认规则：
用户确认后才执行；未确认前只改文档。
```
