# F-007-005 角景页复用资产提示词和图片生成

状态：已完成（由 P8 资产和角景音频批覆盖）  
所属菜单：M-007 角景音频绑定  
对应功能文档：`docs/features/M-007-角景音频绑定.md`  
原则：先确认本文档，再改代码

## 0. 快速理解

```txt
一句话：角景页的提示词润色、图片生成、历史图切换必须复用资产中心能力。
为什么现在做：同一类资产生成如果写两套逻辑，状态和文件一定会乱。
做完后有什么用：角景页可以批量生成 prompt、批量生成图、单项重新生成，并和资产中心数据一致。
这一步不碰什么：不重新发明图片生成服务，不做视频生成，不做 ComfyUI 页面。
```

## 1. 本次做什么

```txt
目标：
  对齐 Toonflow 角景页复用 `/assetsGenerate/*` 和 `/assets/saveAssets` 的行为，同时拆清接口语义。

只做：
  1. 单项提示词保存
  2. 单项 AI 润色
  3. 单项重新生成图片
  4. 批量生成提示词
  5. 批量生成图片
  6. 历史图片切换
  7. 取消生成入口规则

不做：
  1. 单独新建一套生成服务
  2. 生产工作台图片工作流
  3. 视频生成
  4. ComfyUI workflow 编辑
```

## 2. 参考项目怎么做

| 参考文件 | 关键逻辑 |
|---|---|
| `Toonflow-web/src/views/cornerScape/index.vue` | 左侧批量生成提示词/图片，右侧抽屉单项润色/生成/历史图切换 |
| `Toonflow-app/src/routes/assetsGenerate/polishAssetsPrompt.ts` | 单项提示词润色 |
| `Toonflow-app/src/routes/assetsGenerate/batchPolishAssetsPrompt.ts` | 批量提示词润色 |
| `Toonflow-app/src/routes/assetsGenerate/generateAssets.ts` | 单项图片生成 |
| `Toonflow-app/src/routes/assetsGenerate/batchGenerateImageAssets.ts` | 批量图片生成 |
| `Toonflow-app/src/routes/assets/saveAssets.ts` | 保存 prompt 或 imageId，但语义过重 |
| `Toonflow-app/src/routes/assetsGenerate/cancelGenerate.ts` | 取消图片生成 |

参考项目关键事实：

```txt
1. 角景页会传 otherTextPrompt，所以不触发资产中心批量提示词缺参冲突。
2. 提示词失焦保存复用 saveAssets，存在误清 imageId 风险。
3. 历史图片再次点击可能传 imageId undefined，存在误清当前图风险。
4. 取消生成传 freshItem.imageId。
5. onUnmounted 重置本地状态不等于取消后端任务。
```

## 3. 用户操作

```txt
入口：
  角景页左侧批量设置；右侧单项配置抽屉。

按钮/操作：
  1. 批量生成提示词。
  2. 批量生成图片。
  3. 抽屉里 AI 润色。
  4. 抽屉里重新生成。
  5. 失焦保存 prompt。
  6. 选择历史图片。
  7. 取消生成。

弹窗/表单：
  右侧抽屉；取消确认框。

成功反馈：
  prompt、图片、历史图状态更新。

失败反馈：
  缺 prompt、缺模型、缺分辨率、模型失败、取消失败。
```

## 4. 要做什么功能

### 1. 提示词保存

怎么做：
- 输入：assetId、prompt。
- 输出：保存结果。
- 写什么数据：更新 assets.prompt。
- 状态怎么变：dirty -> saved。
- 异常怎么处理：保存失败回滚 UI。
- 限制：单独能力 `asset.updatePrompt`，不复用重接口。

### 2. 单项和批量提示词生成

怎么做：
- 输入：assetId 或 assetIds、otherTextPrompt。
- 输出：prompt 更新。
- 写什么数据：写 assets.prompt/status/error。
- 状态怎么变：running -> succeeded/failed。
- 异常怎么处理：失败原因展示。
- 限制：复用 F-006-007 服务。

### 3. 单项和批量图片生成

怎么做：
- 输入：资产、prompt、model、resolution。
- 输出：图片结果。
- 写什么数据：写 media/image 记录和文件。
- 状态怎么变：running -> succeeded/failed。
- 异常怎么处理：缺 prompt 列出资产名称。
- 限制：复用 F-006-008/F-006-009 服务。

### 4. 历史图片切换

怎么做：
- 输入：assetId、mediaId。
- 输出：主图更新。
- 写什么数据：更新 assets.current_media_id。
- 状态怎么变：selectedHistoryId 更新。
- 异常怎么处理：mediaId 为空时不调用保存。
- 限制：不能误清当前图片。

### 5. 取消生成

怎么做：
- 输入：imageId/mediaId。
- 输出：取消结果。
- 写什么数据：更新 media/task 状态。
- 状态怎么变：running -> cancelled。
- 异常怎么处理：无生成中记录提示。
- 限制：不能把 onUnmounted 本地重置当取消。

## 5. 数据和状态

```txt
字段：
  assets.prompt
  assets.current_media_id
  assets.prompt_status
  media_files.status/error_reason
  tasks.status

接口/能力：
  asset.updatePrompt
  asset.batchGeneratePrompt
  asset.generateImage
  asset.batchGenerateImage
  asset.selectImage
  asset.cancelImageGeneration

数据读写：
  复用资产中心服务

任务状态：
  生成类任务写 tasks

轮询/Socket：
  提示词、图片、音频绑定各自轮询

模型调用：
  复用资产中心模型适配层

删除影响：
  无
```

## 6. VT Studio 怎么落

```txt
能力名：
  cornerScape.assetGeneration

调用链：
  renderer 角景页 -> window.vtStudio.asset.* / cornerScape.* -> main/services/assetGeneration

需要新增：
  角景页对资产中心服务的复用封装

需要修改：
  不复用 saveAssets 这种大而全接口
```

## 7. 偏差

```txt
和 Toonflow 不同的地方：
  拆分 updatePrompt/selectImage/cancelImageGeneration 等能力。

原因：
  参考 saveAssets 语义过重，容易误清 imageId。

是否写入 04：
  写入。
```

## 8. 验收

```txt
1. 角景页保存 prompt 不会改 imageId。
2. AI 润色复用资产中心逻辑。
3. 批量提示词复用资产中心逻辑。
4. 单项/批量图片生成复用资产中心逻辑。
5. 历史图切换不会误清主图。
6. 取消生成传 imageId/mediaId。
7. onUnmounted 不会伪取消任务。
8. 页面不直接访问模型 SDK 或文件系统。
```

## 9. 用户确认点

| 编号 | 确认点 | 专业建议 |
|---|---|---|
| C-F-007-005-001 | 角景页是否允许取消生成 | 建议保留，参考项目角景页已有，但必须接统一任务取消 |

## 10. 执行后记录

```txt
改了哪些文件：（完成后填写）
验证结果：（完成后填写）
未完成事项：（完成后填写）
最终结论：（完成后填写）
```

## 11. 最后大白话

```txt
我这次准备怎么做：
1. 角景页不另写生成逻辑。
2. 提示词和图片生成都复用资产中心服务。
3. 保存 prompt、选图、取消生成拆成清楚的小能力。

我不会做什么：
1. 不做第二套图片生成。
2. 不把卸载页面当取消任务。
3. 不做视频生成。

确认规则：
用户确认后才执行；未确认前只改文档。
```
