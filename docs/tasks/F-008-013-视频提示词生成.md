# F-008-013 视频提示词生成

状态：核心生成闭环已由 `OPT-004` 补齐；prompt 引用编辑器 UI 后续处理  
所属菜单：M-008 生产工作台  
对应功能文档：`docs/features/M-008-生产工作台.md`  
原则：核心服务已完成；后续 UI 增强仍要先写任务边界

## 0. 快速理解

```txt
一句话：为视频轨道生成视频模型需要的 prompt，支持单条和批量。
为什么现在做：视频生成不能直接拿分镜描述，必须按模型和模式生成合适提示词。
做完后有什么用：每条视频轨道都有可编辑的视频 prompt，后续才能生成视频候选。
这一步不碰什么：不生成视频文件，不选择候选视频，不做剪映导出。
```

## 1. 本次做什么

```txt
目标：
  对齐 Toonflow `/production/workbench/generateVideoPrompt` 和 `/batchGeneratePrompt`。

只做：
  1. 单轨视频提示词生成
  2. 批量视频提示词生成
  3. 按视频模型和 mode 读取提示词模板
  4. 写 videoTrack.prompt/state/reason
  5. 轮询提示词生成状态

不做：
  1. 视频候选生成
  2. 轨道删除
  3. 剪映导出
```

## 2. 参考项目怎么做

| 参考文件 | 关键逻辑 |
|---|---|
| `Toonflow-web/src/views/production/components/workbench/*` | 单轨/批量生成视频提示词 |
| `Toonflow-app/src/routes/production/workbench/generateVideoPrompt.ts` | 根据模型/mode 读取提示词，调用文本模型 |
| `Toonflow-app/src/routes/production/workbench/batchGeneratePrompt.ts` | 批量生成提示词 |
| `Toonflow-app/src/routes/production/workbench/checkVideoPrompt.ts` | 轮询轨道提示词状态 |

参考项目关键事实：

```txt
1. 视频提示词主要读取 storyboard.videoDesc。
2. 提示词模板按 video model/mode 从模型提示词映射读取。
3. 写入 o_videoTrack.prompt。
4. 状态有 未生成/生成中/已完成/生成失败。
5. 状态要统一，不能和视频候选成功状态混用。
```

## 3. 用户操作

```txt
入口：
  视频工作台 -> 视频生成模式 -> 轨道提示词区域。

按钮/操作：
  1. 点击单轨生成 prompt。
  2. 点击批量生成 prompt。
  3. 手动编辑 prompt。

弹窗/表单：
  可选确认框。

成功反馈：
  轨道 prompt 填充完成。

失败反馈：
  缺视频模型、缺 mode、缺模板、模型失败。
```

## 4. 要做什么功能

### 1. 单轨生成提示词

怎么做：
- 输入：trackId、storyboardId、model、mode。
- 输出：video prompt。
- 写什么数据：更新 video_tracks.prompt/status。
- 状态怎么变：idle -> running -> succeeded/failed。
- 异常怎么处理：模板缺失或模型失败写 reason。
- 限制：只作用于当前剧本轨道。

### 2. 批量生成提示词

怎么做：
- 输入：trackIds、并发数。
- 输出：每条轨道 prompt。
- 写什么数据：批量更新 video_tracks。
- 状态怎么变：每条独立 running -> succeeded/failed。
- 异常怎么处理：单条失败不影响其他轨道继续。
- 限制：并发数来自设置。

### 3. 手动更新提示词

怎么做：
- 输入：trackId、prompt。
- 输出：更新结果。
- 写什么数据：更新 video_tracks.prompt。
- 状态怎么变：dirty -> saved。
- 异常怎么处理：保存失败回滚 UI。
- 限制：不改变候选视频。

### 4. 轮询提示词状态

怎么做：
- 输入：running trackIds。
- 输出：完成或失败状态。
- 写什么数据：不写。
- 状态怎么变：页面更新。
- 异常怎么处理：轮询失败保留下一轮。
- 限制：轮询不重新执行模型。

## 5. 数据和状态

```txt
字段：
  video_tracks.prompt
  video_tracks.status
  video_tracks.reason
  storyboards.video_desc
  model_prompt_mappings

接口/能力：
  production.workbench.generateVideoPrompt
  production.workbench.batchGenerateVideoPrompt
  production.workbench.updateVideoPrompt
  production.workbench.pollVideoPrompt

数据读写：
  读 storyboards/model prompts
  写 video_tracks.prompt/status/reason

任务状态：
  写 tasks，分类为视频提示词生成

轮询/Socket：
  轮询提示词状态

模型调用：
  文本模型

删除影响：
  无
```

## 6. VT Studio 怎么落

```txt
能力名：
  production.workbench.videoPrompt

调用链：
  renderer 视频工作台 -> window.vtStudio.production.workbench.* -> main/services/videoWorkbench -> model adapter

需要新增：
  视频 prompt 生成服务
  轨道 prompt 保存
  轮询状态

需要修改：
  状态枚举统一
```

## 7. 偏差

```txt
和 Toonflow 不同的地方：
  视频提示词生成写任务中心，状态统一英文枚举。

原因：
  模型异步任务需要可追踪。

是否写入 04：
  写入。
```

## 8. 验收

```txt
1. 单轨能生成视频 prompt。
2. 批量能生成视频 prompt。
3. 缺模型或模板会失败并显示原因。
4. 成功后 prompt 写入轨道。
5. 可手动编辑 prompt。
6. 轮询能更新状态。
7. 任务中心能看到视频提示词任务。
8. 页面不直接调用模型 SDK。
```

## 9. 用户确认点

| 编号 | 确认点 | 专业建议 |
|---|---|---|
| C-F-008-013-001 | 视频提示词生成是否进入任务中心 | 建议进入 |

## 10. 执行后记录

```txt
改了哪些文件：
  src/main/services/settings/model-prompt.ts
  src/main/services/production/service.ts
  scripts/verify-opt-004-model-prompt-mode.mjs
  scripts/verify.mjs

验证结果：
  D:\software\nodejs\node.exe scripts\verify-opt-004-model-prompt-mode.mjs
  D:\software\nodejs\pnpm.cmd run typecheck
  D:\software\nodejs\pnpm.cmd run verify:production
  D:\software\nodejs\pnpm.cmd run verify:docs
  D:\software\nodejs\pnpm.cmd run build

未完成事项：
  promptEditor 引用编辑器 UI、@图/@视频/@音频 引用序列化和提交前数量限制仍在后续 UI 治理任务中处理。

最终结论：
  视频提示词生成不再是占位成功，已经按模型和 mode 读取模板并调用文本模型生成。
```

## 11. 最后大白话

```txt
我这次准备怎么做：
1. 给视频轨道生成视频 prompt。
2. 支持单条和批量。
3. 成功写进轨道，失败写原因。

我不会做什么：
1. 不生成视频文件。
2. 不做剪映导出。

确认规则：
核心服务已按用户连续执行要求完成；后续 UI 增强如涉及交互重构，再单独写任务边界。
```

## 12. 本轮补充

```txt
视频提示词编辑不是普通 textarea，要参考 promptEditor。
必须支持：
1. @图 / @图片 / @视频 / @音频 / @文本 引用。
2. 按模型 mode 限制引用类型和数量。
3. 支持键盘上下选择、Enter/Tab 确认、Esc 关闭。
4. 保存时同时保留 prompt 文本和引用对象。
5. imageReference:N / videoReference:N / audioReference:N 超数量时不能提交。
```
