# F-009-001 任务列表

状态：已完成（由 P4 任务中心批覆盖）  
所属菜单：M-009 任务中心  
对应功能文档：`docs/features/M-009-任务中心.md`  
原则：先确认本文档，再改代码

## 0. 快速理解

```txt
一句话：做任务中心的主表格，只负责查看任务，不负责执行模型生成。
为什么现在做：后续图片、视频、Agent、导出都会写任务，必须先能统一看到状态和失败原因。
做完后有什么用：用户能一眼看到哪些任务正在跑、哪些成功、哪些失败，以及失败在哪里。
这一步不碰什么：不做重试、不做取消、不直接调用模型、不写业务生成逻辑。
```

## 1. 本次做什么

```txt
目标：
  对齐 Toonflow `/task/getTaskApi` 的任务列表能力，落到 VT Studio 的统一任务表。

只做：
  1. 任务中心页面主表格
  2. 查询任务列表
  3. 显示任务分类、关联对象、模型、描述、状态、失败原因、开始时间
  4. 空状态、加载状态、失败提示
  5. 明确项目字段别名，避免 join 后覆盖

不做：
  1. 项目筛选
  2. 分类筛选
  3. 状态筛选
  4. 任务重试
  5. 任务取消
  6. 任务详情弹窗
```

## 2. 参考项目怎么做

| 参考文件 | 关键逻辑 |
|---|---|
| `Toonflow-web/src/views/task/index.vue` | 页面进入后调用任务列表接口，表格展示任务字段，失败状态用 tooltip 展示原因 |
| `Toonflow-app/src/routes/task/getTaskApi.ts` | 从 `o_tasks` leftJoin `o_project`，按条件分页查询，按 id desc 排序 |
| `Toonflow-app/src/utils/taskRecord.ts` | 生成任务写入任务表，字段包含 taskClass、relatedObjects、model、describe、state、startTime、reason |
| `Toonflow-app/src/lib/initDB.ts` | 定义 `o_tasks` 参考字段 |

参考项目关键事实：

```txt
1. 任务中心只展示任务，不直接执行模型。
2. 表格列包括任务大类、关联对象、模型、描述、失败原因、状态、开始时间。
3. 查询按 o_tasks.id desc 排序。
4. 接口 leftJoin 项目表，但正式页面没有展示项目名列。
5. Toonflow select 里有 `o_project.*`，存在字段覆盖风险；VT Studio 必须用明确别名。
```

## 3. 用户操作

```txt
入口：
  左侧全局菜单 -> 任务中心。

按钮/操作：
  打开页面后自动加载任务列表。

弹窗/表单：
  无。

成功反馈：
  表格显示任务；没有任务时显示空状态。

失败反馈：
  查询失败时显示错误提示，不让页面空白。
```

## 4. 要做什么功能

### 1. 打开任务中心并加载任务

怎么做：
- 输入：进入任务中心页面。
- 输出：任务列表和总数。
- 写什么数据：不写。
- 状态怎么变：loading=true -> loading=false。
- 异常怎么处理：查询失败显示错误提示，表格保留空数组。
- 限制：不默认自动轮询。

### 2. 展示任务表格

怎么做：
- 输入：任务数组。
- 输出：表格行。
- 写什么数据：不写。
- 状态怎么变：无。
- 异常怎么处理：字段为空时显示 `-` 或空态文案。
- 限制：关联对象如果是 JSON，只展示简短摘要，完整内容留给后续详情能力。

### 3. 展示状态和失败原因

怎么做：
- 输入：任务状态、失败原因。
- 输出：状态标签和失败原因列。
- 写什么数据：不写。
- 状态怎么变：无。
- 异常怎么处理：失败任务 reason 为空时显示“无失败原因”。
- 限制：不能暴露密钥、完整 stack、本地敏感路径。

### 4. 明确查询字段

怎么做：
- 输入：分页参数。
- 输出：带别名的任务数据。
- 写什么数据：不写。
- 状态怎么变：无。
- 异常怎么处理：字段冲突时按服务层 DTO 输出固定字段。
- 限制：不能照搬 `o_tasks.* + o_project.*` 的覆盖写法。

## 5. 数据和状态

```txt
字段：
  tasks.id
  tasks.project_id
  tasks.category
  tasks.related_object
  tasks.model_name
  tasks.description
  tasks.status
  tasks.fail_reason
  tasks.started_at
  projects.name as project_name 可选

接口/能力：
  task.list

数据读写：
  读 tasks
  可选读 projects.name

任务状态：
  pending/running/succeeded/failed/cancelled

轮询/Socket：
  本任务无

模型调用：
  无

删除影响：
  无
```

## 6. VT Studio 怎么落

```txt
能力名：
  task.list

调用链：
  renderer 任务中心 -> window.vtStudio.task.list -> main/ipc -> main/services/task

需要新增：
  任务中心页面主表格
  task.list IPC/service
  TaskListItem DTO

需要修改：
  如果 CORE-006 任务表字段不足，补齐列表需要的安全别名输出
```

## 7. 偏差

```txt
和 Toonflow 不同的地方：
  1. 内部状态用英文枚举，UI 显示中文。
  2. 查询字段使用明确别名，不照搬 `o_project.*`。

原因：
  避免字段覆盖，避免后续状态字符串混乱。

是否写入 04：
  状态枚举偏差如 04 已有统一记录则不用重复；若新增字段名偏差，补到 04。
```

## 8. 验收

```txt
1. 左侧菜单能进入任务中心。
2. 页面打开后能加载任务列表。
3. 表格字段不缺：分类、关联对象、模型、描述、状态、失败原因、开始时间。
4. 无任务时有空状态。
5. 查询失败有错误提示。
6. 失败任务能看到 reason。
7. 页面不直接访问 SQLite。
8. 任务中心不调用模型。
```

## 9. 用户确认点

| 编号 | 确认点 | 专业建议 |
|---|---|---|
| C-F-009-001-001 | 任务列表是否显示项目名列 | 建议显示，方便“全部项目”视角定位任务 |
| C-F-009-001-002 | 关联对象是否需要详情弹窗 | 建议本任务先摘要展示，详情后续单独做 |

## 10. 执行后记录

```txt
改了哪些文件：由 P4 批次统一完成，见 `docs/tasks/P4-任务中心批.md`
验证结果：verify-p4-task-center、typecheck、build 通过
未完成事项：任务详情弹窗不在本批次
最终结论：任务列表已完成，页面通过 preload 调 main service 查询，不直接访问 SQLite
```

## 11. 最后大白话

```txt
我这次准备怎么做：
1. 做任务中心最基础的任务表格。
2. 从统一任务表读任务。
3. 把状态、失败原因、时间这些关键字段显示出来。

我不会做什么：
1. 不做重试。
2. 不做取消。
3. 不在这里调用模型。

确认规则：
用户确认后才执行；未确认前只改文档。
```
