# F-009-006 刷新任务

状态：已完成（由 P4 任务中心批覆盖）  
所属菜单：M-009 任务中心  
对应功能文档：`docs/features/M-009-任务中心.md`  
原则：先确认本文档，再改代码

## 0. 快速理解

```txt
一句话：任务中心加刷新按钮，手动重新拉取当前筛选下的任务。
为什么现在做：参考项目第一版没有自动轮询，手动刷新最稳。
做完后有什么用：用户能随时更新任务状态，并且不会丢失当前筛选和分页。
这一步不碰什么：不做自动轮询，不重复执行任务，不改任务状态。
```

## 1. 本次做什么

```txt
目标：
  对齐 Toonflow 右上角刷新按钮。

只做：
  1. 刷新按钮
  2. 保留当前项目/分类/状态筛选
  3. 保留当前分页
  4. 刷新 loading 和错误提示

不做：
  1. 自动轮询
  2. WebSocket 推送
  3. 重试失败任务
  4. 重置筛选
```

## 2. 参考项目怎么做

| 参考文件 | 关键逻辑 |
|---|---|
| `Toonflow-web/src/views/task/index.vue` | 右上角刷新按钮直接调用 `getTaskList` |
| `Toonflow-app/src/routes/task/getTaskApi.ts` | 按当前参数重新查任务 |

参考项目关键事实：

```txt
1. 刷新按钮不重置 page。
2. 刷新保留 projectId、taskClass、taskState、page、limit。
3. 参考项目没看到任务中心自动轮询。
```

## 3. 用户操作

```txt
入口：
  任务中心右上角刷新按钮。

按钮/操作：
  点击刷新。

弹窗/表单：
  无。

成功反馈：
  表格数据更新。

失败反馈：
  显示刷新失败提示，保留旧列表。
```

## 4. 要做什么功能

### 1. 手动刷新列表

怎么做：
- 输入：当前筛选和分页状态。
- 输出：最新任务列表。
- 写什么数据：不写。
- 状态怎么变：refreshing=true -> false。
- 异常怎么处理：失败提示，不清空旧数据。
- 限制：刷新不会执行任务。

### 2. 保留筛选和分页

怎么做：
- 输入：projectId、category、status、page、pageSize。
- 输出：相同条件下的新数据。
- 写什么数据：不写。
- 状态怎么变：筛选状态不变。
- 异常怎么处理：如果当前页超出 total，可回退最后一页并重新查。
- 限制：不能像筛选变化一样强制回到第一页。

## 5. 数据和状态

```txt
字段：
  无新增字段

接口/能力：
  task.list

数据读写：
  读 tasks

任务状态：
  只读取，不修改

轮询/Socket：
  本任务不做自动轮询

模型调用：
  无

删除影响：
  无
```

## 6. VT Studio 怎么落

```txt
能力名：
  task.list

调用链：
  renderer 刷新按钮 -> window.vtStudio.task.list -> main/services/task

需要新增：
  刷新按钮和 loading 状态

需要修改：
  无
```

## 7. 偏差

```txt
和 Toonflow 不同的地方：
  刷新失败时保留旧数据并明确提示。

原因：
  防止一次刷新失败把页面清空。

是否写入 04：
  不需要，属于体验增强，不改变业务语义。
```

## 8. 验收

```txt
1. 点击刷新会重新请求任务列表。
2. 当前项目筛选保留。
3. 当前分类筛选保留。
4. 当前状态筛选保留。
5. 当前分页保留。
6. 刷新失败不清空旧列表。
7. 不会重复执行任何任务。
```

## 9. 用户确认点

| 编号 | 确认点 | 专业建议 |
|---|---|---|
| C-F-009-006-001 | 是否第一版加入自动刷新 | 建议不加，先保持手动刷新；任务类页面各自负责轮询 |

## 10. 执行后记录

```txt
改了哪些文件：由 P4 批次统一完成，见 `docs/tasks/P4-任务中心批.md`
验证结果：verify-p4-task-center、typecheck、build 通过
未完成事项：自动轮询不在本批次
最终结论：手动刷新已完成；刷新保留当前筛选和分页，失败时不清空旧数据
```

## 11. 最后大白话

```txt
我这次准备怎么做：
1. 加一个刷新按钮。
2. 点了以后按当前条件重新查任务。
3. 不改变筛选、不改变分页、不执行任务。

我不会做什么：
1. 不做自动轮询。
2. 不做任务重试。

确认规则：
用户确认后才执行；未确认前只改文档。
```
