# F-009-007 分页查看

状态：已完成（由 P4 任务中心批覆盖）  
所属菜单：M-009 任务中心  
对应功能文档：`docs/features/M-009-任务中心.md`  
原则：先确认本文档，再改代码

## 0. 快速理解

```txt
一句话：任务中心列表加分页，支持切页和切每页数量。
为什么现在做：任务数据会越来越多，不分页会卡，也不方便定位。
做完后有什么用：用户能按页查看任务，不会一次加载全部历史任务。
这一步不碰什么：不做无限滚动，不做任务详情，不做导出任务列表。
```

## 1. 本次做什么

```txt
目标：
  对齐 Toonflow `page/limit/total` 的任务分页。

只做：
  1. 分页器
  2. page/pageSize 参数
  3. total 展示
  4. 切页和切 pageSize 后刷新列表

不做：
  1. 虚拟滚动
  2. 导出 CSV
  3. 任务详情弹窗
```

## 2. 参考项目怎么做

| 参考文件 | 关键逻辑 |
|---|---|
| `Toonflow-web/src/views/task/index.vue` | `pagination.page`、`pagination.limit` 通过分页器 v-model 控制 |
| `Toonflow-app/src/routes/task/getTaskApi.ts` | `offset=(page-1)*limit`，另查 count total |

参考项目关键事实：

```txt
1. 默认 page=1，limit=10。
2. 切筛选会把 page 重置为 1。
3. 刷新不会重置 page。
4. 后端返回 data 和 total。
```

## 3. 用户操作

```txt
入口：
  任务中心表格底部分页器。

按钮/操作：
  1. 切换页码。
  2. 切换每页数量。

弹窗/表单：
  无。

成功反馈：
  表格显示对应页数据。

失败反馈：
  查询失败时提示并保留旧数据。
```

## 4. 要做什么功能

### 1. 页码切换

怎么做：
- 输入：page。
- 输出：对应页任务。
- 写什么数据：不写。
- 状态怎么变：page 更新，loading=true -> false。
- 异常怎么处理：失败时回退到原 page 或保留旧数据。
- 限制：page 必须大于等于 1。

### 2. 每页数量切换

怎么做：
- 输入：pageSize。
- 输出：按新数量查询的任务。
- 写什么数据：不写。
- 状态怎么变：pageSize 更新，通常 page=1。
- 异常怎么处理：非法 pageSize 返回参数错误。
- 限制：pageSize 只能来自允许选项，例如 10/20/50/100。

### 3. total 展示

怎么做：
- 输入：total。
- 输出：分页器总数。
- 写什么数据：不写。
- 状态怎么变：查询成功后更新 total。
- 异常怎么处理：total 缺失时按 0 处理并记录服务异常。
- 限制：total 必须由服务层 count 查询返回。

## 5. 数据和状态

```txt
字段：
  page
  pageSize
  total

接口/能力：
  task.list

数据读写：
  读 tasks
  count tasks

任务状态：
  不改变

轮询/Socket：
  无

模型调用：
  无

删除影响：
  无
```

## 6. VT Studio 怎么落

```txt
能力名：
  task.list

调用链：
  renderer 分页器 -> window.vtStudio.task.list -> main/services/task

需要新增：
  分页器组件状态
  page/pageSize 参数校验

需要修改：
  task.list 返回 `{ items, total }`
```

## 7. 偏差

```txt
和 Toonflow 不同的地方：
  参数命名用 pageSize，不用 limit。

原因：
  前端和服务层语义更清楚。

是否写入 04：
  不需要，属于命名规范。
```

## 8. 验收

```txt
1. 默认显示第 1 页。
2. 默认每页 10 条。
3. 切页能刷新数据。
4. 切 pageSize 能刷新数据。
5. total 正确。
6. 筛选变化会回到第 1 页。
7. 手动刷新不会重置页码。
8. 页面不直接访问 SQLite。
```

## 9. 用户确认点

| 编号 | 确认点 | 专业建议 |
|---|---|---|
| C-F-009-007-001 | pageSize 选项是否固定 10/20/50/100 | 建议固定，避免一次查太多任务 |

## 10. 执行后记录

```txt
改了哪些文件：由 P4 批次统一完成，见 `docs/tasks/P4-任务中心批.md`
验证结果：verify-p4-task-center、typecheck、build 通过
未完成事项：无限滚动和导出列表不在本批次
最终结论：分页查看已完成；默认每页 10 条，支持 10/20/50/100
```

## 11. 最后大白话

```txt
我这次准备怎么做：
1. 给任务表格加分页。
2. 后端按页查询，不一次加载全部任务。
3. 筛选变化回第一页，刷新保持当前页。

我不会做什么：
1. 不做无限滚动。
2. 不导出任务列表。

确认规则：
用户确认后才执行；未确认前只改文档。
```
