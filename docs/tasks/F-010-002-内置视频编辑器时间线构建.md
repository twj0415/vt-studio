# F-010-002 内置视频编辑器时间线构建

状态：后置增强（P10 第一版不作为正式导出来源）  
所属菜单：M-010 导出  
对应功能文档：`docs/features/M-010-导出.md`  
原则：先确认本文档，再改代码

## 0. 快速理解

```txt
一句话：构建内置视频编辑器的前端时间线，支持把素材拖到轨道并形成 clip。
为什么现在做：参考项目有 WebAV 编辑器，但它没有后端保存闭环，必须先把边界写清。
做完后有什么用：用户能在内置编辑器里预览和调整素材时间线。
这一步不碰什么：不保存正式剪映导出时间线，不生成剪映草稿，不合成 mp4。
```

## 1. 本次做什么

```txt
目标：
  对齐 Toonflow editVideo 内置视频编辑器的时间线构建能力。

只做：
  1. 素材库数据接入
  2. 拖拽素材到轨道
  3. 自动找可用轨道或创建轨道
  4. 生成 clip
  5. 加载视频缩略图、音频波形、图片缩略图
  6. 切割、删除、撤销、重做
  7. 转场插入规则

不做：
  1. WebAV mp4 导出
  2. 剪映草稿导出
  3. 后端时间线保存
```

## 2. 参考项目怎么做

| 参考文件 | 关键逻辑 |
|---|---|
| `Toonflow-web/src/views/production/components/workbench/editVideo/index.vue` | 编辑器主体、工具栏、drop-media、历史快照 |
| `.../mediaLibrary.vue` | 素材库和拖拽数据 |
| `.../videoPreview.vue` | 预览画布 |
| `.../propertyPanel.vue` | 属性面板 |
| `.../utils/trackHelper.ts` | 轨道查找、clip 创建 |
| `.../utils/transitionHelper.ts` | 转场插入规则 |
| `.../utils/mediaLoader.ts` | 缩略图和波形加载 |

参考项目关键事实：

```txt
1. vue-clip-track 使用前端状态管理轨道和 clips。
2. 轨道数量限制：video 5、image 3、audio 3、subtitle 2、text 2、sticker 2、filter 1、effect 2。
3. 默认时长：video/image 5s、audio 30s、subtitle/text/sticker/filter/effect 3s。
4. 转场必须放在同一轨道相邻两个非 transition clip 之间。
5. operationButtons 有 import，但模板注释掉了入口。
6. 源码未找到时间线保存到后端的接口。
```

## 3. 用户操作

```txt
入口：
  视频工作台 -> 视频编辑器模式。

按钮/操作：
  1. 从素材库拖拽素材到轨道。
  2. 添加转场。
  3. 切割选中 clip。
  4. 删除选中 clip。
  5. 撤销、重做。
  6. 调整属性面板。

弹窗/表单：
  属性面板；转场提示。

成功反馈：
  时间线出现 clip，预览能播放。

失败反馈：
  无可用轨道、转场位置不合法、素材加载失败。
```

## 4. 要做什么功能

### 1. 构建素材库

怎么做：
- 输入：initialVideoItems、initialMediaItems、initialImageItems、initialAudioItems。
- 输出：素材库 tabs。
- 写什么数据：不写。
- 状态怎么变：素材加载中 -> 可拖拽。
- 异常怎么处理：缩略图/波形失败时用默认图。
- 限制：素材 URL 必须受控。

### 2. 拖拽生成 clip

怎么做：
- 输入：media item、drop position、trackId 可选。
- 输出：timeline clip。
- 写什么数据：前端时间线状态。
- 状态怎么变：tracks/clips 更新并写历史快照。
- 异常怎么处理：找不到轨道且无法创建时提示。
- 限制：轨道数量不能超过限制。

### 3. 转场插入

怎么做：
- 输入：transition item、drop position。
- 输出：transition clip。
- 写什么数据：前端时间线状态。
- 状态怎么变：相邻 clip 之间插入转场。
- 异常怎么处理：没有相邻 clip 或已有转场时 warning。
- 限制：转场默认 1 秒，类型必须在支持列表内。

### 4. 时间线编辑工具

怎么做：
- 输入：选中 clips 和当前时间。
- 输出：切割、删除、撤销、重做结果。
- 写什么数据：前端时间线状态。
- 状态怎么变：history 更新。
- 异常怎么处理：无选中 clip 时按钮禁用。
- 限制：本任务不保存到后端。

## 5. 数据和状态

```txt
字段：
  tracksStore.tracks
  clips.id/type/sourceUrl/startTime/endTime/duration/trackId
  historyStore

接口/能力：
  本任务主要是 renderer 时间线状态

数据读写：
  读素材 URL
  前端时间线状态

任务状态：
  不写 tasks

轮询/Socket：
  无

模型调用：
  无

删除影响：
  删除 clip 不删除原始素材
```

## 6. VT Studio 怎么落

```txt
能力名：
  export.videoEditor.timeline

调用链：
  renderer 内置视频编辑器状态管理

需要新增：
  视频编辑器布局
  素材库
  时间线 helper
  history undo/redo

需要修改：
  素材 URL 走 media service
```

## 7. 偏差

```txt
和 Toonflow 不同的地方：
  明确第一版时间线是前端编辑状态，不作为剪映导出的唯一数据源。

原因：
  参考项目没有后端保存闭环，不能把临时 Vue 状态当正式导出依据。

是否写入 04：
  写入。
```

## 8. 验收

```txt
1. 素材库能显示视频、图片、音频、文本、转场、特效、滤镜。
2. 素材能拖进轨道。
3. 轨道数量限制生效。
4. clip 能切割和删除。
5. 撤销和重做可用。
6. 转场只能放在合法位置。
7. 时间线不作为第一版剪映导出唯一来源。
8. 页面不直接访问本地文件路径。
```

## 9. 用户确认点

| 编号 | 确认点 | 专业建议 |
|---|---|---|
| C-F-010-002-001 | 内置编辑器时间线是否保存到后端 | 建议后续单独设计 timeline 表，第一版不作为剪映导出依据 |

## 10. 执行后记录

```txt
改了哪些文件：
  - docs/tasks/P10-导出批次.md
  - src/shared/types/export.ts
  - src/main/services/export/index.ts
  - src/renderer/src/features/production/components/ProductionWorkbenchDialog.vue

验证结果：
  - D:\software\nodejs\node.exe scripts\verify-p10-export.mjs 通过
  - D:\software\nodejs\pnpm.cmd run typecheck 通过
  - D:\software\nodejs\pnpm.cmd run build 通过

未完成事项：
  - 未实现完整内置视频编辑器时间线、素材拖拽、切割、撤销、重做、转场和持久化。
  - 当前项目未接入 vue-clip-track 或等价时间线编辑依赖。

最终结论：
  - P10 第一版明确不依赖临时编辑器状态导出，避免把未持久化的 Vue 时间线当成正式剪映草稿来源。
  - 后续如要做内置编辑器，应单独设计 timeline 表、编辑器状态持久化和导出来源切换。
```

## 11. 最后大白话

```txt
我这次准备怎么做：
1. 做内置视频编辑器的前端时间线。
2. 素材能拖到轨道，能切割、删除、撤销、加转场。
3. 先不把它当正式剪映导出数据源。

我不会做什么：
1. 不合成 mp4。
2. 不导出剪映。
3. 不保存后端时间线。

确认规则：
用户确认后才执行；未确认前只改文档。
```
