# F-010-005 导出素材校验

状态：已完成（由 P10 第一版覆盖）  
所属菜单：M-010 导出  
对应功能文档：`docs/features/M-010-导出.md`  
原则：先确认本文档，再改代码

## 0. 快速理解

```txt
一句话：剪映导出前必须检查所有图片、视频、音频文件是否存在且可用。
为什么现在做：不能边写草稿边失败，缺素材必须提前告诉用户。
做完后有什么用：导出失败能精确定位到轨道、clip、来源对象和路径。
这一步不碰什么：不写草稿文件，不修复素材，不生成缺失素材。
```

## 1. 本次做什么

```txt
目标：
  对剪映导出 timeline 做素材校验，阻止半成品草稿输出。

只做：
  1. 校验 timeline clips
  2. 校验文件路径非空
  3. 校验文件存在
  4. 校验格式支持
  5. 校验读取权限
  6. 输出结构化失败列表

不做：
  1. 写剪映草稿
  2. 复制素材
  3. 自动补生成素材
  4. 修复数据库路径
```

## 2. 参考项目怎么做

| 参考文件 | 关键逻辑 |
|---|---|
| `docs/features/M-010-导出.md` | 明确剪映导出素材校验必须列出轨道、clip、素材类型、来源、路径、失败原因 |
| `Toonflow-app/src/routes/production/workbench/getFileUrl.ts` | 参考项目有补真实 URL 的逻辑，但不是剪映校验 |
| `Toonflow-web/src/views/production/components/workbench/editVideo/utils/mediaLoader.ts` | 前端加载缩略图/波形，失败有默认值 |

参考项目关键事实：

```txt
1. Toonflow 没有剪映草稿导出校验实现。
2. WebAV 和快速预览更多是在前端加载失败时处理，不适合剪映草稿。
3. VT Studio 必须在 main 服务里校验本地文件路径。
4. 校验失败要阻止导出，不写半成品草稿。
```

## 3. 用户操作

```txt
入口：
  点击剪映草稿导出后自动执行校验。

按钮/操作：
  1. 点击导出。
  2. 如果校验失败，查看失败列表。

弹窗/表单：
  校验失败详情弹窗。

成功反馈：
  校验通过，进入草稿导出步骤。

失败反馈：
  显示缺失素材列表，并阻止导出。
```

## 4. 要做什么功能

### 1. 校验路径和文件存在

怎么做：
- 输入：timeline clips。
- 输出：valid 或 failure list。
- 写什么数据：不写。
- 状态怎么变：validating -> passed/failed。
- 异常怎么处理：路径为空、文件不存在、权限不足都生成校验错误。
- 限制：校验在 main 服务做。

### 2. 校验素材格式

怎么做：
- 输入：filePath、mediaType。
- 输出：格式是否支持。
- 写什么数据：不写。
- 状态怎么变：无。
- 异常怎么处理：格式不支持写 failure。
- 限制：第一版只校验剪映草稿支持的基础视频/音频/图片格式。

### 3. 输出失败列表

怎么做：
- 输入：校验错误。
- 输出：结构化 failures。
- 写什么数据：不写。
- 状态怎么变：无。
- 异常怎么处理：无。
- 限制：每条必须包含 trackId、clipId、mediaType、sourceType、sourceId、path、reason。

### 4. 阻止导出

怎么做：
- 输入：failures。
- 输出：阻止进入 F-010-006。
- 写什么数据：不写草稿。
- 状态怎么变：export blocked。
- 异常怎么处理：用户修复后可重新校验。
- 限制：不能写半成品草稿目录。

## 5. 数据和状态

```txt
字段：
  exportTimeline.clips
  clip.trackId
  clip.id
  clip.mediaType
  clip.sourceType
  clip.sourceId
  clip.filePath

接口/能力：
  export.validateAssets

数据读写：
  读文件系统
  不写数据库

任务状态：
  可作为导出任务 validating 阶段

轮询/Socket：
  无

模型调用：
  无

删除影响：
  无
```

## 6. VT Studio 怎么落

```txt
能力名：
  export.validateAssets

调用链：
  renderer 导出流程 -> window.vtStudio.export.validateAssets -> main/services/export

需要新增：
  素材校验 service
  failure DTO
  校验失败弹窗

需要修改：
  导出按钮流程：必须先校验再导出
```

## 7. 偏差

```txt
和 Toonflow 不同的地方：
  这是 VT Studio 新增剪映导出前置校验。

原因：
  Toonflow 没有剪映导出，VT Studio 需要保证专业输出。

是否写入 04：
  写入。
```

## 8. 验收

```txt
1. 路径为空能拦截。
2. 文件不存在能拦截。
3. 权限不足能拦截。
4. 格式不支持能拦截。
5. 失败列表包含轨道、clip、来源和路径。
6. 校验失败不会写草稿。
7. 校验通过才进入 F-010-006。
8. renderer 不直接访问文件系统。
```

## 9. 用户确认点

| 编号 | 确认点 | 专业建议 |
|---|---|---|
| C-F-010-005-001 | 校验失败是否允许强制导出 | 建议不允许，避免生成不可用草稿 |

## 10. 执行后记录

```txt
改了哪些文件：
  - src/shared/types/export.ts
  - src/main/services/export/index.ts
  - src/main/ipc/export.ts
  - src/preload/index.ts
  - src/shared/contracts/preload.ts
  - src/renderer/src/features/production/components/ProductionWorkbenchDialog.vue
  - src/renderer/src/i18n/messages.ts
  - src/renderer/src/styles/index.scss
  - scripts/verify-p10-export.mjs

验证结果：
  - D:\software\nodejs\node.exe scripts\verify-p10-export.mjs 通过
  - D:\software\nodejs\pnpm.cmd run typecheck 通过
  - D:\software\nodejs\pnpm.cmd run build 通过

未完成事项：
  - 第一版校验覆盖当前导出 timeline 的视频 clip 和分镜图片导出，不包含后置内置编辑器的图片、音频、字幕、转场素材。

最终结论：
  - export.validateAssets 已在 main 服务校验路径为空、文件缺失、非文件、权限、格式和读取失败。
  - 校验失败会返回 trackId、clipId、mediaType、sourceType、sourceId、path、reason、message，并阻止草稿写入。
```

## 11. 最后大白话

```txt
我这次准备怎么做：
1. 剪映导出前先检查所有素材。
2. 哪个轨道、哪个 clip、哪个文件有问题都列清楚。
3. 有问题就不写草稿。

我不会做什么：
1. 不自动修素材。
2. 不生成缺失视频。
3. 不写半成品草稿。

确认规则：
用户确认后才执行；未确认前只改文档。
```
