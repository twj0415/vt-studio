# F-011-001 剧本 Agent 对话入口

状态：已完成（由 P6 阶段 1 覆盖）  
所属菜单：M-011 剧本 Agent  
对应功能文档：`docs/features/M-011-剧本Agent.md`  
原则：先确认本文档，再改代码

## 0. 快速理解

```txt
一句话：做剧本 Agent 左侧聊天入口，能发送、流式接收、停止生成和显示连接状态。
为什么现在做：剧本 Agent 是从原文到剧本的核心生产入口。
做完后有什么用：用户可以和 Agent 对话，让它生成故事骨架、改编策略和剧本。
这一步不碰什么：不实现全部子 Agent，不写生产工作台，不直接让页面读数据库或 skill 文件。
```

## 1. 本次做什么

```txt
目标：
  对齐 Toonflow scriptAgent 页面左侧聊天入口和 Socket 流式消息语义。

只做：
  1. 剧本 Agent 页面入口
  2. 欢迎消息和建议 prompt
  3. 建立会话连接
  4. 发送用户消息
  5. 接收 assistant 流式消息
  6. 停止当前生成
  7. 显示连接状态和错误状态

不做：
  1. 子 Agent 调度完整实现
  2. XML 写入工作区
  3. 计划数据保存
  4. 生产 Agent
```

## 2. 参考项目怎么做

| 参考文件 | 关键逻辑 |
|---|---|
| `Toonflow-web/src/views/scriptAgent/index.vue` | 左右分栏，左侧聊天，进入页面初始化欢迎消息和连接 |
| `Toonflow-web/src/stores/scriptAgent.ts` | 维护连接、消息、发送、停止、清记忆、思考配置 |
| `Toonflow-web/src/utils/useChat.ts` | 处理流式消息、thinking、toolcall、XML、suggestion |
| `Toonflow-app/src/socket/routes/scriptAgent.ts` | Socket chat/stop/updateThinkConfig，运行 runDecisionAI |
| `Toonflow-app/src/socket/resTool.ts` | 封装 assistant 消息、文本、错误、完成 |

参考项目关键事实：

```txt
1. 页面左侧是 Agent 对话区，右侧是计划数据 tabs。
2. 进入页面如果 messages 为空，会插入欢迎消息和“开始”建议。
3. 参考项目用 Socket.IO；VT Studio 已在 CORE-008 确认继续使用 Socket.IO，不能再改成 IPC event。
4. stop 前端发送 messageId，后端实际 abort 当前 controller。
5. 源码未找到页面离开时主动 disconnect，VT Studio 必须释放监听。
```

## 3. 用户操作

```txt
入口：
  项目内顶部菜单 -> 剧本 Agent。

按钮/操作：
  1. 输入消息。
  2. 点击发送。
  3. 点击停止。
  4. 点击建议 prompt。
  5. 查看连接状态。

弹窗/表单：
  无。

成功反馈：
  assistant 流式回复，消息状态变 complete。

失败反馈：
  连接失败、模型失败、Agent 失败时显示 error 消息。
```

## 4. 要做什么功能

### 1. 初始化聊天会话

怎么做：
- 输入：projectId。
- 输出：会话连接和欢迎消息。
- 写什么数据：不写业务数据。
- 状态怎么变：disconnected -> connecting -> connected。
- 异常怎么处理：缺 projectId 或连接失败显示错误状态。
- 限制：隔离 key 必须是 `projectId:scriptAgent`。

### 2. 发送用户消息

怎么做：
- 输入：用户文本。
- 输出：user 消息和 assistant 流式消息。
- 写什么数据：后端可写 Agent 记忆。
- 状态怎么变：idle -> pending -> streaming -> complete/error。
- 异常怎么处理：空文本不发送；失败时 assistant 消息标 error。
- 限制：页面只调用 Agent 会话 API，不直接调用模型。

### 3. 接收流式消息

怎么做：
- 输入：Agent 事件。
- 输出：聊天消息内容。
- 写什么数据：XML 完成写入见后续任务，不在本任务完成。
- 状态怎么变：streaming 中持续追加内容。
- 异常怎么处理：事件格式非法时标记消息 error。
- 限制：必须支持 thinking、markdown、toolcall、error、stop 语义。

必须兼容的事件：

```txt
message：兼容旧的整条消息推送
message:update：更新整条消息状态
content:add：新增 thinking/markdown/toolcall/suggestion 内容块
content:update：更新内容块状态
error：服务端错误
connect/disconnect/connect_error：连接状态
```

### 4. 停止生成

怎么做：
- 输入：当前 messageId。
- 输出：停止事件。
- 写什么数据：可写 stopped 状态和记忆。
- 状态怎么变：streaming -> stop。
- 异常怎么处理：没有运行中消息时按钮禁用。
- 限制：停止当前生成，不清空历史消息。

### 5. 离开页面释放连接

怎么做：
- 输入：页面卸载。
- 输出：关闭会话监听。
- 写什么数据：不写。
- 状态怎么变：connected -> disconnected。
- 异常怎么处理：释放失败写日志。
- 限制：不能残留重复事件监听。

## 5. 数据和状态

```txt
字段：
  projectId
  isolationKey
  message.id
  message.role
  message.content
  message.status

接口/能力：
  agent.script.connect
  agent.script.chat
  agent.script.stop
  agent.script.disconnect

数据读写：
  本任务不直接读写业务表
  记忆写入由 Agent 服务处理

任务状态：
  建议 Agent 长任务写入 tasks；是否启用由 F-011-014 和 F-009-008 决定

轮询/Socket：
  使用 CORE-008 Socket.IO

模型调用：
  main/services/agent 调 Model Adapter

删除影响：
  无
```

## 6. VT Studio 怎么落

```txt
能力名：
  agent.script

调用链：
  renderer 剧本 Agent 页 -> window.vtStudio.agent.script -> main/services/agent/script

需要新增：
  剧本 Agent 页面
  左侧聊天组件
  Agent 会话事件订阅
  停止生成能力

需要修改：
  preload 暴露 agent.script API
```

## 7. 偏差

```txt
和 Toonflow 不同的地方：
  无。底层已定继续用 Socket.IO。

原因：
  参考项目 useChat 和 resTool 的事件粒度已经清楚，继续 Socket.IO 更容易对齐。

是否写入 04：
  不需要。
```

## 8. 验收

```txt
1. 能进入剧本 Agent 页面。
2. 首次进入有欢迎消息。
3. 能发送消息。
4. 能看到 assistant 流式回复。
5. 能停止当前生成。
6. 连接状态清楚。
7. 错误以消息形式显示。
8. 离开页面不会残留重复监听。
9. 页面不直接访问 SQLite、skill 文件或模型 SDK。
```

## 9. 用户确认点

| 编号 | 确认点 | 专业建议 |
|---|---|---|
| C-F-011-001-001 | VT Studio 是否用 IPC event 替代 Socket.IO | 不替代；已定使用 CORE-008 Socket.IO |
| C-F-011-001-002 | Agent 对话是否写任务中心 | 建议长耗时生成写任务中心 |

## 10. 执行后记录

```txt
改了哪些文件：由 P6 阶段 1 统一完成，详见 docs/tasks/P6-剧本Agent批.md
验证结果：verify-p6-script-agent-phase1 通过；typecheck 通过；build 通过
未完成事项：决策层、子 Agent、XML 写入和工作区持久化归 P6 后续阶段
最终结论：已完成剧本 Agent 左侧对话入口、Socket 连接、发送、停止、重连、连接状态和错误展示
```

## 11. 最后大白话

```txt
我这次准备怎么做：
1. 做剧本 Agent 的聊天入口。
2. 用户能发消息，Agent 能流式回复。
3. 用户能停止当前生成，页面能显示连接和错误。

我不会做什么：
1. 不在页面里直接读数据库。
2. 不在页面里直接跑模型。
3. 不一次性做完整子 Agent。

确认规则：
用户确认后才执行；未确认前只改文档。
```
