# F-011-002 读取历史记忆

状态：已完成（由 P6 阶段 1 覆盖）  
所属菜单：M-011 剧本 Agent  
对应功能文档：`docs/features/M-011-剧本Agent.md`  
原则：先确认本文档，再改代码

## 0. 快速理解

```txt
一句话：进入剧本 Agent 时读取这个项目的历史对话记忆。
为什么现在做：Agent 不能每次打开都忘记之前聊过什么。
做完后有什么用：用户能继续上次的剧本规划上下文。
这一步不碰什么：不清记忆，不做向量检索实现，不生成剧本。
```

## 1. 本次做什么

```txt
目标：
  对齐 Toonflow `/agents/getMemory`，按 `projectId:scriptAgent` 隔离读取 message 类型记忆。

只做：
  1. 进入页面后读取历史 message 记忆
  2. 转成聊天消息格式
  3. 和欢迎消息合并展示
  4. 读取失败兜底

不做：
  1. 清理记忆
  2. 记忆摘要生成
  3. RAG 检索
```

## 2. 参考项目怎么做

| 参考文件 | 关键逻辑 |
|---|---|
| `Toonflow-web/src/views/scriptAgent/index.vue` | 如果只有欢迎消息，调用 `/agents/getMemory` 读取历史 |
| `Toonflow-app/src/routes/agents/getMemory.ts` | 按 isolationKey/type 读取 memories |
| `Toonflow-app/src/agents/scriptAgent/index.ts` | Agent 运行时写入 memory |

参考项目关键事实：

```txt
1. isolationKey 是 `${projectId}:scriptAgent`。
2. 读取 message 类型历史消息。
3. Memory 里还有 summary 类型，但聊天列表主要展示 message。
4. 读取失败没有完整页面兜底，VT Studio 要补。
```

## 3. 用户操作

```txt
入口：
  打开剧本 Agent 页面自动读取。

按钮/操作：
  无。

弹窗/表单：
  无。

成功反馈：
  历史消息显示在聊天列表。

失败反馈：
  读取失败显示轻量提示，不阻止新对话。
```

## 4. 要做什么功能

### 1. 读取历史消息

怎么做：
- 输入：projectId、type=message。
- 输出：历史消息列表。
- 写什么数据：不写。
- 状态怎么变：memoryLoading=true -> false。
- 异常怎么处理：读取失败提示，但保留欢迎消息。
- 限制：只读当前项目的 scriptAgent 记忆。

### 2. 转换消息格式

怎么做：
- 输入：memory role/name/content。
- 输出：聊天 message。
- 写什么数据：不写。
- 状态怎么变：messages 追加历史记录。
- 异常怎么处理：空 content 的记忆跳过。
- 限制：不能把 summary 直接当普通聊天逐条显示。

### 3. 避免重复加载

怎么做：
- 输入：当前 messages。
- 输出：只在需要时读取。
- 写什么数据：不写。
- 状态怎么变：避免重复 append。
- 异常怎么处理：重复消息按 id 去重。
- 限制：刷新页面可以重新读取，页面内重连不重复塞消息。

## 5. 数据和状态

```txt
字段：
  memories.id
  memories.isolation_key
  memories.type
  memories.role
  memories.name
  memories.content
  memories.created_at

接口/能力：
  agent.script.getMemory

数据读写：
  读 memories

任务状态：
  不写 tasks

轮询/Socket：
  无

模型调用：
  无

删除影响：
  无
```

## 6. VT Studio 怎么落

```txt
能力名：
  agent.script.getMemory

调用链：
  renderer 剧本 Agent 页 -> window.vtStudio.agent.script.getMemory -> main/services/memory

需要新增：
  历史记忆读取方法
  消息格式转换

需要修改：
  剧本 Agent 页面初始化流程
```

## 7. 偏差

```txt
和 Toonflow 不同的地方：
  读取失败不阻塞新对话，且消息按 id 去重。

原因：
  记忆损坏不应该导致 Agent 页面不可用。

是否写入 04：
  不需要，属于容错增强。
```

## 8. 验收

```txt
1. 打开页面能读取当前项目历史消息。
2. 只读取 scriptAgent 隔离 key。
3. 不读取其他项目记忆。
4. 不把 summary 当普通聊天塞进去。
5. 读取失败仍可发送新消息。
6. 不重复追加相同历史消息。
```

## 9. 用户确认点

| 编号 | 确认点 | 专业建议 |
|---|---|---|
| C-F-011-002-001 | 历史消息是否默认全部展示 | 建议分页或限制最近 N 条，完整记忆留给后续详情 |

## 10. 执行后记录

```txt
改了哪些文件：由 P6 阶段 1 统一完成，详见 docs/tasks/P6-剧本Agent批.md
验证结果：verify-p6-script-agent-phase1 通过；typecheck 通过；build 通过
未完成事项：RAG 深度检索和摘要细节展示不在阶段 1 页面展开
最终结论：已完成按 projectId:scriptAgent 读取历史 message 记忆并展示到聊天列表
```

## 11. 最后大白话

```txt
我这次准备怎么做：
1. 打开剧本 Agent 时读取历史聊天。
2. 只读当前项目的剧本 Agent 记忆。
3. 读取失败也不影响继续聊天。

我不会做什么：
1. 不清理记忆。
2. 不做记忆摘要生成。

确认规则：
用户确认后才执行；未确认前只改文档。
```
