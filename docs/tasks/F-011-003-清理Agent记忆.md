# F-011-003 清理 Agent 记忆

状态：已完成（由 P6 阶段 1 覆盖）  
所属菜单：M-011 剧本 Agent  
对应功能文档：`docs/features/M-011-剧本Agent.md`  
原则：先确认本文档，再改代码

## 0. 快速理解

```txt
一句话：剧本 Agent 设置菜单里支持清理 message、summary 或全部记忆。
为什么现在做：记忆错了会持续影响 Agent 判断，必须能清。
做完后有什么用：用户能重置当前项目剧本 Agent 的历史上下文。
这一步不碰什么：不删除剧本数据，不删除项目，不清生产 Agent 记忆。
```

## 1. 本次做什么

```txt
目标：
  对齐 Toonflow `/agents/clearMemory` 清理能力。

只做：
  1. 清 message 记忆
  2. 清 summary 记忆
  3. 清全部记忆
  4. 二次确认
  5. 清理后刷新历史消息

不做：
  1. 清其他 Agent 记忆
  2. 删除工作区计划数据
  3. 删除剧本表
```

## 2. 参考项目怎么做

| 参考文件 | 关键逻辑 |
|---|---|
| `Toonflow-web/src/views/scriptAgent/index.vue` | 设置菜单里清 message、summary、all，弹确认 |
| `Toonflow-web/src/stores/scriptAgent.ts` | 调 `/agents/clearMemory`，成功后刷新历史 |
| `Toonflow-app/src/routes/agents/clearMemory.ts` | 按 isolationKey 和 type 删除 memories |

参考项目关键事实：

```txt
1. 清理类型是 message、summary、all。
2. 清理前有确认框。
3. 成功后 toast，并刷新历史。
4. handleClearMemory 类型里有 reconnect，但实际不是记忆类型。
```

## 3. 用户操作

```txt
入口：
  剧本 Agent 聊天区设置菜单。

按钮/操作：
  1. 清理消息记忆。
  2. 清理摘要记忆。
  3. 清理全部记忆。
  4. 确认。

弹窗/表单：
  二次确认框。

成功反馈：
  清理成功提示，聊天历史刷新。

失败反馈：
  清理失败提示。
```

## 4. 要做什么功能

### 1. 清 message 记忆

怎么做：
- 输入：projectId、type=message。
- 输出：删除结果。
- 写什么数据：删除当前 isolationKey 下 message 记忆。
- 状态怎么变：确认 -> 清理中 -> 刷新历史。
- 异常怎么处理：删除失败提示。
- 限制：不删 summary。

### 2. 清 summary 记忆

怎么做：
- 输入：projectId、type=summary。
- 输出：删除结果。
- 写什么数据：删除当前 isolationKey 下 summary 记忆。
- 状态怎么变：确认 -> 清理中 -> 刷新历史。
- 异常怎么处理：删除失败提示。
- 限制：不删 message。

### 3. 清全部记忆

怎么做：
- 输入：projectId、type=all。
- 输出：删除结果。
- 写什么数据：删除当前 isolationKey 下所有记忆。
- 状态怎么变：确认 -> 清理中 -> 只保留欢迎消息。
- 异常怎么处理：删除失败提示。
- 限制：只清 scriptAgent，不清 productionAgent。

## 5. 数据和状态

```txt
字段：
  memories.isolation_key
  memories.type

接口/能力：
  agent.script.clearMemory

数据读写：
  删除 memories

任务状态：
  不写 tasks

轮询/Socket：
  无

模型调用：
  无

删除影响：
  只影响 Agent 记忆，不影响剧本、计划数据和原文
```

## 6. VT Studio 怎么落

```txt
能力名：
  agent.script.clearMemory

调用链：
  renderer 设置菜单 -> window.vtStudio.agent.script.clearMemory -> main/services/memory

需要新增：
  设置菜单清理项
  二次确认

需要修改：
  清理后刷新历史消息
```

## 7. 偏差

```txt
和 Toonflow 不同的地方：
  不保留 reconnect 这种伪记忆类型。

原因：
  reconnect 是连接操作，不是记忆类型。

是否写入 04：
  不需要，属于类型修正。
```

## 8. 验收

```txt
1. 清 message 有确认。
2. 清 summary 有确认。
3. 清 all 有确认。
4. 清理只影响当前项目 scriptAgent。
5. 清理后历史消息刷新。
6. 清理失败有提示。
7. 不删除剧本或计划数据。
```

## 9. 用户确认点

| 编号 | 确认点 | 专业建议 |
|---|---|---|
| C-F-011-003-001 | 清全部记忆是否保留当前页面消息 | 建议清理后只保留欢迎消息，避免误以为旧记忆还在 |

## 10. 执行后记录

```txt
改了哪些文件：由 P6 阶段 1 统一完成，详见 docs/tasks/P6-剧本Agent批.md
验证结果：verify-p6-script-agent-phase1 通过；typecheck 通过；build 通过
未完成事项：无
最终结论：已完成 message、summary、all 三类记忆清理，清理前有确认，清理后刷新当前聊天状态
```

## 11. 最后大白话

```txt
我这次准备怎么做：
1. 加清理记忆菜单。
2. 用户可以清消息、清摘要、清全部。
3. 清理只作用于当前项目的剧本 Agent。

我不会做什么：
1. 不删剧本。
2. 不删项目。
3. 不清生产 Agent 记忆。

确认规则：
用户确认后才执行；未确认前只改文档。
```
