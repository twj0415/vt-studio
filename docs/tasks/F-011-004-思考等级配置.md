# F-011-004 思考等级配置

状态：已完成（由 P6 阶段 1 覆盖）  
所属菜单：M-011 剧本 Agent  
对应功能文档：`docs/features/M-011-剧本Agent.md`  
原则：先确认本文档，再改代码

## 0. 快速理解

```txt
一句话：如果剧本 Agent 模型支持 think，就显示思考等级切换。
为什么现在做：不同模型对推理深度支持不同，不能强行给所有模型显示。
做完后有什么用：用户可以按需要选择关闭、轻度、深度、极限思考。
这一步不碰什么：不改模型供应商配置，不训练模型，不改 Agent prompt。
```

## 1. 本次做什么

```txt
目标：
  对齐 Toonflow 根据模型详情显示思考等级按钮，并把配置发给 Agent 会话。

只做：
  1. 查询 scriptAgent 模型详情
  2. 判断是否支持 think
  3. 显示思考等级按钮
  4. 切换 thinkLevel
  5. 通知 Agent 会话更新配置

不做：
  1. 模型供应商配置
  2. Agent 模型映射配置
  3. 模型调用协议改造
```

## 2. 参考项目怎么做

| 参考文件 | 关键逻辑 |
|---|---|
| `Toonflow-web/src/views/scriptAgent/index.vue` | 调 `/project/getModelDetails key=scriptAgent`，判断是否显示思考按钮 |
| `Toonflow-web/src/stores/scriptAgent.ts` | emit `updateThinkConfig` |
| `Toonflow-app/src/socket/routes/scriptAgent.ts` | 接收 `updateThinkConfig` 更新 thinkConfig |
| `Toonflow-app/src/agents/scriptAgent/index.ts` | 调 `u.Ai.Text(agentKey, think, thinlLevel).stream` |

参考项目关键事实：

```txt
1. 思考等级选项：关闭、轻度、深度、极限，对应 0/1/2/3。
2. 只有模型支持 think 时显示。
3. 源码里 thinlLevel 有拼写错误，VT Studio 不能照搬。
4. 切换配置没有明确失败提示，VT Studio 要补。
```

## 3. 用户操作

```txt
入口：
  剧本 Agent 聊天区设置或思考按钮。

按钮/操作：
  选择关闭、轻度、深度、极限。

弹窗/表单：
  弹出菜单。

成功反馈：
  当前思考等级更新。

失败反馈：
  更新失败提示并回滚。
```

## 4. 要做什么功能

### 1. 判断模型是否支持思考

怎么做：
- 输入：projectId、agentKey=scriptAgent。
- 输出：模型详情和 think 支持状态。
- 写什么数据：不写。
- 状态怎么变：supportsThinking=true/false。
- 异常怎么处理：查询失败时隐藏思考按钮并提示模型配置异常。
- 限制：不能默认所有模型都支持。

### 2. 切换思考等级

怎么做：
- 输入：thinkLevel。
- 输出：Agent 会话配置更新。
- 写什么数据：可写会话内存状态，不写业务表。
- 状态怎么变：当前等级更新。
- 异常怎么处理：更新失败回滚到旧等级。
- 限制：thinkLevel 只能是 0/1/2/3。

### 3. 发送给 Agent 服务

怎么做：
- 输入：thinkLevel。
- 输出：后续模型调用使用新配置。
- 写什么数据：不写。
- 状态怎么变：agent session thinkConfig 更新。
- 异常怎么处理：会话未连接时提示无法更新。
- 限制：配置影响后续生成，不改已经生成中的消息。

## 5. 数据和状态

```txt
字段：
  agent_model_configs.key
  vendor_models.think
  session.think_level

接口/能力：
  agent.script.getModelCapabilities
  agent.script.updateThinkConfig

数据读写：
  读 Agent 模型配置
  更新会话状态

任务状态：
  不写 tasks

轮询/Socket：
  参考项目通过 Socket 事件；VT Studio 通过 IPC event/session API

模型调用：
  后续文本模型 stream 使用 thinkLevel

删除影响：
  无
```

## 6. VT Studio 怎么落

```txt
能力名：
  agent.script.updateThinkConfig

调用链：
  renderer 思考菜单 -> window.vtStudio.agent.script.updateThinkConfig -> main/services/agent/script

需要新增：
  思考等级 UI
  模型能力判断

需要修改：
  Agent 文本调用参数使用 thinkLevel，不保留 thinlLevel 拼写
```

## 7. 偏差

```txt
和 Toonflow 不同的地方：
  修正 thinlLevel 拼写为 thinkLevel，并补失败回滚。

原因：
  旧拼写是明显错误；失败回滚避免 UI 状态和后端不一致。

是否写入 04：
  不需要，属于命名修正。
```

## 8. 验收

```txt
1. 支持 think 的模型显示思考等级。
2. 不支持 think 的模型不显示。
3. 四个等级可切换。
4. 切换后后续消息使用新等级。
5. 更新失败会提示并回滚。
6. 生成中切换不影响当前消息。
```

## 9. 用户确认点

| 编号 | 确认点 | 专业建议 |
|---|---|---|
| C-F-011-004-001 | 默认思考等级 | 建议默认关闭或按模型配置默认值，不强行深度 |

## 10. 执行后记录

```txt
改了哪些文件：由 P6 阶段 1 统一完成，详见 docs/tasks/P6-剧本Agent批.md
验证结果：verify-p6-script-agent-phase1 通过；typecheck 通过；build 通过
未完成事项：无
最终结论：已完成剧本 Agent 模型能力检查；模型支持 think 时可切换关闭、轻度、深度、极限并发送 Socket 配置
```

## 11. 最后大白话

```txt
我这次准备怎么做：
1. 先判断当前剧本 Agent 模型支不支持思考。
2. 支持才显示等级按钮。
3. 用户切换后，后续 Agent 调用按新等级执行。

我不会做什么：
1. 不改供应商配置。
2. 不给不支持的模型硬塞思考参数。

确认规则：
用户确认后才执行；未确认前只改文档。
```
