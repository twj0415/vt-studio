# F-011-006 获取计划数据

状态：已完成（P6 阶段 2 覆盖）  
所属菜单：M-011 剧本 Agent  
对应功能文档：`docs/features/M-011-剧本Agent.md`  
原则：已按 P6 批次合并执行，不再单独等待确认

## 0. 快速理解

```txt
一句话：读取右侧工作区的故事骨架、改编策略和剧本列表。
为什么现在做：Agent 对话产出的内容要落到右侧工作区，不只是聊天文本。
做完后有什么用：用户打开页面能看到已有计划和剧本卡片。
这一步不碰什么：不编辑内容，不写 XML，不删除剧本。
```

## 1. 本次做什么

```txt
目标：
  对齐 Toonflow `/scriptAgent/getPlanData`，同时修正 schema 不一致问题。

只做：
  1. 读取故事骨架
  2. 读取改编策略
  3. 读取真实剧本列表
  4. 首次进入创建空工作区数据
  5. 统一 planData 类型

不做：
  1. 编辑故事骨架
  2. 编辑改编策略
  3. 编辑剧本卡片
  4. XML 写入
```

## 2. 参考项目怎么做

| 参考文件 | 关键逻辑 |
|---|---|
| `Toonflow-web/src/views/scriptAgent/index.vue` | 进入页面调用 `/scriptAgent/getPlanData` |
| `Toonflow-web/src/stores/scriptAgent.ts` | planData 包含 storySkeleton、adaptationStrategy、script 数组 |
| `Toonflow-app/src/routes/scriptAgent/getPlanData.ts` | 查询 `o_agentWorkData key=scriptAgent`，同时读取 `o_script` 拼 script |
| `Toonflow-app/src/lib/initDB.ts` | `o_agentWorkData` 保存工作区 data |

参考项目关键事实：

```txt
1. 首次没有 o_agentWorkData 时创建空 storySkeleton 和 adaptationStrategy。
2. 剧本列表不是只从 agentWorkData 读取，而是从 o_script 查出来拼上。
3. getPlanData 返回 storySkeleton、adaptationStrategy、script。
4. 参考项目 schema 有冲突：setPlanData schema 没声明 script，但代码又使用 script。
```

## 3. 用户操作

```txt
入口：
  打开剧本 Agent 页面自动读取。

按钮/操作：
  无。

弹窗/表单：
  无。

成功反馈：
  右侧 tabs 展示故事骨架、改编策略、剧本卡片。

失败反馈：
  读取失败显示错误空状态。
```

## 4. 要做什么功能

### 1. 读取工作区数据

怎么做：
- 输入：projectId。
- 输出：storySkeleton、adaptationStrategy。
- 写什么数据：首次没有时创建空工作区记录。
- 状态怎么变：loading=true -> false。
- 异常怎么处理：JSON 损坏时返回错误并提示修复。
- 限制：key 固定为 scriptAgent。

### 2. 读取真实剧本列表

怎么做：
- 输入：projectId。
- 输出：scripts 数组。
- 写什么数据：不写。
- 状态怎么变：planData.scripts 更新。
- 异常怎么处理：读取失败时右侧剧本 tab 显示错误。
- 限制：剧本事实以 scripts 表为准，不只信 agentWorkData 快照。

### 3. 统一 planData 类型

怎么做：
- 输入：工作区 data 和 scripts 表。
- 输出：统一 DTO。
- 写什么数据：不写。
- 状态怎么变：无。
- 异常怎么处理：缺字段补默认值。
- 限制：前端、服务、Agent 工具共用同一类型。

## 5. 数据和状态

```txt
字段：
  agent_work_data.project_id
  agent_work_data.key
  agent_work_data.data
  scripts.id
  scripts.name
  scripts.content

接口/能力：
  agent.script.getWorkspace

数据读写：
  读/创建 agent_work_data
  读 scripts

任务状态：
  不写 tasks

轮询/Socket：
  无

模型调用：
  无

删除影响：
  无
```

## 6. VT Studio 怎么落

```txt
能力名：
  agent.script.getWorkspace

调用链：
  renderer 右侧工作区 -> window.vtStudio.agent.script.getWorkspace -> main/services/agent/script-workspace

需要新增：
  planData DTO
  getWorkspace service
  右侧 tabs 数据加载

需要修改：
  scripts 表读取和 Agent 工作区读取统一
```

## 7. 偏差

```txt
和 Toonflow 不同的地方：
  修正 planData schema，把 scripts 明确作为数组。

原因：
  参考项目 schema 和真实前端数据不一致，照搬会运行时出错。

是否写入 04：
  写入，属于修正源码 schema 冲突。
```

## 8. 验收

```txt
1. 打开页面能读取故事骨架。
2. 能读取改编策略。
3. 能读取剧本列表。
4. 没有工作区数据时会创建空记录。
5. scripts 是数组，不是 string。
6. 数据损坏有错误提示。
7. 页面不直接访问 SQLite。
```

## 9. 用户确认点

| 编号 | 确认点 | 专业建议 |
|---|---|---|
| C-F-011-006-001 | 首次创建空工作区是否自动完成 | 建议自动创建，避免页面大量空判断 |

## 10. 执行后记录

```txt
改了哪些文件：
  新增 agent_work_data/scripts 迁移、ScriptAgentWorkspace DTO、script-workspace service、agent IPC/preload 工作区接口；
  ScriptAgentHome.vue 右侧工作区接 getWorkspace，首次打开自动创建空工作区。
验证结果：
  P6 阶段 2专项验证、typecheck、build 均通过。
未完成事项：
  XML 写入、工具读取上下文和子 Agent 调度留到 P6 阶段 3/4。
最终结论：
  已完成；scripts 明确为数组，剧本事实以 scripts 表为准。
```

## 11. 最后大白话

```txt
我这次准备怎么做：
1. 打开剧本 Agent 时读取右侧工作区。
2. 故事骨架和改编策略从工作区读。
3. 剧本列表从真实剧本表读。

我不会做什么：
1. 不编辑内容。
2. 不删除剧本。
3. 不写 Agent XML。

确认规则：
用户确认后才执行；未确认前只改文档。
```
