# F-011-007 编辑故事骨架

状态：已完成（P6 阶段 2 覆盖）  
所属菜单：M-011 剧本 Agent  
对应功能文档：`docs/features/M-011-剧本Agent.md`  
原则：已按 P6 批次合并执行，不再单独等待确认

## 0. 快速理解

```txt
一句话：右侧“故事骨架”tab 支持查看 Markdown 和手动编辑保存。
为什么现在做：Agent 生成的骨架需要用户可检查、可修正。
做完后有什么用：用户不用重新对话，也能直接调整故事结构。
这一步不碰什么：不生成新骨架，不编辑改编策略，不改剧本列表。
```

## 1. 本次做什么

```txt
目标：
  对齐 Toonflow 故事骨架 tab 的 MdPreview + 编辑弹窗。

只做：
  1. 故事骨架 Markdown 展示
  2. 空状态
  3. 编辑按钮
  4. Markdown 编辑弹窗
  5. 保存到 agent_work_data

不做：
  1. Agent 自动生成
  2. 改编策略编辑
  3. 剧本卡片编辑
```

## 2. 参考项目怎么做

| 参考文件 | 关键逻辑 |
|---|---|
| `Toonflow-web/src/views/scriptAgent/index.vue` | storySkeleton tab 有内容用 MdPreview，无内容 t-empty，编辑打开 editMdPreivew |
| `Toonflow-app/src/routes/scriptAgent/updateData.ts` | 更新 `o_agentWorkData.data.storySkeleton` |

参考项目关键事实：

```txt
1. storySkeleton 是 planData 顶层字段。
2. 保存调用 updateData。
3. Markdown 编辑器阻止图片拖拽上传。
4. 源码组件 editMdPreivew 有拼写问题，VT Studio 不能照搬。
```

## 3. 用户操作

```txt
入口：
  剧本 Agent 右侧 -> 故事骨架 tab。

按钮/操作：
  1. 点击编辑。
  2. 修改 Markdown。
  3. 点击保存。

弹窗/表单：
  Markdown 编辑弹窗。

成功反馈：
  保存成功，预览更新。

失败反馈：
  保存失败提示。
```

## 4. 要做什么功能

### 1. 展示故事骨架

怎么做：
- 输入：planData.storySkeleton。
- 输出：Markdown 预览或空状态。
- 写什么数据：不写。
- 状态怎么变：无。
- 异常怎么处理：Markdown 渲染失败显示纯文本兜底。
- 限制：不允许 Markdown 图片拖拽上传。

### 2. 编辑并保存故事骨架

怎么做：
- 输入：Markdown 文本。
- 输出：更新后的 storySkeleton。
- 写什么数据：更新 agent_work_data.data.storySkeleton。
- 状态怎么变：保存中 -> 成功关闭弹窗。
- 异常怎么处理：保存失败保留弹窗内容。
- 限制：只更新 storySkeleton，不覆盖 scripts。

## 5. 数据和状态

```txt
字段：
  agent_work_data.data.storySkeleton

接口/能力：
  agent.script.updateWorkspaceField(field=storySkeleton)

数据读写：
  读/写 agent_work_data

任务状态：
  不写 tasks

轮询/Socket：
  无

模型调用：
  无

删除影响：
  无
```

## 6. VT Studio 怎么落

```txt
能力名：
  agent.script.updateWorkspaceField

调用链：
  renderer 右侧工作区 -> window.vtStudio.agent.script.updateWorkspaceField -> main/services/agent/script-workspace

需要新增：
  故事骨架 tab
  Markdown 编辑弹窗

需要修改：
  planData 保存服务，避免覆盖 scripts
```

## 7. 偏差

```txt
和 Toonflow 不同的地方：
  修正 editMdPreivew 拼写，不保留旧组件名。

原因：
  明显拼写错误不进入 VT Studio 正式命名。

是否写入 04：
  不需要，属于旧痕迹修正。
```

## 8. 验收

```txt
1. 有故事骨架时能预览。
2. 无内容时显示空状态。
3. 点击编辑能打开 Markdown 弹窗。
4. 保存后预览更新。
5. 保存失败保留编辑内容。
6. 不覆盖改编策略和剧本列表。
7. 页面不直接访问 SQLite。
```

## 9. 用户确认点

| 编号 | 确认点 | 专业建议 |
|---|---|---|
| C-F-011-007-001 | Markdown 是否允许插入图片 | 建议不允许，图片生产走资产和生产工作台 |

## 10. 执行后记录

```txt
改了哪些文件：
  ScriptAgentHome.vue 增加 storySkeleton 草稿、保存状态和保存入口；
  script-workspace service 支持 updateWorkspaceField；
  i18n 和 SCSS 补齐右侧工作区编辑态。
验证结果：
  P6 阶段 2专项验证、typecheck、build 均通过。
未完成事项：
  Agent 自动生成故事骨架留到 XML 写入和子 Agent 阶段。
最终结论：
  已完成；保存只更新 storySkeleton，不覆盖改编策略和 scripts。
```

## 11. 最后大白话

```txt
我这次准备怎么做：
1. 显示故事骨架 Markdown。
2. 用户可以打开弹窗手动改。
3. 保存只更新故事骨架这一项。

我不会做什么：
1. 不自动生成骨架。
2. 不改剧本列表。

确认规则：
用户确认后才执行；未确认前只改文档。
```
