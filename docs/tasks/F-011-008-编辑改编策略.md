# F-011-008 编辑改编策略

状态：已完成（P6 阶段 2 覆盖）  
所属菜单：M-011 剧本 Agent  
对应功能文档：`docs/features/M-011-剧本Agent.md`  
原则：已按 P6 批次合并执行，不再单独等待确认

## 0. 快速理解

```txt
一句话：右侧“改编策略”tab 支持查看 Markdown 和手动编辑保存。
为什么现在做：改编策略会影响后续剧本生成方向，必须能人工修正。
做完后有什么用：用户可以调整改编原则、节奏和取舍。
这一步不碰什么：不生成新策略，不编辑故事骨架，不改剧本内容。
```

## 1. 本次做什么

```txt
目标：
  对齐 Toonflow 改编策略 tab 的 MdPreview + 编辑弹窗。

只做：
  1. 改编策略 Markdown 展示
  2. 空状态
  3. 编辑按钮
  4. Markdown 编辑弹窗
  5. 保存到 agent_work_data

不做：
  1. Agent 自动生成
  2. 故事骨架编辑
  3. 剧本卡片编辑
```

## 2. 参考项目怎么做

| 参考文件 | 关键逻辑 |
|---|---|
| `Toonflow-web/src/views/scriptAgent/index.vue` | adaptationStrategy tab 有内容用 MdPreview，无内容 t-empty，编辑打开弹窗 |
| `Toonflow-app/src/routes/scriptAgent/updateData.ts` | 更新 `o_agentWorkData.data.adaptationStrategy` |

参考项目关键事实：

```txt
1. adaptationStrategy 是 planData 顶层字段。
2. 保存调用 updateData。
3. Markdown 编辑器阻止图片拖拽上传。
```

## 3. 用户操作

```txt
入口：
  剧本 Agent 右侧 -> 改编策略 tab。

按钮/操作：
  1. 点击编辑。
  2. 修改 Markdown。
  3. 点击保存。

弹窗/表单：
  Markdown 编辑弹窗。

成功反馈：
  保存成功，预览更新。

失败反馈：
  保存失败提示。
```

## 4. 要做什么功能

### 1. 展示改编策略

怎么做：
- 输入：planData.adaptationStrategy。
- 输出：Markdown 预览或空状态。
- 写什么数据：不写。
- 状态怎么变：无。
- 异常怎么处理：Markdown 渲染失败显示纯文本兜底。
- 限制：不允许 Markdown 图片拖拽上传。

### 2. 编辑并保存改编策略

怎么做：
- 输入：Markdown 文本。
- 输出：更新后的 adaptationStrategy。
- 写什么数据：更新 agent_work_data.data.adaptationStrategy。
- 状态怎么变：保存中 -> 成功关闭弹窗。
- 异常怎么处理：保存失败保留弹窗内容。
- 限制：只更新 adaptationStrategy，不覆盖 scripts。

## 5. 数据和状态

```txt
字段：
  agent_work_data.data.adaptationStrategy

接口/能力：
  agent.script.updateWorkspaceField(field=adaptationStrategy)

数据读写：
  读/写 agent_work_data

任务状态：
  不写 tasks

轮询/Socket：
  无

模型调用：
  无

删除影响：
  无
```

## 6. VT Studio 怎么落

```txt
能力名：
  agent.script.updateWorkspaceField

调用链：
  renderer 右侧工作区 -> window.vtStudio.agent.script.updateWorkspaceField -> main/services/agent/script-workspace

需要新增：
  改编策略 tab
  Markdown 编辑弹窗复用

需要修改：
  planData 保存服务
```

## 7. 偏差

```txt
和 Toonflow 不同的地方：
  保存时只更新当前字段，不依赖可能冲突的整体 schema。

原因：
  参考项目 setPlanData/updateData schema 有 script 字段冲突。

是否写入 04：
  如 F-011-006 已记录 schema 修正，不重复。
```

## 8. 验收

```txt
1. 有改编策略时能预览。
2. 无内容时显示空状态。
3. 点击编辑能打开 Markdown 弹窗。
4. 保存后预览更新。
5. 保存失败保留编辑内容。
6. 不覆盖故事骨架和剧本列表。
7. 页面不直接访问 SQLite。
```

## 9. 用户确认点

| 编号 | 确认点 | 专业建议 |
|---|---|---|
| C-F-011-008-001 | 改编策略是否允许为空 | 建议允许为空，但生成剧本前提示缺少策略 |

## 10. 执行后记录

```txt
改了哪些文件：
  ScriptAgentHome.vue 增加 adaptationStrategy 草稿、保存状态和保存入口；
  script-workspace service 支持 updateWorkspaceField；
  i18n 和 SCSS 补齐右侧工作区编辑态。
验证结果：
  P6 阶段 2专项验证、typecheck、build 均通过。
未完成事项：
  Agent 自动生成改编策略留到 XML 写入和子 Agent 阶段。
最终结论：
  已完成；保存只更新 adaptationStrategy，不覆盖故事骨架和 scripts。
```

## 11. 最后大白话

```txt
我这次准备怎么做：
1. 显示改编策略 Markdown。
2. 用户可以打开弹窗手动改。
3. 保存只更新改编策略这一项。

我不会做什么：
1. 不自动生成策略。
2. 不改剧本内容。

确认规则：
用户确认后才执行；未确认前只改文档。
```
