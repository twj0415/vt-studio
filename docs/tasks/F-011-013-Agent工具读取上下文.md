# F-011-013 Agent 工具读取上下文

状态：已完成  
所属菜单：M-011 剧本 Agent  
对应功能文档：`docs/features/M-011-剧本Agent.md`  
原则：先确认本文档，再改代码

## 0. 快速理解

```txt
一句话：给剧本 Agent 提供工具，让它能读取原文、事件、计划数据和已有剧本。
为什么现在做：Agent 决策不能靠猜，必须能查真实项目上下文。
做完后有什么用：Agent 可以基于章节事件、原文和已有剧本继续工作。
这一步不碰什么：不写入剧本，不生成图片，不调生产 Agent。
```

## 1. 本次做什么

```txt
目标：
  对齐 Toonflow scriptAgent tools：get_novel_events、get_planData、get_novel_text、get_script_content。

只做：
  1. 读取章节事件
  2. 读取章节原文
  3. 读取当前计划数据
  4. 读取已有剧本内容
  5. 工具参数校验和返回格式

不做：
  1. 写剧本
  2. 删除剧本
  3. 资产生成
  4. 生产工作台工具
```

## 2. 参考项目怎么做

| 参考文件 | 关键逻辑 |
|---|---|
| `Toonflow-app/src/agents/scriptAgent/tools.ts` | 定义 get_novel_events、get_planData、get_novel_text、get_script_content |
| `Toonflow-app/src/routes/novel/getNovelData.ts` | 读取原文章节 |
| `Toonflow-app/src/routes/scriptAgent/getPlanData.ts` | 读取计划数据 |
| `Toonflow-app/src/routes/scriptAgent/updateData.ts` | 写入计划数据 |

参考项目关键事实：

```txt
1. get_novel_events 参数是 chapterIndexs: number[]。
2. get_novel_text 参数是 chapterIndex: string。
3. get_planData 参数 key 可为 storySkeleton/adaptationStrategy/script。
4. get_script_content 参数 ids: string[]。
5. tools.ts 里 planData.script 定义成 string，但前端真实是数组，VT Studio 必须修正。
```

## 3. 用户操作

```txt
入口：
  用户和 Agent 对话时，Agent 自动调用工具。

按钮/操作：
  无直接按钮。

弹窗/表单：
  无。

成功反馈：
  Agent 回复更准确，工具调用可在聊天中展示。

失败反馈：
  工具参数错误或数据不存在时，Agent 收到明确错误。
```

## 4. 要做什么功能

### 1. 读取章节事件

怎么做：
- 输入：chapterIndexes。
- 输出：章节编号、标题、事件摘要、状态。
- 写什么数据：不写。
- 状态怎么变：无。
- 异常怎么处理：章节不存在返回空结果和说明。
- 限制：只读当前项目章节。

### 2. 读取章节原文

怎么做：
- 输入：chapterIndex。
- 输出：章节正文。
- 写什么数据：不写。
- 状态怎么变：无。
- 异常怎么处理：章节不存在返回工具错误。
- 限制：长文本要考虑截断或分块策略。

### 3. 读取计划数据

怎么做：
- 输入：key。
- 输出：storySkeleton/adaptationStrategy/scripts 中指定内容。
- 写什么数据：不写。
- 状态怎么变：无。
- 异常怎么处理：key 非法返回错误。
- 限制：scripts 返回数组结构，不返回含糊字符串。

### 4. 读取已有剧本内容

怎么做：
- 输入：scriptIds。
- 输出：剧本名称和内容。
- 写什么数据：不写。
- 状态怎么变：无。
- 异常怎么处理：id 不存在时返回缺失列表。
- 限制：按 id 查询，不按 name 查询。

## 5. 数据和状态

```txt
字段：
  source_chapters.chapter_index
  source_chapters.chapter_title
  source_chapters.content
  source_chapters.event_summary
  source_chapters.event_status
  agent_work_data.data
  scripts.id/name/content

接口/能力：
  agent.script.tools.getNovelEvents
  agent.script.tools.getNovelText
  agent.script.tools.getPlanData
  agent.script.tools.getScriptContent

数据读写：
  只读

任务状态：
  不写 tasks

轮询/Socket：
  工具调用通过 Agent 流式事件展示

模型调用：
  Agent 运行中调用这些工具，但工具本身不调模型

删除影响：
  无
```

## 6. VT Studio 怎么落

```txt
能力名：
  agent.script.tools

调用链：
  main/services/agent/script tools -> source/script/plan services

需要新增：
  工具参数 schema
  工具返回 DTO

需要修改：
  Agent 决策层工具注册
```

## 7. 偏差

```txt
和 Toonflow 不同的地方：
  planData.scripts 用数组结构，工具返回明确 DTO。

原因：
  参考项目 tools schema 和前端真实数据冲突。

是否写入 04：
  如 F-011-006 已记录 schema 修正，不重复。
```

## 8. 验收

```txt
1. 工具能读取章节事件。
2. 工具能读取章节正文。
3. 工具能读取故事骨架。
4. 工具能读取改编策略。
5. 工具能读取已有剧本内容。
6. 非法参数返回明确错误。
7. 工具只读当前项目数据。
8. renderer 不直接调用这些数据库查询。
```

## 9. 用户确认点

| 编号 | 确认点 | 专业建议 |
|---|---|---|
| C-F-011-013-001 | 长章节原文是否自动截断 | 建议工具支持分块或范围读取，避免一次塞爆上下文 |

## 10. 执行后记录

```txt
改了哪些文件：
  src/shared/types/script-agent.ts
  src/main/services/agent/script-tools.ts
  src/main/services/socket/agent-handler.ts
  scripts/verify-p6-script-agent-phase3.mjs
验证结果：
  verify-p6-script-agent-phase1 通过
  verify-p6-script-agent-phase2 通过
  typecheck 通过
  build 通过
未完成事项：
  子 Agent 调度不在本任务做；继续归 F-011-014/P6 阶段 4。
最终结论：
  P6 阶段 3 已完成；剧本 Agent 已注册 get_novel_events、get_novel_text、get_planData、get_script_content 只读工具，支持长文本 offset/limit 截断和参考项目旧字段兼容。
```

## 11. 最后大白话

```txt
我这次准备怎么做：
1. 给剧本 Agent 准备读取上下文的工具。
2. 它能查原文、事件、计划、已有剧本。
3. 工具只读数据，不乱写业务表。

我不会做什么：
1. 不生成剧本。
2. 不写生产工作台数据。
3. 不让前端直接查数据库。

确认规则：
用户确认后才执行；未确认前只改文档。
```
