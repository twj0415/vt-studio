# F-011-014 子 Agent 调度

状态：已完成  
所属菜单：M-011 剧本 Agent  
对应功能文档：`docs/features/M-011-剧本Agent.md`  
原则：已按确认点执行；后续如扩展 Production Agent 另开任务

## 0. 快速理解

```txt
一句话：剧本 Agent 不是单轮聊天，要有决策层、执行层和监督层子 Agent。
为什么现在做：参考项目的专业性在多 Agent 调度和工具调用，不能简化成普通聊天。
做完后有什么用：Agent 能按任务选择生成骨架、策略、剧本或做监督检查。
这一步不碰什么：不做生产 Agent，不做视频生成，不做资产中心。
```

## 1. 本次做什么

```txt
目标：
  对齐 Toonflow runDecisionAI 和子 Agent 工具。

只做：
  1. 决策层 Agent
  2. 故事骨架子 Agent
  3. 改编策略子 Agent
  4. 剧本生成子 Agent
  5. 监督层子 Agent
  6. 子 Agent 输出 XML 校验
  7. 任务中心记录建议

不做：
  1. Production Agent
  2. 图片生成
  3. 视频生成
  4. 剪映导出
```

## 2. 参考项目怎么做

| 参考文件 | 关键逻辑 |
|---|---|
| `Toonflow-app/src/agents/scriptAgent/index.ts` | runDecisionAI 读取 memory、project、novel count、skill，调用决策模型 |
| `Toonflow-app/src/agents/scriptAgent/tools.ts` | 注册上下文工具和子 Agent 工具 |
| `data/skills/script_agent_decision.md` | 决策层提示词 |
| `data/skills/script_execution_skeleton.md` | 故事骨架执行层 |
| `data/skills/script_execution_adaptation.md` | 改编策略执行层 |
| `data/skills/script_execution_script.md` | 剧本生成执行层 |
| `data/skills/script_agent_supervision.md` | 监督层 |

参考项目关键事实：

```txt
1. 决策层模型 key：scriptAgent:decisionAgent。
2. 故事骨架子 Agent key：scriptAgent:storySkeletonAgent。
3. 改编策略子 Agent key：scriptAgent:adaptationStrategyAgent。
4. 剧本生成子 Agent key：scriptAgent:scriptAgent。
5. 监督层 key：scriptAgent:supervisionAgent。
6. 子 Agent 输出必须是 storySkeleton/adaptationStrategy/scriptItem XML。
7. Agent 对话没有写任务中心，VT Studio 建议增强。
```

## 3. 用户操作

```txt
入口：
  用户在剧本 Agent 聊天框发送任务。

按钮/操作：
  1. 发送需求。
  2. 可停止当前生成。
  3. 可查看工具调用和子 Agent 输出。

弹窗/表单：
  无。

成功反馈：
  Agent 输出聊天内容，并把结构化结果写入右侧工作区。

失败反馈：
  模型失败、工具失败、XML 非法时显示 error，并记录失败原因。
```

## 4. 要做什么功能

### 1. 决策层 Agent

怎么做：
- 输入：用户消息、项目资料、记忆、可用工具。
- 输出：决策回复或子 Agent 调用。
- 写什么数据：写 message 记忆。
- 状态怎么变：Agent 会话 streaming。
- 异常怎么处理：模型失败写 error 消息。
- 限制：必须使用 script_agent_decision skill，不做普通单 prompt 聊天。

### 2. 故事骨架子 Agent

怎么做：
- 输入：决策层任务、原文/事件/计划上下文。
- 输出：`<storySkeleton>` XML。
- 写什么数据：XML 完成后写故事骨架。
- 状态怎么变：子消息 streaming -> complete。
- 异常怎么处理：输出不含 storySkeleton 时标记失败。
- 限制：模型 key 来自 Agent 模型配置。

### 3. 改编策略子 Agent

怎么做：
- 输入：故事骨架、原文事件、用户目标。
- 输出：`<adaptationStrategy>` XML。
- 写什么数据：XML 完成后写改编策略。
- 状态怎么变：子消息 streaming -> complete。
- 异常怎么处理：输出格式错误不写入。
- 限制：不能把策略写进故事骨架字段。

### 4. 剧本生成子 Agent

怎么做：
- 输入：故事骨架、改编策略、已有剧本、原文上下文。
- 输出：一个或多个 `<scriptItem>` XML。
- 写什么数据：写 scripts 表和 agent_work_data。
- 状态怎么变：子消息 streaming -> complete。
- 异常怎么处理：scriptItem name/content 缺失时拒绝写入。
- 限制：不按 name 覆盖已有剧本。

### 5. 监督层 Agent

怎么做：
- 输入：生成结果和目标要求。
- 输出：监督意见或修正建议。
- 写什么数据：写监督消息记忆，不直接覆盖业务数据。
- 状态怎么变：supervision streaming -> complete。
- 异常怎么处理：失败不回滚已保存结果，但提示监督失败。
- 限制：监督层不能绕过 XML 写入规则直接改表。

### 6. 任务中心记录

怎么做：
- 输入：一次 Agent 生成请求。
- 输出：任务记录。
- 写什么数据：写 tasks。
- 状态怎么变：running -> succeeded/failed/cancelled。
- 异常怎么处理：停止时写 cancelled 或 stopped 状态映射。
- 限制：这是 VT Studio 增强，需写 04。

## 5. 数据和状态

```txt
字段：
  agent_model_configs
  memories
  agent_work_data
  scripts
  tasks

接口/能力：
  agent.script.runDecision
  agent.script.runSubAgent
  agent.script.stop
  agent.script.applyXmlPatch

数据读写：
  读项目、原文、事件、计划、剧本、记忆
  写记忆、计划、剧本、任务

任务状态：
  Agent 请求建议写 running/succeeded/failed/cancelled

轮询/Socket：
  通过 Agent 流式事件返回

模型调用：
  决策层、故事骨架、改编策略、剧本生成、监督层均走 Model Adapter

删除影响：
  无
```

## 6. VT Studio 怎么落

```txt
能力名：
  agent.script.run

调用链：
  renderer chat -> window.vtStudio.agent.script.chat -> main/services/agent/script -> model adapter/tools

需要新增：
  决策层服务
  子 Agent runner
  skill 文件读取
  XML 输出校验
  Agent 任务记录接入

需要修改：
  F-011-001 对话入口接入真实调度
  F-011-012 XML 写入工作区
```

## 7. 偏差

```txt
和 Toonflow 不同的地方：
  1. Agent 生成建议写任务中心。
  2. XML 非法时不写入业务数据。
  3. 不按 name 覆盖 scriptItem。

原因：
  长耗时 Agent 需要可追踪；结构化写入必须保护业务数据。

是否写入 04：
  写入。
```

## 8. 验收

```txt
1. 用户发送消息能进入决策层。
2. 决策层能调用上下文工具。
3. 能调用故事骨架子 Agent。
4. 能调用改编策略子 Agent。
5. 能调用剧本生成子 Agent。
6. 能调用监督层。
7. 子 Agent 输出 XML 合法才写入工作区。
8. 停止生成能中断当前请求。
9. 失败有错误消息和任务失败原因。
10. 页面不直接读取 skill 文件或调用模型 SDK。
```

## 9. 用户确认点

| 编号 | 确认点 | 专业建议 |
|---|---|---|
| C-F-011-014-001 | Agent 生成是否写任务中心 | 建议写 |
| C-F-011-014-002 | 监督层失败是否阻止保存结果 | 建议不阻止已合法保存的结果，但显示监督失败 |
| C-F-011-014-003 | skill 文件放哪里 | 建议放应用可管理资源目录，renderer 不直接访问路径 |

## 10. 执行后记录

```txt
改了哪些文件：
  新增 src/main/services/agent/script-runner.ts；
  修改 src/main/services/socket/agent-handler.ts；
  新增 scripts/verify-p6-script-agent-phase4.mjs；
  更新 P6、F-011-014、03、04 文档状态。
验证结果：
  verify-p6-script-agent-phase1 通过；
  verify-p6-script-agent-phase2 通过；
  verify-p6-script-agent-phase3 通过；
  verify-p6-script-agent-phase4 通过；
  typecheck 通过；
  build 通过。
未完成事项：
  不做 Production Agent、图片生成、视频生成、剪映导出；
  P7 再做完整剧本结果页、搜索、导出和资产提取。
最终结论：
  P6 阶段 4 已完成；剧本 Agent 已接入决策层、故事骨架、改编策略、剧本生成和监督层子 Agent 的最小闭环；
  子 Agent XML 合法后写入工作区，非法 XML 不写业务表；
  Agent 长生成已写入任务中心，并支持成功、失败、停止状态闭环。
```

## 11. 最后大白话

```txt
我这次准备怎么做：
1. 把剧本 Agent 做成决策层 + 子 Agent，不是普通聊天。
2. 子 Agent 分别负责骨架、策略、剧本和监督。
3. 输出格式正确才写右侧工作区。
4. 长任务写入任务中心，方便查失败。

我不会做什么：
1. 不做生产 Agent。
2. 不做图片视频生成。
3. 不让前端直接读 skill 或跑模型。

确认规则：
用户确认后才执行；未确认前只改文档。
```
