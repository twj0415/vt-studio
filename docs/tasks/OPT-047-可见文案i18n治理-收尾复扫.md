# OPT-047 可见文案 i18n 治理 - 收尾复扫

状态：已完成

## 0. 快速理解

一句话：全 renderer 复扫中文硬编码，只允许语言包和明确业务解析正则，最后集中跑 build。

这一步不碰什么：不改业务逻辑，不改 UI，不再拆小批次。

## 1. 扫描结论

允许保留：

1. `src/renderer/src/i18n/messages.ts`
   - 语言包，本来就应该包含中文。
2. `NovelHome.vue` 的 `DEFAULT_CHAPTER_REG`
   - 中文小说章节解析规则。
3. `NovelHome.vue` 的 `volumeRegex`
   - 中文小说卷名识别规则。
4. `ScriptHome.vue` 的 `DEFAULT_SCRIPT_REG`
   - 中文剧本分集解析规则。

除此之外，不允许 renderer 代码出现中文硬编码。

## 2. 要做什么功能：怎么做

| 要做什么 | 怎么做 |
|---|---|
| 全局防回退 | 新增 `verify-opt-047-renderer-i18n-sweep.mjs` |
| 明确允许项 | 脚本里只允许语言包和 3 个业务解析规则 |
| 集中验证 | 跑 `typecheck`、`verify:docs`、`build` |

## 3. 文件改动

| 文件 | 作用 |
|---|---|
| `scripts/verify-opt-047-renderer-i18n-sweep.mjs` | renderer 全局中文硬编码防回退 |
| `scripts/verify.mjs` | 加入 settings 分组，保证常规验证能覆盖 |

## 4. 验收标准

1. 除语言包和允许解析正则外，renderer 无中文硬编码。
2. `verify-opt-047-renderer-i18n-sweep.mjs` 通过。
3. `typecheck`、`verify:docs`、`build` 通过。

## 5. 后续批次

1. OPT-047 收尾后，下一步按 TODO 进入 `OPT-042 Tailwind 优先和 SCSS 边界治理` 或 `OPT-048 全局菜单和页面布局治理`。

## 6. 执行记录

- 完成时间：2026-07-03
- 涉及文件：`scripts/verify-opt-047-renderer-i18n-sweep.mjs`、`scripts/verify.mjs`
- 验证结果：`D:\software\nodejs\node.exe scripts\verify-opt-047-renderer-i18n-sweep.mjs`、`D:\software\nodejs\pnpm.cmd run typecheck`、`D:\software\nodejs\pnpm.cmd run verify:docs`、`D:\software\nodejs\pnpm.cmd run build` 通过。

## 7. 最后大白话

这就是最后兜底检查：以后谁在页面里直接写中文，脚本会拦住；真正的中文解析规则不乱改。
