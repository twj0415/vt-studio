# OPT-047 可见文案 i18n 治理 - 第十六批：剩余业务页复扫

状态：已完成

## 0. 快速理解

一句话：这批不硬改代码，先把资产、角景音频、生产、导出全部扫一遍，确认没有中文硬编码残留，再用专项校验防回退。

这一步不碰什么：不改资产生成逻辑，不改生产画布，不改导出逻辑，不做 UI 重构。

## 1. 扫描结论

扫描范围：

1. `src/renderer/src/features/assets`
2. `src/renderer/src/features/corner-scape`
3. `src/renderer/src/features/production`
4. `src/renderer/src/features/export`

结果：

1. 未发现中文硬编码。
2. 页面、按钮、弹窗、toast、placeholder、空状态基本都通过 `t(...)` 或 `home.export.*` 获取。
3. 这批不需要改业务代码，只加专项校验，防止后续新增中文残留。

## 2. 要做什么功能：怎么做

| 要做什么 | 怎么做 |
|---|---|
| 剩余业务页防回退 | 新增专项 verify，检查这些目录没有中文硬编码 |
| 确认 i18n 接入 | 校验资产、角景、生产、导出入口都存在 `useI18n` 和对应 key |
| 接入统一验证 | 把专项校验加入 `verify:assets`、`verify:production` 或集中分组 |

## 3. 文件改动

| 文件 | 作用 |
|---|---|
| `scripts/verify-opt-047-business-pages-i18n.mjs` | 剩余业务页 i18n 防回退校验 |
| `scripts/verify.mjs` | 加入对应验证分组 |

## 4. 验收标准

1. 剩余业务页目录无中文硬编码。
2. 资产、角景音频、生产、导出入口都仍接入 i18n。
3. 专项 verify、assets verify、production verify、docs verify 通过。
4. 本批不单独跑 build，等 OPT-047 收尾批统一跑。

## 5. 后续批次

1. OPT-047 收尾批：全 renderer 复扫、补文档、集中跑 `build`。

## 6. 执行记录

- 完成时间：2026-07-03
- 涉及文件：`scripts/verify-opt-047-business-pages-i18n.mjs`、`scripts/verify.mjs`
- 验证结果：`D:\software\nodejs\node.exe scripts\verify-opt-047-business-pages-i18n.mjs`、`D:\software\nodejs\pnpm.cmd run verify:assets`、`D:\software\nodejs\pnpm.cmd run verify:production`、`D:\software\nodejs\pnpm.cmd run typecheck` 通过；本批为复扫和防回退，未改业务代码，未单独跑 build。

## 7. 最后大白话

这批是复扫和加防线。代码本身没发现要改的中文文案，就不要为了“看起来做了很多”乱改。
