# 功能ID 功能名称

状态：等待用户确认  
所属菜单：M-xxx  
对应功能文档：`docs/features/M-xxx-xxx.md`  
原则：先确认本文档，再改代码

文件命名规则：

```txt
正式功能 task 必须用真实功能 ID 命名，并且一个 features 文件只对应一个前缀：
  docs/features/00-公共底层.md        -> CORE-xxx
  docs/features/M-001-全局导航与项目入口.md -> F-001-xxx
  docs/features/M-002-设置.md         -> F-002-xxx
  docs/features/M-003-项目管理.md     -> F-003-xxx
  docs/features/M-004-小说原文.md     -> F-004-xxx
  docs/features/M-005-剧本.md         -> F-005-xxx
  docs/features/M-006-资产中心.md     -> F-006-xxx
  docs/features/M-007-角景音频绑定.md -> F-007-xxx
  docs/features/M-008-生产工作台.md   -> F-008-xxx
  docs/features/M-009-任务中心.md     -> F-009-xxx
  docs/features/M-010-导出.md         -> F-010-xxx
  docs/features/M-011-剧本Agent.md    -> F-011-xxx

示例：
  CORE-012-本地媒体访问服务.md
  F-002-003-模型供应商配置.md

模板、扫描、搭框架这类非真实功能不能占用数字编号：
  TEMPLATE-功能执行文档模板.md
  DOC-001-扫描参考项目菜单与功能.md
  FRAME-001-搭建可运行项目框架.md

不允许再用 013、014、015 这种流水号命名正式功能。
不允许同一个 features 文件下混多个前缀。
不允许把模板、扫描、框架准备文档算进正式功能前缀。
```

## 0. 快速理解

用普通话说明这一步到底要干什么，先让人一眼看懂，再写细节。

```txt
一句话：
为什么现在做：
做完后有什么用：
这一步不碰什么：
```

## 1. 本次做什么

```txt
目标：
只做：
不做：
```

## 2. 参考项目怎么做

本次开工前必须重新看参考源码，不能凭记忆。

| 参考文件 | 关键逻辑 |
|---|---|
| `Toonflow-web/...` |  |
| `Toonflow-app/...` |  |

参考事实必须直接写结论，不能写“后续再看”。如果源码里存在坏逻辑，也要写出来。

```txt
参考项目已确认：
1.
2.

参考项目不能照搬：
1.
2.

源码未找到：
1.
```

## 3. 用户操作

不涉及页面就写“无”。

```txt
入口：
按钮/操作：
弹窗/表单：
成功反馈：
失败反馈：
```

## 4. 要做什么功能

不能写“去看参考项目”“后续分析”这种空步骤。参考源码必须已经查完，这里直接写结论。

```txt
### 1. 功能/操作名称
怎么做：
- 输入：
- 输出：
- 写什么数据：
- 状态怎么变：
- 异常怎么处理：
- 限制：

### 2. 功能/操作名称
怎么做：
- 输入：
- 输出：
- 写什么数据：
- 状态怎么变：
- 异常怎么处理：
- 限制：
```

## 5. 数据和状态

Toonflow 源码名只用于回查，VT Studio 不照搬旧表名、旧字段名、拼写错误。

```txt
字段：
接口/能力：
数据读写：
任务状态：
轮询/Socket：
模型调用：
删除影响：
```

## 5.1 不能漏的收口点

这一节必须写具体结论，不能写空话。没有相关内容就写“无”。

```txt
依赖谁：
被谁依赖：
服务契约：
数据契约：
状态契约：
文件契约：
模型契约：
任务契约：
异常契约：
回滚契约：
参考项目缺陷：
VT Studio 差异：
```

写法要求：

```txt
能确定的直接写定。
参考源码没有的写“源码未找到：xxx”。
需要用户做产品选择的写“需要你确认：xxx”。
不能把“建议后续再补”当成执行依据。
```

## 6. VT Studio 怎么落

总规则见 `docs/00-项目规范.md`，这里写本功能具体落点。

```txt
能力名：
调用链：renderer -> window.vtStudio -> main/ipc -> main/services
需要新增：
需要修改：
样式方案：能用 Tailwind 的全部用 Tailwind；只有穿透、全局、滚动、z-index、复杂动画/伪元素或公共样式才写 SCSS
多语言方案：所有用户能看到的文案必须写入 i18n，同时补 zh-CN 和 en；动态文案必须用 i18n params
UI/UX 方案：涉及页面结构、视觉层级、交互、响应式、可访问性时，必须使用 ui-ux-pro-max Skill 检查，并写明检查重点
```

## 7. 偏差

没有就写“无”。

```txt
和 Toonflow 不同的地方：
原因：
是否写入 04：
```

## 8. 验收

```txt
1. 入口能找到。
2. 操作、字段、状态不缺。
3. 成功路径通过。
4. 失败路径有提示。
5. 没有把 Toonflow 旧命名当成 VT Studio 正式命名。
6. 如有偏差，已写入 04。
7. 可见文案已走 i18n，zh-CN/en 都已补齐。
8. 样式优先使用 Tailwind，新增 SCSS 必须属于允许场景并说明原因。
9. UI 相关任务已按 ui-ux-pro-max 检查可访问性、点击区域、布局响应式、颜色对比、表单反馈和导航清晰度。
```

## 9. 执行后记录

```txt
改了哪些文件：
验证结果：
未完成事项：
最终结论：完全对齐 / 可接受偏差 / 不可接受偏差
```

## 10. 最后大白话

用普通话再总结一次，避免只看技术细节看不懂实际动作。

```txt
我这次准备怎么做：
1.
2.
3.

我不会做什么：
1.
2.

确认规则：
用户确认后才执行；未确认前只改文档。
如果本任务有需要用户确认点，或执行中新增确认点，必须先停下来问用户；未确认前不能开始或继续实现。
如果后续扫描参考项目发现本 task 漏了逻辑：
1. 先把漏掉的逻辑补进本文档。
2. 本 task 未实现时，状态仍保持“等待用户确认”。
3. 本 task 已实现时，状态改成“已完成，新增补充待补做”，补完代码和验证后再改回“已完成”。
4. 不允许只改 features 不同步 task。
```
