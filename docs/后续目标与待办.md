# 后续目标与待办

本文是唯一长期 TODO。状态使用：未开始、进行中、已完成、暂缓。

## TODO-001 生产流程收敛

- 状态：进行中。
- 优先级：P0。
- 目标：统一为通用视频主流程。
- 验收：顶部流程固定，用户无需编辑导演计划或分镜表。

## TODO-002 资源提取草稿审核

- 状态：进行中。
- 优先级：P0。
- 目标：AI 提取先进入可编辑草稿。
- 验收：支持编辑、合并、替换、跳过和部分入库。

## TODO-003 分镜可选化

- 状态：未开始。
- 优先级：P0。
- 目标：不拆分时保存一条真实分镜。
- 验收：不增加制作模式，新数据不使用空 storyboardIds。

## TODO-004 分镜画面候选版本

- 状态：未开始。
- 优先级：P1。
- 目标：分镜图支持候选和正式选择。
- 验收：支持选择、删除、单项重试和生成信息查看。

## TODO-005 视频输入槽位增强

- 状态：进行中。
- 优先级：P1。
- 目标：按模型显示视频操作和素材槽位。
- 验收：参考素材可增删替换，候选展示完整参数和错误。

## TODO-006 导出校验和诊断

- 状态：未开始。
- 优先级：P1。
- 目标：导出前明确全部阻塞项。
- 验收：阻塞项可定位对象，导出记录 payload、快照和错误。

## TODO-007 任务中心生产诊断

- 状态：未开始。
- 优先级：P1。
- 目标：任务详情展示步骤、对象和调用信息。
- 验收：失败可定位、可查看参数并提供单项重试入口。

## TODO-008 生成结果追溯

- 状态：未开始。
- 优先级：P1。
- 目标：统一记录结果来源。
- 验收：资源图、分镜图、视频和导出可查看模型、Prompt、参数、参考素材、规则和任务。

## TODO-009 Production Agent 工具化写入

- 状态：进行中。
- 优先级：P1。
- 目标：正式写入只走工具或明确 API。
- 验收：参数可校验，写入有权限和幂等控制，子 Agent 不直写数据库。

## TODO-010 剪辑和时间线

- 状态：暂缓。
- 优先级：P2。
- 目标：补充基础视频、音频和字幕编排。
- 验收：支持顺序、时长和预览且不影响现有草稿导出。

## TODO-011 手册和 Prompt 影响链路

- 状态：未开始。
- 优先级：P2。
- 目标：显示规则如何影响生成结果。
- 验收：资源、分镜、视频和任务详情可查看规则摘要。

## TODO-012 清理旧命名和入口

- 状态：未开始。
- 优先级：P2。
- 目标：用户界面统一通用视频语义。
- 验收：不强化 scriptPlan/storyboardTable，统一使用视频片段。

## TODO-013 生产步骤规则与手册入口

- 状态：进行中。
- 优先级：P1。
- 目标：每一步展示实际读取的规则。
- 验收：入口与快照只记录真正参与的规则、手册和 Prompt。

## TODO-014 统一结构化模型调用

- 状态：进行中。
- 优先级：P0。
- 目标：资源提取和分镜拆分共用 structured.ts。
- 验收：统一 resultTool、JSON 解析、JSON-only retry、归一化、校验和诊断。

## TODO-015 统一模型能力与项目默认值

- 状态：进行中。
- 优先级：P0。
- 目标：打通设置默认、项目默认和单次任务参数。
- 验收：图片质量正确联动，视频参数合法，设置变化不影响已有项目。

